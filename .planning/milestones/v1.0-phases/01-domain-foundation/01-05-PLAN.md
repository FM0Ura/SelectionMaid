---
phase: "01-domain-foundation"
plan: "05"
type: tdd
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - tests/stubs/__init__.py
  - tests/stubs/adapters.py
  - tests/domain/test_models.py
  - tests/domain/test_service.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-06
  - ARCH-07

must_haves:
  truths:
    - "pytest collects and passes all tests in tests/domain/ with zero errors"
    - "D-04: Stub adapters live in tests/stubs/adapters.py — reusable across all phases, NOT inlined in individual test files"
    - "Stub adapters satisfy their respective Protocols structurally (mypy confirms)"
    - "ExtractionService.process() with stub adapters returns ExtractionResult with correct shape"
    - "Exception wrapping is verified: non-domain exception from stub extractor becomes ExtractionError"
    - "All CHUNK-03 fields are present and non-None in the returned DocumentChunk from stub"
    - "All META-01 fields are present and non-None in the returned DocumentMetadata from stub"
    - "Domain models frozen constraint is verified: mutation attempt raises FrozenInstanceError"
    - "mypy --strict passes on all test files"
  artifacts:
    - path: "tests/stubs/adapters.py"
      provides: "Reusable stub adapters for all four ports; NOT inlined in test files"
      exports:
        - StubExtractor
        - StubFilter
        - StubChunker
        - StubEnricher
      contains: "class StubExtractor"
    - path: "tests/domain/test_models.py"
      provides: "Tests for frozen dataclass constraints and field presence"
    - path: "tests/domain/test_service.py"
      provides: "Tests for ExtractionService pipeline and exception wrapping"
  key_links:
    - from: "tests/domain/test_service.py"
      to: "tests/stubs/adapters.py"
      via: "imports StubExtractor, StubFilter, StubChunker, StubEnricher"
      pattern: "from tests.stubs.adapters import"
    - from: "tests/stubs/adapters.py"
      to: "src/selection_maid/domain/models.py"
      via: "stub methods return real domain model instances"
      pattern: "return RawDocument\|return DocumentMetadata"
---

<objective>
Write the unit test suite that exercises the full pipeline with stub adapters.

Purpose: This is the acceptance gate for Phase 1. Tests prove that domain models are frozen, that ExtractionService orchestrates the pipeline correctly, that exception wrapping works at each boundary, and that the output ExtractionResult has the correct shape. Stubs live in tests/stubs/adapters.py (reusable across phases 2–7, never inlined).

Output: tests/stubs/adapters.py with four stub adapter classes; tests/domain/test_models.py with frozen dataclass tests; tests/domain/test_service.py with pipeline and exception tests. All passing via `uv run pytest tests/domain/`.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/01-domain-foundation/01-CONTEXT.md

Depends on all prior plans. Full interface summary:

From src/selection_maid/domain/models.py (plan 01-01):
  @dataclass(frozen=True)
  class RawInput:
      path: Path
      filename: str
      mime_type: str

  @dataclass(frozen=True)
  class RawDocument:
      content: str
      filename: str
      page_count: int
      format: str

  @dataclass(frozen=True)
  class DocumentChunk:
      chunk_id: str
      content: str
      page_start: int
      page_end: int
      section_title: str
      chunk_index: int
      total_chunks: int
      word_count: int

  @dataclass(frozen=True)
  class DocumentMetadata:
      title: str
      author: str
      language: str
      document_type: str
      page_count: int
      chunk_count: int
      ingestion_date: datetime

  @dataclass(frozen=True)
  class ExtractionResult:
      metadata: DocumentMetadata
      chunks: tuple[DocumentChunk, ...]

From src/selection_maid/domain/ports.py (plan 01-02):
  class ExtractorPort(Protocol):
      def extract(self, document: RawInput) -> RawDocument: ...
  class FilterPort(Protocol):
      def filter(self, document: RawDocument) -> RawDocument: ...
  class ChunkerPort(Protocol):
      def chunk(self, content: str) -> list[DocumentChunk]: ...
  class MetadataEnricherPort(Protocol):
      def enrich(self, raw: RawDocument, chunks: list[DocumentChunk]) -> DocumentMetadata: ...

From src/selection_maid/service.py (plan 01-03):
  class ExtractionService:
      def __init__(self, extractor: ExtractorPort, filter_: FilterPort, chunker: ChunkerPort, enricher: MetadataEnricherPort) -> None: ...
      def process(self, input: RawInput) -> ExtractionResult: ...

From src/selection_maid/errors.py (plan 01-04):
  SelectionMaidError (base)
  ExtractionError (code="EXT-001")
  FilterError (code="FILT-001")
  ChunkingError (code="CHUNK-001")
  EnrichmentError (code="ENRICH-001")
</context>

<feature>
  <name>ExtractionService pipeline with stub adapters</name>
  <files>tests/stubs/adapters.py, tests/domain/test_models.py, tests/domain/test_service.py</files>
  <behavior>
    Stub adapter behavior:
    - StubExtractor.extract(RawInput) always returns RawDocument(content="# Hello\nContent.", filename=input.filename, page_count=1, format="pdf")
    - StubFilter.filter(RawDocument) returns the document unchanged (identity filter)
    - StubChunker.chunk(content: str) always returns [DocumentChunk(chunk_id="chunk-0", content=content, page_start=1, page_end=1, section_title="Hello", chunk_index=0, total_chunks=1, word_count=2)]
    - StubEnricher.enrich(raw, chunks) returns DocumentMetadata(title="Test Doc", author="Author", language="en", document_type="article", page_count=raw.page_count, chunk_count=len(chunks), ingestion_date=datetime.now())

    Pipeline tests:
    - test_process_returns_extraction_result: ExtractionService(stubs).process(RawInput(...)) returns ExtractionResult instance
    - test_result_chunks_is_tuple: result.chunks is a tuple (not list)
    - test_result_has_one_chunk: result.chunks has length 1 (stub returns one chunk)
    - test_chunk_has_all_required_fields: result.chunks[0] has non-None chunk_id, content, page_start, page_end, section_title, chunk_index, total_chunks, word_count
    - test_metadata_has_all_required_fields: result.metadata has non-None title, author, language, document_type, page_count, chunk_count, ingestion_date
    - test_metadata_chunk_count_matches: result.metadata.chunk_count == len(result.chunks)

    Exception wrapping tests:
    - test_non_domain_extractor_exception_becomes_extraction_error: when StubExtractor.extract raises ValueError, ExtractionService.process raises ExtractionError
    - test_domain_error_propagates_unchanged: when StubExtractor.extract raises ExtractionError directly, ExtractionService.process re-raises the same ExtractionError instance
    - test_chunker_exception_becomes_chunking_error: when StubChunker.chunk raises RuntimeError, process raises ChunkingError

    Frozen model tests (in test_models.py):
    - test_raw_input_is_frozen: attempt to set raw_input.filename = "x" raises FrozenInstanceError
    - test_document_chunk_is_frozen: attempt to set chunk.chunk_index = 99 raises FrozenInstanceError
    - test_extraction_result_chunks_is_tuple: isinstance(result.chunks, tuple) is True
  </behavior>
  <implementation>
    Step 1 (RED): Write all test functions with assertions before implementing stubs. Run `uv run pytest tests/domain/ -x` — expect ImportError or AttributeError since stubs don't exist yet.

    Step 2 (GREEN): Implement tests/stubs/adapters.py with StubExtractor, StubFilter, StubChunker, StubEnricher. Create failing-adapter variants as local classes inside test functions (not in stubs/adapters.py) for exception wrapping tests.

    Step 3: Run `uv run pytest tests/domain/ -v` — all tests must pass.

    Step 4: Run `uv run mypy tests/ --strict` — must exit 0.

    Stub location rule (per D-04): StubExtractor, StubFilter, StubChunker, StubEnricher are in tests/stubs/adapters.py. Failing variants (e.g., ExtractorThatRaises) are local classes inside the specific test function that needs them — they are one-off fixtures, not reusable stubs.

    pytest fixture pattern: Use `@pytest.fixture(scope="session")` for stub instances (matches CLAUDE.md pattern for session-scoped fixtures, even though no model loading occurs here — establish the pattern for Phase 2).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared stub adapters in tests/stubs/adapters.py</name>
  <read_first>
    .planning/phases/01-domain-foundation/01-CONTEXT.md — decision D-04 (stubs in tests/stubs/, NOT inlined)
    src/selection_maid/domain/models.py — verify all model fields before writing stub return values
    src/selection_maid/domain/ports.py — verify port method signatures before writing stub implementations
    tests/stubs/__init__.py — verify stubs package exists
  </read_first>
  <files>
    tests/stubs/adapters.py
  </files>
  <action>
    Create `tests/stubs/adapters.py` implementing four stub classes that satisfy their respective Protocols structurally (no inheritance from Protocol required).

    Imports: `from __future__ import annotations`, `from datetime import datetime`, `from pathlib import Path`, `from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata`.

    `class StubExtractor` — method `extract(self, document: RawInput) -> RawDocument`: returns `RawDocument(content="# Hello\nContent.", filename=document.filename, page_count=1, format="pdf")`.

    `class StubFilter` — method `filter(self, document: RawDocument) -> RawDocument`: returns `document` unchanged (identity).

    `class StubChunker` — method `chunk(self, content: str) -> list[DocumentChunk]`: returns a list containing exactly one `DocumentChunk(chunk_id="chunk-0", content=content, page_start=1, page_end=1, section_title="Hello", chunk_index=0, total_chunks=1, word_count=2)`.

    `class StubEnricher` — method `enrich(self, raw: RawDocument, chunks: list[DocumentChunk]) -> DocumentMetadata`: returns `DocumentMetadata(title="Test Doc", author="Author", language="en", document_type="article", page_count=raw.page_count, chunk_count=len(chunks), ingestion_date=datetime.now())`.

    Do NOT add `@pytest.fixture` decorators to stub classes — fixtures are defined in test files using these classes. Stubs are plain Python classes.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid && uv run python -c "from tests.stubs.adapters import StubExtractor, StubFilter, StubChunker, StubEnricher; print('stubs OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from tests.stubs.adapters import StubExtractor, StubFilter, StubChunker, StubEnricher; print('stubs OK')"` prints `stubs OK`
    - `grep "class StubExtractor\|class StubFilter\|class StubChunker\|class StubEnricher" tests/stubs/adapters.py | wc -l` returns 4
    - `grep "def extract\|def filter\|def chunk\|def enrich" tests/stubs/adapters.py | wc -l` returns 4 (one method per stub)
    - No inheritance from Protocol classes: `grep "ExtractorPort\|FilterPort\|ChunkerPort\|MetadataEnricherPort" tests/stubs/adapters.py` returns empty (stubs are structural, not nominal)
  </acceptance_criteria>
  <done>Four stub adapter classes in tests/stubs/adapters.py; importable; satisfy port signatures structurally; no Protocol inheritance.</done>
</task>

<task type="auto">
  <name>Task 2: Write and run domain unit tests (RED-GREEN cycle)</name>
  <read_first>
    tests/stubs/adapters.py — read stub implementations before writing tests that depend on their output
    src/selection_maid/service.py — read ExtractionService.__init__ and process() signatures
    src/selection_maid/errors.py — read error class hierarchy for exception wrapping tests
    src/selection_maid/domain/models.py — read all field names for assertion specifics
    tests/domain/__init__.py — verify test package exists
  </read_first>
  <files>
    tests/domain/test_models.py,
    tests/domain/test_service.py
  </files>
  <action>
    Create two test files following the RED-GREEN-REFACTOR cycle.

    RED phase: Write test function bodies with all assertions first, before confirming they pass.

    tests/domain/test_models.py — tests for frozen dataclasses:
      Import: `import dataclasses`, `import pytest`, `from pathlib import Path`, `from datetime import datetime`, `from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult`.

      `test_raw_input_is_frozen`: Create `RawInput(path=Path("/tmp/f.pdf"), filename="f.pdf", mime_type="application/pdf")`. Assert `pytest.raises(dataclasses.FrozenInstanceError)` when executing `object.__setattr__(raw_input, "filename", "changed")` — use `object.__setattr__` to bypass Python's assignment syntax which mypy would reject for frozen dataclasses.

      `test_document_chunk_has_all_chunk03_fields`: Create a DocumentChunk with all 8 fields. Assert `hasattr(chunk, field)` for each of: chunk_id, content, page_start, page_end, section_title, chunk_index, total_chunks, word_count.

      `test_document_metadata_has_all_meta01_fields`: Create a DocumentMetadata. Assert `hasattr(meta, field)` for each of: title, author, language, document_type, page_count, chunk_count, ingestion_date.

      `test_extraction_result_chunks_is_tuple`: Create ExtractionResult with a tuple of one DocumentChunk. Assert `isinstance(result.chunks, tuple)`.

    tests/domain/test_service.py — tests for ExtractionService pipeline:
      Imports: `import pytest`, `from pathlib import Path`, `from selection_maid.service import ExtractionService`, `from selection_maid.domain.models import RawInput, ExtractionResult, DocumentChunk, DocumentMetadata`, `from selection_maid.errors import ExtractionError, ChunkingError, SelectionMaidError`, `from tests.stubs.adapters import StubExtractor, StubFilter, StubChunker, StubEnricher`.

      Session-scoped fixture `stub_service`: returns `ExtractionService(StubExtractor(), StubFilter(), StubChunker(), StubEnricher())`.

      Session-scoped fixture `raw_input`: returns `RawInput(path=Path("/tmp/test.pdf"), filename="test.pdf", mime_type="application/pdf")`.

      `test_process_returns_extraction_result(stub_service, raw_input)`: Assert `isinstance(stub_service.process(raw_input), ExtractionResult)`.

      `test_result_chunks_is_tuple(stub_service, raw_input)`: result = process(); assert `isinstance(result.chunks, tuple)`.

      `test_result_has_one_chunk(stub_service, raw_input)`: assert `len(result.chunks) == 1`.

      `test_chunk_has_all_required_fields(stub_service, raw_input)`: get `chunk = result.chunks[0]`; assert chunk.chunk_id, chunk.page_start, chunk.page_end, chunk.section_title, chunk.chunk_index == 0, chunk.total_chunks == 1, chunk.word_count > 0.

      `test_metadata_has_all_required_fields(stub_service, raw_input)`: get `meta = result.metadata`; assert meta.title, meta.author, meta.language == "en", meta.document_type, meta.page_count >= 0, meta.chunk_count == 1, meta.ingestion_date is not None.

      `test_metadata_chunk_count_matches(stub_service, raw_input)`: assert `result.metadata.chunk_count == len(result.chunks)`.

      Exception wrapping tests — define local failing classes inside each test function:

      `test_non_domain_extractor_exception_becomes_extraction_error(raw_input)`:
        Define `class ExtractorThatRaises: def extract(self, document): raise ValueError("raw error")`.
        Build service with this extractor + real stubs for others.
        `pytest.raises(ExtractionError)` when calling process(raw_input).

      `test_domain_error_propagates_unchanged(raw_input)`:
        Define `class ExtractorThatRaisesDomain: def extract(self, document): raise ExtractionError("domain error")`.
        `pytest.raises(ExtractionError)` — verify it is the same type (not wrapped again).

      `test_chunker_exception_becomes_chunking_error(raw_input)`:
        Define `class ChunkerThatRaises: def chunk(self, content): raise RuntimeError("chunk fail")`.
        Build service with StubExtractor, StubFilter, ChunkerThatRaises, StubEnricher.
        `pytest.raises(ChunkingError)` when calling process(raw_input).

    GREEN phase: Run `uv run pytest tests/domain/ -v` — all tests must pass. Fix any failures.

    REFACTOR: Remove any duplication. Ensure fixture scope is `session` on stub_service and raw_input. Run mypy: `uv run mypy tests/ --strict`.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid && uv run pytest tests/domain/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/domain/ -v` exits 0 with all tests passing (no FAILED, no ERROR lines)
    - `uv run pytest tests/domain/ -v` output shows at least 10 test functions collected
    - `uv run pytest tests/domain/test_service.py -k "raises" -v` collects and passes at least 3 exception wrapping tests
    - `uv run mypy tests/ --strict` exits 0
    - `grep "def test_" tests/domain/test_models.py | wc -l` returns at least 4
    - `grep "def test_" tests/domain/test_service.py | wc -l` returns at least 9
    - `grep "FrozenInstanceError" tests/domain/test_models.py` matches (frozen constraint test present)
    - `grep "from tests.stubs.adapters import" tests/domain/test_service.py` matches (stubs imported from stubs package, not inlined)
  </acceptance_criteria>
  <done>All domain unit tests pass; exception wrapping tests confirm boundary behavior; stubs imported from tests/stubs/adapters (not inlined); mypy strict passes on test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→service | Tests invoke ExtractionService directly; stub adapters simulate trust boundary crossings |
| stub→domain | Stubs return real domain model instances; tests verify the shape of those instances |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | test fixtures mutating shared state | mitigate | Session-scoped fixtures use frozen dataclasses as inputs and outputs; stubs are stateless; no shared mutable state between tests |
| T-01-05-02 | Repudiation | Missing exception wrapping test | mitigate | Three explicit exception wrapping tests verify that non-domain exceptions never leak from ExtractionService; this validates the security of the boundary contract |
| T-01-05-SC | Tampering | Dev dependency installs (pytest, mypy, ruff) | mitigate | These are well-known tools from PSF/trusted maintainers; run `uv add --dev pytest mypy ruff` with locked versions in uv.lock; review uv.lock diff before committing |
</threat_model>

<verification>
Final phase verification (run after all 5 plans complete):

1. `uv run pytest tests/domain/ -v` exits 0 with all tests green
2. `uv run mypy src/selection_maid/ tests/ --strict` exits 0
3. `uv run python -c "from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult; from selection_maid.domain.ports import ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort; from selection_maid.service import ExtractionService; from selection_maid.errors import SelectionMaidError, ExtractionError; print('full domain importable')"` prints `full domain importable`
4. `grep "docling\|fastapi\|pydantic" src/selection_maid/service.py src/selection_maid/domain/models.py src/selection_maid/domain/ports.py src/selection_maid/errors.py` returns empty (no external library imports in domain layer)
</verification>

<success_criteria>
- tests/stubs/adapters.py has StubExtractor, StubFilter, StubChunker, StubEnricher (reusable, not inlined)
- tests/domain/test_models.py tests frozen constraint and field presence for all CHUNK-03 and META-01 fields
- tests/domain/test_service.py tests the full pipeline, output shape, and exception wrapping at each boundary
- uv run pytest tests/domain/ exits 0
- uv run mypy src/ tests/ --strict exits 0
- All 6 Phase 1 requirements verified end-to-end: ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-06, ARCH-07
</success_criteria>

<output>
Create `.planning/phases/01-domain-foundation/01-05-SUMMARY.md` when done
</output>
