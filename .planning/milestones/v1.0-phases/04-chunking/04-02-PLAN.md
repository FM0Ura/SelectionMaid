---
phase: 04-chunking
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/selection_maid/adapters/chunker/markdown.py, tests/adapters/chunker/test_markdown_chunker.py]
autonomous: true
requirements: [CHUNK-01, CHUNK-03]
user_setup: []

must_haves:
  truths:
    - "Document with H1/H2 headings is split into chunks starting at each heading"
    - "Pre-heading text is preserved as the first chunk with an empty section title"
    - "Large sections are subdivided by paragraph boundaries to respect token limits"
    - "Each chunk contains valid UUID v4, correct word count, and consistent indexing"
  artifacts:
    - path: "src/selection_maid/adapters/chunker/markdown.py"
      provides: "Heading-based splitting logic and metadata generation"
    - path: "tests/adapters/chunker/test_markdown_chunker.py"
      provides: "Unit tests for heading split and large section handling"
  key_links:
    - from: "src/selection_maid/adapters/chunker/markdown.py"
      to: "uuid.uuid4"
      via: "chunk_id generation"
---

<objective>
Implement the primary chunking strategy: splitting Markdown content by H1 and H2 headings.
This includes handling text before the first heading, subdividing large sections that
exceed the token budget by paragraph, and generating all mandatory metadata fields per chunk.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chunking/04-CONTEXT.md
@src/selection_maid/adapters/chunker/markdown.py
@src/selection_maid/domain/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement heading-based split logic</name>
  <files>src/selection_maid/adapters/chunker/markdown.py, tests/adapters/chunker/test_markdown_chunker.py</files>
  <behavior>
    - Test 1: Split on H1 (`# Title`) and H2 (`## Section`). Headings H3+ should NOT trigger a split.
    - Test 2: Pre-heading text should be captured in a chunk with `section_title=""`.
    - Test 3: `section_title` should be the heading text without the `#` prefix.
    - Test 4: Basic metadata: `page_start=0`, `page_end=0` (D-53), `chunk_id` as UUID (D-54), `word_count` via `len(split())` (D-55).
  </behavior>
  <action>
    1. Implement heading detection using regex `^#{1,2}\s+(.+)` (D-45).
    2. Iterate through lines, accumulating content until a heading is found.
    3. When a heading is found, finalize the previous chunk and start a new one with the heading as `section_title`.
    4. Ensure pre-heading text is handled correctly (D-46).
  </action>
  <verify>
    <automated>pytest tests/adapters/chunker/test_markdown_chunker.py</automated>
  </verify>
  <done>Markdown content correctly split by H1/H2 headings with initial metadata</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement large section subdivision</name>
  <files>src/selection_maid/adapters/chunker/markdown.py, tests/adapters/chunker/test_markdown_chunker.py</files>
  <behavior>
    - Test 1: If content between headings exceeds token budget, split by paragraph boundary (double newline).
    - Test 2: Subdivision should NOT break inside a paragraph.
    - Test 3: Subdivided chunks must retain the `section_title` of the parent heading (D-special).
  </behavior>
  <action>
    Implement D-47 and D-48: subdivide sections by paragraph boundaries if the accumulated content exceeds `max_tokens` using `tiktoken` to accurately measure token count.
  </action>
  <verify>
    <automated>pytest tests/adapters/chunker/test_markdown_chunker.py</automated>
  </verify>
  <done>Large sections subdivided by paragraph boundary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown Content → Chunker | Untrusted string input from previous pipeline stages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | MarkdownChunker.chunk | mitigate | Paragraph boundary check prevents infinite loop; tiktoken has built-in limits |
</threat_model>

<verification>
Run the unit tests to verify that headings are detected and large sections are split appropriately.
</verification>

<success_criteria>
1. All heading split unit tests pass.
2. Content before first heading is preserved.
3. Subdivided sections keep their section title.
4. Word count and UUID are correctly populated.
</success_criteria>

<output>
Create `.planning/phases/04-chunking/04-02-SUMMARY.md` when done
</output>
