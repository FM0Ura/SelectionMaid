---
phase: 06-http-api-layer
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [pyproject.toml, src/selection_maid/adapters/http/router.py, src/selection_maid/adapters/http/app.py, tests/adapters/http/conftest.py, tests/adapters/http/test_router.py]
autonomous: true
requirements: [API-01, API-02, ARCH-05]
user_setup:
  - service: local
    why: "Install system dependencies for libmagic if not present"
    dashboard_config:
      - task: "Ensure libmagic is installed (e.g., sudo apt install libmagic1). For Windows/macOS, python-magic-bin is used instead."
must_haves:
  truths:
    - "Router uses factory pattern to capture ExtractionService (ARCH-05)"
    - "Lifespan manager initializes app state and singleton converter (D-84)"
    - "Health endpoint returns system metrics (API-02)"
    - "Test infrastructure is ready for subsequent validation tests"
  artifacts:
    - path: "src/selection_maid/adapters/http/app.py"
      provides: "FastAPI application instance and lifecycle management"
    - path: "src/selection_maid/adapters/http/router.py"
      provides: "HTTP route definitions"
    - path: "tests/adapters/http/conftest.py"
      provides: "TestClient fixture"
---

<objective>
Implement the FastAPI router factory, basic application structure, and test infrastructure.

Purpose: Create the entry point for the HTTP API and the harness to test it immediately.
Output: `router.py`, `app.py`, and initial test suite.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-http-api-layer/06-01-SUMMARY.md
@.planning/phases/06-http-api-layer/06-CONTEXT.md
@.planning/phases/06-http-api-layer/06-RESEARCH.md
@src/selection_maid/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies</name>
  <files>pyproject.toml</files>
  <action>
    Add required HTTP dependencies:
    - fastapi, uvicorn, python-multipart (D-83, RESEARCH)
    - psutil (D-78, for RSS metrics)
    - python-magic (D-81, for file validation)
    
    Note: On Windows and macOS, `python-magic` requires the `python-magic-bin` package instead of the system `libmagic`. Use `uv add python-magic-bin` if developing on those platforms.
  </action>
  <verify>
    <automated>uv add fastapi uvicorn python-multipart psutil python-magic</automated>
  </verify>
  <done>Environment ready for FastAPI development.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Router Factory with endpoints</name>
  <files>src/selection_maid/adapters/http/router.py</files>
  <action>
    Implement `build_router(service: ExtractionService) -> APIRouter` (D-85).
    Handlers must capture `service` via closure.

    Endpoints (skeleton):
    - `GET /health`: Returns `HealthResponse`. 
      - Calculate `uptime_seconds` using `request.app.state.start_time` (D-86, D-78).
      - Calculate `rss_mb` using `psutil` (D-78).
      - Include version via `importlib.metadata.version("selectionmaid")` (D-78).
    - `POST /ingest`: Returns `ExtractionResponse`. 
      - Basic skeleton accepting `file: UploadFile`. Implementation details follow in later plans.
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.adapters.http.router import build_router; from unittest.mock import Mock; build_router(Mock())"</automated>
  </verify>
  <done>Router factory correctly captures service and defines routes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement App Factory and Lifespan</name>
  <files>src/selection_maid/adapters/http/app.py</files>
  <action>
    Implement `create_app() -> FastAPI` and `app = create_app()` for uvicorn (D-83).
    Use `@asynccontextmanager` for lifespan (D-84).

    Lifespan duties:
    - Set `app.state.start_time = datetime.now(UTC)` (D-86).
    - Instantiate `DocumentConverter` (singleton).
    - Instantiate ports, `ExtractionService`, and call `build_router(service)`.
    - Include router in the app.
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.adapters.http.app import app; print(app.title)"</automated>
  </verify>
  <done>FastAPI application wired and ready to run.</done>
</task>

<task type="auto">
  <name>Task 4: Setup Test Infrastructure</name>
  <files>tests/adapters/http/conftest.py, tests/adapters/http/test_router.py</files>
  <action>
    1. Create `tests/adapters/http/conftest.py` providing a `client` fixture using `fastapi.testclient.TestClient`.
    2. Create `tests/adapters/http/test_router.py` with a basic `test_health` that checks status 200 and schema validity.
  </action>
  <verify>
    <automated>pytest tests/adapters/http/test_router.py</automated>
  </verify>
  <done>Testing harness initialized and health check verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| External → API | Public endpoints exposed via FastAPI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Denial of Service | /health | mitigate | Use efficient psutil calls and request.app.state to minimize overhead |
</threat_model>

<verification>
Start the app with uvicorn (background) and test `curl localhost:8000/health`.
</verification>

<success_criteria>
- [ ] `router.py` and `app.py` implemented.
- [ ] Dependencies installed.
- [ ] `GET /health` returns valid JSON with RSS and version.
- [ ] `pytest tests/adapters/http/test_router.py` passes.
</success_criteria>

<output>
Create `.planning/phases/06-http-api-layer/06-02-SUMMARY.md` when done
</output>
