---
phase: 06-http-api-layer
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [src/selection_maid/config.py, src/selection_maid/adapters/http/router.py, src/selection_maid/adapters/http/error_map.py, tests/adapters/http/test_router.py]
autonomous: true
requirements: [API-03]
must_haves:
  truths:
    - "Files larger than 50MB are rejected with 413 (D-79)"
    - "Unsupported MIME types are rejected with 415 (D-80)"
    - "Magic bytes verification detects spoofed files with 422 (D-81)"
    - "Validation occurs before domain processing (fail fast)"
  artifacts:
    - path: "src/selection_maid/adapters/http/error_map.py"
      provides: "Domain Error Code to HTTP Status mapping"
    - path: "tests/adapters/http/test_router.py"
      provides: "Validation tests (size, MIME, magic)"
---

<objective>
Implement 3-layer file validation and HTTP configuration.

Purpose: Protect the system from malicious or oversized files and provide clear error messages to consumers.
Output: Extended `GlobalConfig`, validation logic in `/ingest`, and domain-to-HTTP error mapping.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-http-api-layer/06-02-SUMMARY.md
@.planning/phases/06-http-api-layer/06-CONTEXT.md
@src/selection_maid/config.py
@src/selection_maid/errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GlobalConfig with HttpConfig</name>
  <files>src/selection_maid/config.py</files>
  <action>
    Define `HttpConfig` dataclass (D-89, D-90):
    - `max_file_bytes: int = 52_428_800` (50MB)
    - `allowed_mime_types: list[str] = [...]` (PDF, DOCX, HTML)

    Add `http: HttpConfig` to `GlobalConfig`.
    Update `get_config()` to parse `[http]` section from TOML.
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.config import get_config; cfg = get_config(); print(cfg.http.max_file_bytes)"</automated>
  </verify>
  <done>HTTP parameters are now configurable via config.toml.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Error Mapping</name>
  <files>src/selection_maid/adapters/http/error_map.py</files>
  <action>
    Create `ERROR_CODE_TO_HTTP` dictionary (D-82, CONTEXT):
    - "EXT-002" (Unsupported) -> 415
    - "EXT-003" (Timeout) -> 504
    - "UPLOAD-001" (Size) -> 413
    - "UPLOAD-002" (Magic mismatch) -> 422
    - Default/Others -> 500

    Implement a simple helper `get_http_status(error_code: str) -> int`.
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.adapters.http.error_map import ERROR_CODE_TO_HTTP; assert ERROR_CODE_TO_HTTP['EXT-002'] == 415"</automated>
  </verify>
  <done>Domain errors correctly map to HTTP semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement 3-Layer Validation in /ingest</name>
  <files>src/selection_maid/adapters/http/router.py, tests/adapters/http/test_router.py</files>
  <behavior>
    - test_validation_size: Check Content-Length header (if present) against max_file_bytes (413).
    - test_validation_mime: Check UploadFile.content_type against allowed_mime_types (415).
    - test_validation_magic: Read first 2048 bytes and check against python-magic (422).
  </behavior>
  <action>
    1. In `tests/adapters/http/test_router.py`, add failing tests for the validation behaviors described above.
    2. In `src/selection_maid/adapters/http/router.py`, implement the 3-layer validation in the `POST /ingest` handler:
       - Check `Content-Length` header against `max_file_bytes`.
       - Check `UploadFile.content_type` against `allowed_mime_types`.
       - Read first 2048 bytes and verify magic bytes using `python-magic`.
    3. Raise appropriate HTTP exceptions (413, 415, 422) with structured body on failure.
    4. Ensure validation fails fast before any processing occurs.
  </action>
  <verify>
    <automated>pytest tests/adapters/http/test_router.py -k "validation"</automated>
  </verify>
  <done>Uploaded files are strictly validated before processing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted file upload |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service | /ingest | mitigate | Strict 50MB limit and fail-fast header validation |
| T-06-04 | Tampering | /ingest | mitigate | Magic bytes validation to prevent file extension spoofing |
</threat_model>

<verification>
Test with `curl` using:
1. File > 50MB
2. Fake PDF (text file renamed to .pdf)
3. Unsupported extension (.txt)
</verification>

<success_criteria>
- [ ] Config supports `max_file_bytes` and `allowed_mime_types`.
- [ ] Magic bytes validation integrated.
- [ ] Correct HTTP status codes for all validation failures.
</success_criteria>

<output>
Create `.planning/phases/06-http-api-layer/06-03-SUMMARY.md` when done
</output>
