---
phase: 06-http-api-layer
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified: [src/selection_maid/adapters/http/router.py, tests/adapters/http/test_router.py]
autonomous: true
requirements: [API-01, API-02, API-03, ARCH-05]
must_haves:
  truths:
    - "Docling processing runs in threadpool to avoid event loop blocking (D-88)"
    - "Temporary files are deleted even if processing fails (D-87)"
    - "Full API integration tests cover happy path and error cases"
    - "Multiple simultaneous requests are handled without blocking (Success Criteria #5)"
  artifacts:
    - path: "tests/adapters/http/test_router.py"
      provides: "HTTP API Integration and Concurrency testing"
---

<objective>
Complete the /ingest implementation with threading and expand integration tests.

Purpose: Ensure the API is responsive, resource-efficient, and fully functional across all layers.
Output: Responsive /ingest endpoint and comprehensive test suite.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-http-api-layer/06-03-SUMMARY.md
@.planning/phases/06-http-api-layer/06-CONTEXT.md
@.planning/phases/06-http-api-layer/06-RESEARCH.md
@src/selection_maid/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement /ingest with Threading and Tempfiles</name>
  <files>src/selection_maid/adapters/http/router.py</files>
  <action>
    Complete `POST /ingest` implementation:
    1. After validation, write `UploadFile` to `tempfile.NamedTemporaryFile(delete=False, suffix=ext)` (D-87).
    2. Derived `ext` from MIME type.
    3. Construct `RawInput` using the tempfile path.
    4. Call `await run_in_threadpool(service.process, raw_input)` (D-88).
    5. Convert `ExtractionResult` to `ExtractionResponse` using `model_validate`.
    6. Ensure `os.unlink()` is called in a `finally` block to clean up tempfiles.
    7. Map domain exceptions to HTTP responses using `error_map`.
  </action>
  <verify>
    <automated>python3 -c "import os, tempfile; from selection_maid.adapters.http.router import build_router; print('OK')"</automated>
  </verify>
  <done>Ingestion is responsive and handles cleanup correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand Integration and Concurrency Tests</name>
  <files>tests/adapters/http/test_router.py</files>
  <behavior>
    - test_ingest_success: upload valid PDF, returns 200 with chunks (End-to-end integration).
    - test_ingest_error_mapping: mock service to raise SelectionMaidError, check status.
    - test_ingest_concurrency: send two simultaneous requests to POST /ingest (using asyncio.gather) and verify they both complete successfully without blocking the event loop.
  </behavior>
  <action>
    1. Expand `tests/adapters/http/test_router.py` with comprehensive integration tests for the success path.
    2. Implement `test_ingest_concurrency` specifically to verify non-blocking behavior of the Docling threadpool by sending multiple requests concurrently.
    3. Ensure tests cover error mapping from domain to HTTP statuses.
    4. Use small real files (PDF/HTML/DOCX) from existing fixtures or create minimal stubs.
  </action>
  <verify>
    <automated>pytest tests/adapters/http/test_router.py -x</automated>
  </verify>
  <done>Full API suite passing with high coverage of E2E cases and concurrency verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server → OS | Tempfile creation and deletion |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | tempfiles | mitigate | Use NamedTemporaryFile with restricted permissions and ensure deletion in finally block |
</threat_model>

<verification>
Run `pytest tests/adapters/http/` and verify no tempfiles are left behind in `/tmp`.
</verification>

<success_criteria>
- [ ] `/ingest` returns full `ExtractionResponse`.
- [ ] No blocking of event loop (verified by concurrency test).
- [ ] Tempfiles cleaned up reliably.
- [ ] All tests in `test_router.py` pass.
</success_criteria>

<output>
Create `.planning/phases/06-http-api-layer/06-04-SUMMARY.md` when done
</output>
