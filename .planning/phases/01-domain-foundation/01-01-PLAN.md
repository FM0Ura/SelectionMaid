---
phase: "01-domain-foundation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/selection_maid/__init__.py
  - src/selection_maid/domain/__init__.py
  - src/selection_maid/domain/models.py
  - src/selection_maid/adapters/__init__.py
  - src/selection_maid/adapters/extractor/__init__.py
  - src/selection_maid/adapters/filter/__init__.py
  - src/selection_maid/adapters/chunker/__init__.py
  - src/selection_maid/adapters/enricher/__init__.py
  - src/selection_maid/adapters/http/__init__.py
  - tests/__init__.py
  - tests/domain/__init__.py
  - tests/stubs/__init__.py
  - tests/adapters/__init__.py
  - tests/adapters/extractor/__init__.py
  - tests/adapters/filter/__init__.py
  - tests/adapters/chunker/__init__.py
  - tests/adapters/enricher/__init__.py
autonomous: true
requirements:
  - ARCH-07

must_haves:
  truths:
    - "Package `selection_maid` is importable from `src/` layout with `uv run python -c 'import selection_maid'`"
    - "All four domain dataclasses (RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult) are importable from `selection_maid.domain.models`"
    - "All domain dataclasses are frozen (mutation raises FrozenInstanceError)"
    - "Domain models contain zero third-party imports — only `__future__`, `dataclasses`, `datetime`, `pathlib`, `typing` stdlib modules"
    - "mypy --strict passes on src/selection_maid/domain/models.py with zero errors"
  artifacts:
    - path: "src/selection_maid/domain/models.py"
      provides: "All domain dataclasses: RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult"
      contains: "@dataclass(frozen=True)"
    - path: "src/selection_maid/adapters/extractor/__init__.py"
      provides: "Empty adapter subpackage marker"
    - path: "tests/stubs/__init__.py"
      provides: "Shared stub package marker"
  key_links:
    - from: "src/selection_maid/domain/models.py"
      to: "ExtractionResult"
      via: "chunks: tuple[DocumentChunk, ...]"
      pattern: "chunks.*tuple\\[DocumentChunk"
    - from: "src/selection_maid/domain/models.py"
      to: "DocumentMetadata"
      via: "ingestion_date: datetime"
      pattern: "ingestion_date.*datetime"
---

<objective>
Create the hexagonal package scaffold and all frozen domain dataclasses.

Purpose: Establish the directory structure that all subsequent phases depend on, and define the domain value objects (RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult) with zero external dependencies. This is the foundation every adapter and service imports from.

Output: `src/selection_maid/` tree with `domain/models.py` containing all dataclasses; `adapters/` subpackage tree with empty `__init__.py` files; `tests/` mirror structure with `stubs/` directory; `pyproject.toml` updated with tool config sections.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/PROJECT.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/01-domain-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hexagonal package scaffold and configure pyproject.toml</name>
  <read_first>
    pyproject.toml — read entire file before adding sections; do not duplicate existing keys
  </read_first>
  <files>
    pyproject.toml,
    src/selection_maid/__init__.py,
    src/selection_maid/domain/__init__.py,
    src/selection_maid/adapters/__init__.py,
    src/selection_maid/adapters/extractor/__init__.py,
    src/selection_maid/adapters/filter/__init__.py,
    src/selection_maid/adapters/chunker/__init__.py,
    src/selection_maid/adapters/enricher/__init__.py,
    src/selection_maid/adapters/http/__init__.py,
    tests/__init__.py,
    tests/domain/__init__.py,
    tests/stubs/__init__.py,
    tests/adapters/__init__.py,
    tests/adapters/extractor/__init__.py,
    tests/adapters/filter/__init__.py,
    tests/adapters/chunker/__init__.py,
    tests/adapters/enricher/__init__.py
  </files>
  <action>
    Create the full directory tree for the hexagonal architecture. Every directory listed in files_modified needs an `__init__.py` file (empty or with a module docstring).

    Update pyproject.toml to add the following sections if not already present:

    Under `[build-system]`: use `hatchling` as the build backend (standard uv default): requires = ["hatchling"], build-backend = "hatchling.build".

    Under `[tool.hatch.build.targets.wheel]`: set packages = ["src/selection_maid"] so uv finds the src layout.

    Under `[tool.mypy]`: strict = true, python_version = "3.13", exclude = ["tests/"].

    Under `[tool.ruff]`: line-length = 88, target-version = "py313". Under `[tool.ruff.lint]`: select = ["E", "F", "I", "UP", "B", "SIM"]. Under `[tool.ruff.lint.isort]`: known-first-party = ["selection_maid"].

    Under `[tool.pytest.ini_options]`: testpaths = ["tests"], asyncio_mode = "auto".

    Do NOT add any runtime dependencies to `[project].dependencies` in this task — that is Phase 2's job for Docling. Dev dependencies (pytest, mypy, ruff) are added here via uv add --dev when the pyproject.toml sections are in place.

    Each `__init__.py` file should be a zero-byte file or contain only `"""Package."""` — no imports.

    Per D-03: adapter subpackages created here are empty placeholders; implementation comes in Phases 2–6.
  </action>
  <verify>
    <automated>find /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/src/selection_maid/adapters -name "__init__.py" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find src/selection_maid -name "__init__.py" | wc -l` returns at least 7 (domain, adapters, adapters/extractor, adapters/filter, adapters/chunker, adapters/enricher, adapters/http, plus root)
    - `find tests -name "__init__.py" | wc -l` returns at least 7
    - `pyproject.toml` contains `[tool.mypy]` section with `strict = true`
    - `pyproject.toml` contains `[tool.ruff]` section with `line-length = 88`
    - `pyproject.toml` contains `[tool.pytest.ini_options]` section with `testpaths = ["tests"]`
    - `pyproject.toml` contains `[build-system]` section
  </acceptance_criteria>
  <done>All package directories exist with __init__.py markers; pyproject.toml has all tool config sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement frozen domain dataclasses in models.py</name>
  <read_first>
    .planning/phases/01-domain-foundation/01-CONTEXT.md — decisions D-05 through D-08, and CHUNK-03 and META-01 field lists
    .planning/REQUIREMENTS.md — CHUNK-03 and META-01 required fields
    src/selection_maid/domain/__init__.py — verify package exists before creating models.py
  </read_first>
  <files>
    src/selection_maid/domain/models.py
  </files>
  <behavior>
    - RawInput is frozen and immutable: mutating `path` raises `dataclasses.FrozenInstanceError`
    - RawDocument.content is a str (Markdown blob); page_count=0 is valid when unknown
    - DocumentChunk has exactly 8 fields per CHUNK-03: chunk_id (str), content (str), page_start (int), page_end (int), section_title (str), chunk_index (int), total_chunks (int), word_count (int)
    - DocumentMetadata has exactly 7 fields per META-01: title (str), author (str), language (str — ISO 639-1), document_type (str), page_count (int), chunk_count (int), ingestion_date (datetime)
    - ExtractionResult.chunks is typed as `tuple[DocumentChunk, ...]` (not list)
    - `from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult` succeeds with no ImportError
    - No import outside stdlib: `__future__`, `dataclasses`, `datetime`, `pathlib`, `typing` only
  </behavior>
  <action>
    Create `src/selection_maid/domain/models.py` with `from __future__ import annotations` at the top.

    Import only from stdlib: `dataclasses` (dataclass, field), `datetime` (datetime), `pathlib` (Path), `typing` (no runtime-checkable needed here).

    Define five frozen dataclasses in this order (order matters for forward reference avoidance):

    1. `RawInput` — per D-05: fields `path: Path`, `filename: str`, `mime_type: str`. Decorated with `@dataclass(frozen=True)`.

    2. `RawDocument` — per D-06: fields `content: str`, `filename: str`, `page_count: int`, `format: str`. `page_count` default is NOT set — callers must provide it; 0 is a valid value for unknown page count. Decorated with `@dataclass(frozen=True)`.

    3. `DocumentChunk` — per D-08 and CHUNK-03: fields `chunk_id: str`, `content: str`, `page_start: int`, `page_end: int`, `section_title: str`, `chunk_index: int`, `total_chunks: int`, `word_count: int`. Decorated with `@dataclass(frozen=True)`.

    4. `DocumentMetadata` — per META-01: fields `title: str`, `author: str`, `language: str`, `document_type: str`, `page_count: int`, `chunk_count: int`, `ingestion_date: datetime`. Decorated with `@dataclass(frozen=True)`.

    5. `ExtractionResult` — per D-07: fields `metadata: DocumentMetadata`, `chunks: tuple[DocumentChunk, ...]`. The `chunks` field must use `tuple[DocumentChunk, ...]` type annotation (not list). Decorated with `@dataclass(frozen=True)`.

    No default values on any field (all required). No `field()` with default_factory needed since tuple literals are the caller's responsibility.

    Do NOT add `__post_init__` validation — that belongs in the service layer, not the value objects.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid && uv run python -c "from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult; print('OK')"` prints `OK`
    - `grep -c "frozen=True" src/selection_maid/domain/models.py` returns 5 (all five dataclasses are frozen)
    - `grep -v "^#" src/selection_maid/domain/models.py | grep "^import\|^from" | grep -v "from __future__\|from dataclasses\|from datetime\|from pathlib\|from typing\|import dataclasses\|import datetime\|import pathlib\|import typing"` returns empty (no third-party imports)
    - `grep "chunks.*tuple\[DocumentChunk" src/selection_maid/domain/models.py` matches (ExtractionResult.chunks is tuple, not list)
    - `grep "ingestion_date.*datetime" src/selection_maid/domain/models.py` matches (DocumentMetadata.ingestion_date field present)
    - `grep "chunk_id\|page_start\|page_end\|section_title\|chunk_index\|total_chunks\|word_count" src/selection_maid/domain/models.py | wc -l` returns 7 (all CHUNK-03 required fields present)
    - `uv run python -c "import dataclasses; from selection_maid.domain.models import RawInput; from pathlib import Path; r = RawInput(path=Path('/tmp/f'), filename='f.pdf', mime_type='application/pdf'); dataclasses.replace(r, filename='x')"` raises FrozenInstanceError (frozen enforcement)
  </acceptance_criteria>
  <done>All five frozen dataclasses defined in models.py; importable; zero third-party imports; all CHUNK-03 and META-01 fields present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→source | Developer writes domain models; no runtime external inputs at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | domain/models.py | mitigate | `frozen=True` on all dataclasses prevents mutation after construction; enforced by Python runtime |
| T-01-02 | Information Disclosure | models.py imports | mitigate | Verify zero third-party imports via grep gate in acceptance_criteria; mypy strict confirms no external type leakage |
| T-01-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pyproject.toml sections are config-only; uv dev deps (pytest/mypy/ruff) are well-known tools |
</threat_model>

<verification>
After both tasks complete:

1. `uv run python -c "from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata, ExtractionResult; print('all importable')"` prints `all importable`
2. `grep -c "frozen=True" src/selection_maid/domain/models.py` returns 5
3. `uv run mypy src/selection_maid/domain/models.py --strict` exits 0
4. `find src/selection_maid/adapters -name "__init__.py" | wc -l` returns 5 (one per adapter subpackage)
</verification>

<success_criteria>
- Package scaffold exists: src/selection_maid/ tree with domain/ and adapters/{extractor,filter,chunker,enricher,http}/ subdirectories, all with __init__.py
- tests/ mirrors src/ structure with tests/stubs/ for shared stub adapters
- pyproject.toml has [tool.mypy] strict=true, [tool.ruff] line-length=88, [tool.pytest.ini_options] testpaths=["tests"]
- Five frozen dataclasses importable from selection_maid.domain.models with zero third-party imports
- All CHUNK-03 fields in DocumentChunk, all META-01 fields in DocumentMetadata
- ExtractionResult.chunks typed as tuple[DocumentChunk, ...] not list
</success_criteria>

<output>
Create `.planning/phases/01-domain-foundation/01-01-SUMMARY.md` when done
</output>
