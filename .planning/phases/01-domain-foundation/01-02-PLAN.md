---
phase: "01-domain-foundation"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/selection_maid/domain/ports.py
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04

must_haves:
  truths:
    - "ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort are importable from selection_maid.domain.ports"
    - "All four Protocols satisfy structural typing — a class with matching method signatures satisfies the Protocol without inheriting from it"
    - "No @runtime_checkable decorator on any Protocol — isinstance() is not supported by design"
    - "All port methods are sync (def, not async def)"
    - "mypy --strict passes on ports.py with zero errors"
    - "ports.py imports only from stdlib (typing) and from selection_maid.domain.models"
  artifacts:
    - path: "src/selection_maid/domain/ports.py"
      provides: "Four typing.Protocol definitions: ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort"
      contains: "class ExtractorPort(Protocol)"
  key_links:
    - from: "src/selection_maid/domain/ports.py"
      to: "src/selection_maid/domain/models.py"
      via: "import RawInput, RawDocument, DocumentChunk, DocumentMetadata"
      pattern: "from selection_maid.domain.models import"
    - from: "src/selection_maid/domain/ports.py"
      to: "ExtractorPort"
      via: "extract(self, document: RawInput) -> RawDocument"
      pattern: "def extract.*RawInput.*RawDocument"
---

<objective>
Define the four port contracts as typing.Protocol in ports.py.

Purpose: Port Protocols are the contracts every adapter must satisfy. Defining them now (in Wave 1, parallel to models.py) ensures that ExtractionService (Wave 2, plan 01-03) can import both models and ports without circular dependencies. Per the hexagonal architecture constraint, no adapter type ever appears in ports.py.

Output: `src/selection_maid/domain/ports.py` with ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort as typing.Protocol classes, using only model types from models.py as parameter/return annotations.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/01-domain-foundation/01-CONTEXT.md

Ports depend on models.py (plan 01-01, same wave). Since both are Wave 1, ports.py can be created concurrently with models.py — the executor for this plan creates ports.py with the correct import path; no runtime execution is needed until Wave 2.

Key model types used in port signatures (from 01-01):
- RawInput(path: Path, filename: str, mime_type: str) — entry object for ExtractorPort
- RawDocument(content: str, filename: str, page_count: int, format: str) — output of ExtractorPort, input of FilterPort
- DocumentChunk — output element of ChunkerPort
- DocumentMetadata — output of MetadataEnricherPort
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define four port Protocols in ports.py</name>
  <read_first>
    .planning/phases/01-domain-foundation/01-CONTEXT.md — decisions D-09 through D-14 (all port signatures and Protocol constraints)
    .planning/REQUIREMENTS.md — ARCH-01 through ARCH-04 (one requirement per port)
    src/selection_maid/domain/__init__.py — verify domain package exists before creating ports.py
  </read_first>
  <files>
    src/selection_maid/domain/ports.py
  </files>
  <behavior>
    - A class `class StubExtractor: def extract(self, document: RawInput) -> RawDocument: ...` satisfies ExtractorPort without inheriting from it
    - A class `class StubFilter: def filter(self, document: RawDocument) -> RawDocument: ...` satisfies FilterPort
    - A class `class StubChunker: def chunk(self, content: str) -> list[DocumentChunk]: ...` satisfies ChunkerPort
    - A class `class StubEnricher: def enrich(self, raw: RawDocument, chunks: list[DocumentChunk]) -> DocumentMetadata: ...` satisfies MetadataEnricherPort
    - `isinstance(StubExtractor(), ExtractorPort)` raises TypeError (not runtime_checkable — this is the desired behavior per D-14)
    - `from selection_maid.domain.ports import ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort` succeeds
    - mypy accepts a stub class as satisfying the Protocol via structural subtyping
  </behavior>
  <action>
    Create `src/selection_maid/domain/ports.py` with `from __future__ import annotations` at the top.

    Imports: `from typing import Protocol` and `from selection_maid.domain.models import RawInput, RawDocument, DocumentChunk, DocumentMetadata`.

    Define four Protocol classes. Do NOT use `@runtime_checkable` on any of them (per D-14). Each Protocol has exactly one abstract method:

    1. `class ExtractorPort(Protocol)` — method `extract(self, document: RawInput) -> RawDocument` (per D-09). Body is `...` (ellipsis, standard Protocol abstract method convention).

    2. `class FilterPort(Protocol)` — method `filter(self, document: RawDocument) -> RawDocument` (per D-10). Body is `...`.

    3. `class ChunkerPort(Protocol)` — method `chunk(self, content: str) -> list[DocumentChunk]` (per D-11). Returns `list[DocumentChunk]` (not tuple — the service converts to tuple when building ExtractionResult). Body is `...`.

    4. `class MetadataEnricherPort(Protocol)` — method `enrich(self, raw: RawDocument, chunks: list[DocumentChunk]) -> DocumentMetadata` (per D-12). Body is `...`.

    All methods are sync (`def`, not `async def`) per D-13.

    No `__all__` required but acceptable if added for documentation purposes.

    Do NOT add any other methods, class variables, or `__init__` to any Protocol.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid && uv run python -c "from selection_maid.domain.ports import ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from selection_maid.domain.ports import ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort; print('OK')"` prints `OK`
    - `grep "runtime_checkable" src/selection_maid/domain/ports.py` returns empty (no @runtime_checkable)
    - `grep "async def" src/selection_maid/domain/ports.py` returns empty (all methods are sync)
    - `grep "class ExtractorPort(Protocol)" src/selection_maid/domain/ports.py` matches exactly
    - `grep "class FilterPort(Protocol)" src/selection_maid/domain/ports.py` matches exactly
    - `grep "class ChunkerPort(Protocol)" src/selection_maid/domain/ports.py` matches exactly
    - `grep "class MetadataEnricherPort(Protocol)" src/selection_maid/domain/ports.py` matches exactly
    - `grep "def extract.*RawInput.*RawDocument\|def extract(self, document: RawInput) -> RawDocument" src/selection_maid/domain/ports.py` matches
    - `grep "def chunk.*str.*list\[DocumentChunk\]" src/selection_maid/domain/ports.py` matches (ChunkerPort returns list, not tuple)
    - `grep "def enrich.*RawDocument.*list\[DocumentChunk\].*DocumentMetadata" src/selection_maid/domain/ports.py` matches
    - `uv run mypy src/selection_maid/domain/ports.py --strict` exits 0
    - `grep -v "^#" src/selection_maid/domain/ports.py | grep "^import\|^from" | grep -v "from __future__\|from typing\|from selection_maid"` returns empty (only stdlib typing + own models)
  </acceptance_criteria>
  <done>Four Protocol classes defined in ports.py; all sync; no @runtime_checkable; importable; mypy strict passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol→adapter | Port signatures constrain what adapters can pass through the boundary; no external types cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | domain/ports.py | mitigate | Port signatures reference only domain model types; grep gate in acceptance_criteria confirms no external library types in port signatures |
| T-01-02-02 | Elevation of Privilege | typing.Protocol without @runtime_checkable | accept | Deliberately not runtime_checkable per D-14; prevents accidental isinstance() bypass; mypy enforces structural compliance at type-check time |
| T-01-02-SC | Tampering | No package installs in this plan | accept | ports.py uses only stdlib typing + project-local models; no npm/pip/cargo activity |
</threat_model>

<verification>
After task completes:

1. `uv run python -c "from selection_maid.domain.ports import ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort; print('all ports importable')"` prints `all ports importable`
2. `grep "runtime_checkable" src/selection_maid/domain/ports.py` returns empty
3. `grep "async def" src/selection_maid/domain/ports.py` returns empty
4. `uv run mypy src/selection_maid/domain/ports.py --strict` exits 0
</verification>

<success_criteria>
- ports.py defines ExtractorPort, FilterPort, ChunkerPort, MetadataEnricherPort as typing.Protocol
- All four methods are sync with correct signatures matching D-09 through D-12
- No @runtime_checkable, no async def, no third-party imports
- mypy strict passes
- Satisfies ARCH-01 (ExtractorPort), ARCH-02 (FilterPort), ARCH-03 (ChunkerPort), ARCH-04 (MetadataEnricherPort)
</success_criteria>

<output>
Create `.planning/phases/01-domain-foundation/01-02-SUMMARY.md` when done
</output>
