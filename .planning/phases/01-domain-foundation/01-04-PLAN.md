---
phase: "01-domain-foundation"
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/selection_maid/errors.py
autonomous: true
requirements:
  - ARCH-06

must_haves:
  truths:
    - "SelectionMaidError is the importable base exception for all domain errors"
    - "All six error subclasses are importable from selection_maid.errors"
    - "Each subclass carries a fixed class-level code attribute (e.g., ExtractionError.code == 'EXT-001')"
    - "UnsupportedFormatError carries a format: str field beyond the base fields"
    - "ExtractionTimeoutError is defined (for Phase 2 use) with code='EXT-003'"
    - "errors.py has zero third-party imports"
    - "mypy --strict passes on errors.py"
  artifacts:
    - path: "src/selection_maid/errors.py"
      provides: "Domain error taxonomy: SelectionMaidError + 6 subclasses"
      exports:
        - SelectionMaidError
        - ExtractionError
        - UnsupportedFormatError
        - ExtractionTimeoutError
        - FilterError
        - ChunkingError
        - EnrichmentError
      contains: "class SelectionMaidError(Exception)"
  key_links:
    - from: "src/selection_maid/errors.py"
      to: "src/selection_maid/service.py"
      via: "ExtractionError, FilterError, ChunkingError, EnrichmentError used in exception wrapping"
      pattern: "class ExtractionError.*SelectionMaidError"
    - from: "src/selection_maid/errors.py"
      to: "Phase 6 HTTP adapter"
      via: "code class attribute used in ERROR_CODE_TO_HTTP mapping"
      pattern: "code.*=.*EXT-"
---

<objective>
Implement the domain error taxonomy in errors.py.

Purpose: SelectionMaidError and its subclasses are the cross-cutting exception contract for the entire service. ExtractionService (plan 01-03) uses them for exception wrapping; the HTTP adapter (Phase 6) maps them to HTTP status codes via the code class attribute. Defining all error classes now — including ExtractionTimeoutError for Phase 2 use — ensures the domain error contract is complete from Phase 1.

Output: `src/selection_maid/errors.py` with SelectionMaidError base and six subclasses, all with fixed code class attributes and correct field signatures per D-17 through D-20.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/01-domain-foundation/01-CONTEXT.md

Depends on 01-01 for package structure. This plan runs in Wave 2 parallel to 01-03 (service.py).

Error hierarchy from CONTEXT.md D-17 through D-20:

SelectionMaidError(Exception)
  fields: code (class attr, str), message (str), cause (Exception | None = None)

  ExtractionError(SelectionMaidError)        code = "EXT-001"
  UnsupportedFormatError(SelectionMaidError) code = "EXT-002"  extra field: format: str
  ExtractionTimeoutError(SelectionMaidError) code = "EXT-003"  (defined now, raised in Phase 2)
  FilterError(SelectionMaidError)            code = "FILT-001"
  ChunkingError(SelectionMaidError)          code = "CHUNK-001"
  EnrichmentError(SelectionMaidError)        code = "ENRICH-001"

D-19: No HTTP status in any domain error class.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement domain error taxonomy in errors.py</name>
  <read_first>
    .planning/phases/01-domain-foundation/01-CONTEXT.md — decisions D-17 through D-20 (error hierarchy, field signatures, codes)
    src/selection_maid/__init__.py — verify root package exists before creating errors.py
  </read_first>
  <files>
    src/selection_maid/errors.py
  </files>
  <behavior>
    - `SelectionMaidError("message here")` is constructable with positional message and optional cause kwarg
    - `ExtractionError.code == "EXT-001"` (class attribute, not instance attribute)
    - `UnsupportedFormatError(message="bad", format="xyz").format == "xyz"` (extra field accessible)
    - `ExtractionTimeoutError.code == "EXT-003"`
    - `FilterError.code == "FILT-001"`
    - `ChunkingError.code == "CHUNK-001"`
    - `EnrichmentError.code == "ENRICH-001"`
    - `isinstance(ExtractionError("x"), SelectionMaidError)` is True
    - `isinstance(UnsupportedFormatError("x", format="pdf"), ExtractionError)` is False — UnsupportedFormatError inherits from SelectionMaidError directly, not from ExtractionError
    - Raising `ExtractionError("msg", cause=original)` and catching it as SelectionMaidError works
    - No HTTP status attributes on any class (grep for "http_status", "status_code" returns empty)
  </behavior>
  <action>
    Create `src/selection_maid/errors.py` with `from __future__ import annotations` at the top.

    Imports: only `from __future__ import annotations`. No other imports needed (Exception is a builtin).

    Define `SelectionMaidError(Exception)` as the base. It must have:
    - `code: str` — define as a class attribute with an empty string default `code = ""` on the base class; subclasses override with their fixed string.
    - `__init__(self, message: str, cause: Exception | None = None) -> None` — stores `self.message = message`, `self.cause = cause`, and calls `super().__init__(message)`.

    Do NOT use `@dataclass` on error classes — they inherit from Exception and dataclass+Exception interactions can cause issues with mypy strict. Use a plain `__init__`.

    Define the six subclasses in this order:

    1. `ExtractionError(SelectionMaidError)` — `code = "EXT-001"`. No extra fields. No `__init__` override needed (inherits base).

    2. `UnsupportedFormatError(SelectionMaidError)` — `code = "EXT-002"`. Extra field `format: str`. Override `__init__(self, message: str, format: str, cause: Exception | None = None) -> None`. Store `self.format = format`, then call `super().__init__(message, cause)`.

    3. `ExtractionTimeoutError(SelectionMaidError)` — `code = "EXT-003"`. No extra fields. No `__init__` override.

    4. `FilterError(SelectionMaidError)` — `code = "FILT-001"`. No extra fields.

    5. `ChunkingError(SelectionMaidError)` — `code = "CHUNK-001"`. No extra fields.

    6. `EnrichmentError(SelectionMaidError)` — `code = "ENRICH-001"`. No extra fields.

    All six inherit from `SelectionMaidError` directly (NOT from each other), except UnsupportedFormatError and ExtractionTimeoutError which also directly extend SelectionMaidError (they are siblings of ExtractionError, not children).

    Do NOT add `http_status`, `status_code`, or any HTTP-related field to any class (per D-19).
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid && uv run python -c "from selection_maid.errors import SelectionMaidError, ExtractionError, UnsupportedFormatError, ExtractionTimeoutError, FilterError, ChunkingError, EnrichmentError; assert ExtractionError.code == 'EXT-001'; assert ExtractionTimeoutError.code == 'EXT-003'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from selection_maid.errors import SelectionMaidError, ExtractionError, UnsupportedFormatError, ExtractionTimeoutError, FilterError, ChunkingError, EnrichmentError; assert ExtractionError.code == 'EXT-001'; assert UnsupportedFormatError.code == 'EXT-002'; assert ExtractionTimeoutError.code == 'EXT-003'; assert FilterError.code == 'FILT-001'; assert ChunkingError.code == 'CHUNK-001'; assert EnrichmentError.code == 'ENRICH-001'; print('all codes OK')"` prints `all codes OK`
    - `uv run python -c "from selection_maid.errors import UnsupportedFormatError; e = UnsupportedFormatError('bad format', format='xyz'); assert e.format == 'xyz'; print('format field OK')"` prints `format field OK`
    - `uv run python -c "from selection_maid.errors import ExtractionError, SelectionMaidError; e = ExtractionError('x'); assert isinstance(e, SelectionMaidError); print('isinstance OK')"` prints `isinstance OK`
    - `grep "http_status\|status_code" src/selection_maid/errors.py` returns empty (no HTTP coupling)
    - `grep -c "class.*SelectionMaidError" src/selection_maid/errors.py` returns 6 (six subclasses each inheriting SelectionMaidError)
    - `uv run mypy src/selection_maid/errors.py --strict` exits 0
    - `grep -v "^#" src/selection_maid/errors.py | grep "^import\|^from" | grep -v "from __future__"` returns empty (no external imports)
  </acceptance_criteria>
  <done>All six error subclasses defined with fixed code class attributes; UnsupportedFormatError has format field; ExtractionTimeoutError defined for Phase 2; zero third-party imports; mypy strict passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter→service | Adapters raise any exception; service wraps in domain errors; only SelectionMaidError crosses to HTTP layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | Error message fields | mitigate | `cause` field stores original exception for debugging; Phase 6 HTTP adapter controls what portion of error details is exposed in API responses — domain errors carry no HTTP status, reducing accidental info leakage |
| T-01-04-02 | Spoofing | code class attribute | accept | Code strings are fixed class attributes; subclasses cannot accidentally inherit wrong codes because each subclass overrides `code` explicitly; risk is low (internal domain taxonomy) |
| T-01-04-SC | Tampering | No package installs | accept | errors.py uses only builtins and `__future__`; no external packages |
</threat_model>

<verification>
After task completes:

1. All six error classes importable from selection_maid.errors
2. `ExtractionError.code == "EXT-001"` through `EnrichmentError.code == "ENRICH-001"` verified via assert
3. `UnsupportedFormatError.format` field accessible on instances
4. No HTTP status attributes on any class
5. `uv run mypy src/selection_maid/errors.py --strict` exits 0
</verification>

<success_criteria>
- errors.py defines SelectionMaidError base with message, cause, code fields
- Six subclasses: ExtractionError (EXT-001), UnsupportedFormatError (EXT-002, +format), ExtractionTimeoutError (EXT-003), FilterError (FILT-001), ChunkingError (CHUNK-001), EnrichmentError (ENRICH-001)
- No HTTP status fields on any class (per D-19)
- Zero third-party imports; mypy strict passes
- Supports ARCH-06 (service.py imports these error classes without external dependencies)
</success_criteria>

<output>
Create `.planning/phases/01-domain-foundation/01-04-SUMMARY.md` when done
</output>
