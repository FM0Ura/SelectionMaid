---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/selection_maid/adapters/extractor/__init__.py
  - src/selection_maid/adapters/extractor/docling.py
  - tests/adapters/extractor/conftest.py
  - tests/adapters/extractor/test_docling_adapter.py
  - tests/fixtures/.gitkeep
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03

must_haves:
  truths:
    - "uv add docling>=2.95.0 installs without error using pytorch-cpu index"
    - "D-21: DoclingAdapter class exists in src/selection_maid/adapters/extractor/docling.py with constructor accepting converter and timeout_seconds"
    - "D-23: build_docling_adapter factory function exists and returns DoclingAdapter"
    - "Importing `selection_maid.adapters.extractor` in a fresh Python process completes without triggering torch model loading (no docling runtime in `sys.modules` after bare import)"
    - "D-22: SUPPORTED_MIME_TYPES frozenset with three MIME types exists as module-level constant"
    - "Test scaffold exists with empty unit and integration test classes"
    - "D-26: DocumentConverter is constructed exactly once per pytest session — the `real_converter` fixture `scope='session'` parameter ensures this"
    - "D-27: Session-scoped download fixtures for real_pdf_path, real_docx_path, real_html_path with skip-on-failure behavior"
    - "D-30: extract() raises UnsupportedFormatError at method entry when document.mime_type is not in SUPPORTED_MIME_TYPES, before any Docling call is made"
  artifacts:
    - path: "src/selection_maid/adapters/extractor/docling.py"
      provides: "DoclingAdapter skeleton + SUPPORTED_MIME_TYPES + build_docling_adapter"
      exports:
        - "DoclingAdapter"
        - "build_docling_adapter"
        - "SUPPORTED_MIME_TYPES"
    - path: "src/selection_maid/adapters/extractor/__init__.py"
      provides: "Public re-exports of DoclingAdapter and build_docling_adapter"
    - path: "tests/adapters/extractor/conftest.py"
      provides: "real_converter, real_pdf_path, real_docx_path, real_html_path session fixtures"
    - path: "tests/adapters/extractor/test_docling_adapter.py"
      provides: "Test class scaffolds (TestDoclingAdapterUnit, TestDoclingAdapterIntegration)"
    - path: "pyproject.toml"
      provides: "docling>=2.95.0 dependency + pytorch-cpu uv sources + uv index"
  key_links:
    - from: "src/selection_maid/adapters/extractor/__init__.py"
      to: "src/selection_maid/adapters/extractor/docling.py"
      via: "from selection_maid.adapters.extractor.docling import DoclingAdapter, build_docling_adapter"
      pattern: "from selection_maid.adapters.extractor.docling import"
    - from: "tests/adapters/extractor/test_docling_adapter.py"
      to: "src/selection_maid/adapters/extractor/docling.py"
      via: "from selection_maid.adapters.extractor import DoclingAdapter"
      pattern: "from selection_maid.adapters.extractor import DoclingAdapter"
---

<objective>
Install Docling using CPU-only PyTorch and create the DoclingAdapter skeleton: module-level constants, class constructor, factory function, and TYPE_CHECKING guard. Also create the test infrastructure: conftest fixtures (download-on-demand, session-scoped real converter) and an empty test scaffold that subsequent plans populate.

Purpose: Wave 1 foundation — all downstream plans depend on the adapter skeleton being importable and mypy-clean, and on the test fixtures existing before integration tests are written.

Output: pyproject.toml with docling dependency, DoclingAdapter skeleton (extract() stub only), build_docling_adapter factory, conftest.py with four session fixtures, empty test class scaffold.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/PROJECT.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-CONTEXT.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-RESEARCH.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-PATTERNS.md

<interfaces>
<!-- Domain contracts DoclingAdapter must satisfy. Read before implementing. -->

From src/selection_maid/domain/models.py:
  RawInput(path: Path, filename: str, mime_type: str)  -- frozen dataclass
  RawDocument(content: str, filename: str, page_count: int, format: str)  -- frozen dataclass

From src/selection_maid/domain/ports.py:
  class ExtractorPort(Protocol):
      def extract(self, document: RawInput) -> RawDocument: ...

From src/selection_maid/errors.py:
  class ExtractionError(SelectionMaidError): code = "EXT-001"
  class UnsupportedFormatError(SelectionMaidError): code = "EXT-002"
      def __init__(self, message: str, format: str, cause: Exception | None = None) -> None
  class ExtractionTimeoutError(SelectionMaidError): code = "EXT-003"
  class SelectionMaidError(Exception):
      def __init__(self, message: str, cause: Exception | None = None) -> None

From src/selection_maid/adapters/extractor/__init__.py (current):
  """Extractor adapter subpackage — ExtractorPort implementations."""
  (single docstring, no exports yet)

From pyproject.toml (current):
  dependencies = []
  [dependency-groups] dev = [anyio, httpx, mypy, pytest, pytest-asyncio, ruff]
  requires-python = ">=3.13"
  [tool.mypy] strict = true, python_version = "3.13", exclude = ["tests/"]

From tests/domain/test_service.py (session fixture pattern to replicate):
  @pytest.fixture(scope="session")
  def stub_service() -> ExtractionService: ...
  @pytest.fixture(scope="session")
  def raw_input() -> RawInput:
      return RawInput(path=Path("/tmp/test.pdf"), filename="test.pdf", mime_type="application/pdf")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docling dependency and pytorch-cpu index to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current state: dependencies=[], no [tool.uv.sources], no [[tool.uv.index]])
    - .planning/phases/02-docling-extraction-adapter/02-RESEARCH.md (Standard Stack section: exact toml stanzas for tool.uv.sources and tool.uv.index)
    - .planning/phases/02-docling-extraction-adapter/02-PATTERNS.md (pyproject.toml section: exact diff to apply)
  </read_first>
  <action>
    Edit pyproject.toml to make three changes:

    1. Replace `dependencies = []` with `dependencies = ["docling>=2.95.0"]` in [project].

    2. Append a new [tool.uv.sources] section after the [dependency-groups] block:
       torch sources pointing to the "pytorch-cpu" index, torchvision sources pointing to the same index.

    3. Append a new [[tool.uv.index]] entry with name="pytorch-cpu", url="https://download.pytorch.org/whl/cpu", explicit=true.

    After editing, run `uv sync` to install docling and its CPU-only torch dependencies. This may take several minutes on first run (downloading torch CPU wheels). Do not add optional extras beyond the base docling package.
  </action>
  <verify>
    <automated>uv run python -c "import docling; print('docling imported OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml line 7 reads: `dependencies = ["docling>=2.95.0"]`
    - pyproject.toml contains a `[tool.uv.sources]` section with torch and torchvision entries pointing to "pytorch-cpu"
    - pyproject.toml contains a `[[tool.uv.index]]` section with name="pytorch-cpu" and url="https://download.pytorch.org/whl/cpu" and explicit=true
    - `uv run python -c "import docling"` exits 0 without error
    - `uv run python -c "from docling.document_converter import DocumentConverter"` exits 0
  </acceptance_criteria>
  <done>docling is installed and importable; pyproject.toml has the CPU-only torch configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Create DoclingAdapter skeleton with TYPE_CHECKING guard and test infrastructure</name>
  <files>
    src/selection_maid/adapters/extractor/docling.py
    src/selection_maid/adapters/extractor/__init__.py
    tests/adapters/extractor/conftest.py
    tests/adapters/extractor/test_docling_adapter.py
    tests/fixtures/.gitkeep
    .gitignore
  </files>
  <read_first>
    - src/selection_maid/adapters/extractor/__init__.py (current: single docstring, expand to add exports)
    - src/selection_maid/service.py (import header convention, constructor injection pattern D-16 wrapping pattern)
    - src/selection_maid/errors.py (error class signatures: UnsupportedFormatError extra format kwarg)
    - src/selection_maid/domain/models.py (RawInput, RawDocument field names)
    - tests/stubs/adapters.py (StubExtractor.extract signature — the protocol to satisfy)
    - tests/domain/test_service.py (session-fixture pattern, test-class grouping pattern)
    - .planning/phases/02-docling-extraction-adapter/02-RESEARCH.md (Pattern 4: DoclingAdapter skeleton; Pattern 5: conftest download fixtures; anti-patterns to avoid)
    - .planning/phases/02-docling-extraction-adapter/02-PATTERNS.md (all sections — exact code excerpts)
  </read_first>
  <action>
    Create five files as described below. Do NOT include fenced code blocks in this action — use the read_first sources for exact implementations.

    FILE 1 — src/selection_maid/adapters/extractor/docling.py:
    Start with `from __future__ import annotations`. Import `concurrent.futures`, `Path` from pathlib, `TYPE_CHECKING` from typing. Use `if TYPE_CHECKING:` guard to import `DocumentConverter` from `docling.document_converter` — this import MUST NOT be at module top level. Import domain types (RawDocument, RawInput) and errors (ExtractionError, ExtractionTimeoutError, SelectionMaidError, UnsupportedFormatError) from their existing module paths.

    Define module-level constants:
    - `SUPPORTED_MIME_TYPES: frozenset[str]` containing exactly three strings: "application/pdf", "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "text/html" (per D-22)
    - `_MIME_TO_FORMAT: dict[str, str]` mapping those three MIME types to "pdf", "docx", "html" respectively

    Define class `DoclingAdapter` with:
    - `__init__(self, converter: "DocumentConverter", timeout_seconds: int = 120) -> None` storing `self._converter = converter` and `self._timeout_seconds = timeout_seconds` (per D-21)
    - `extract(self, document: RawInput) -> RawDocument` method that raises `UnsupportedFormatError(message=..., format=document.mime_type)` if `document.mime_type not in SUPPORTED_MIME_TYPES`, then raises `NotImplementedError("extract() not yet implemented")` for the rest — this stub will be replaced in Plan 02-02

    Define factory function `build_docling_adapter(converter: "DocumentConverter", timeout_seconds: int = 120) -> DoclingAdapter` that returns `DoclingAdapter(converter=converter, timeout_seconds=timeout_seconds)` (per D-23).

    FILE 2 — src/selection_maid/adapters/extractor/__init__.py:
    Keep the existing docstring. Add import of `DoclingAdapter` and `build_docling_adapter` from `selection_maid.adapters.extractor.docling`. Add `__all__ = ["DoclingAdapter", "build_docling_adapter"]`. Do NOT import anything from docling.* here.

    FILE 3 — tests/adapters/extractor/conftest.py:
    Start with `from __future__ import annotations`. Import `urllib.request`, `Path` from pathlib, `pytest`. Define `FIXTURES_DIR = Path(__file__).parent.parent.parent / "fixtures"`. Define `FIXTURE_URLS` dict mapping "sample.pdf", "sample.docx", "sample.html" to their public download URLs (from RESEARCH.md Pattern 5).

    Define a session-scoped `real_converter` fixture that imports `DocumentConverter`, `InputFormat`, `PdfPipelineOptions`, `PdfFormatOption`, `WordFormatOption`, `SimplePipeline`, `StandardPdfPipeline` inside the fixture body (NOT at module level — per anti-patterns). Construct `DocumentConverter` per RESEARCH.md Pattern 1 with `allowed_formats=[InputFormat.PDF, InputFormat.DOCX, InputFormat.HTML]`, `format_options` with `InputFormat.PDF: PdfFormatOption(pipeline_cls=StandardPdfPipeline, pipeline_options=PdfPipelineOptions(do_ocr=False, do_table_structure=True))` and `InputFormat.DOCX: WordFormatOption(pipeline_cls=SimplePipeline)`. HTML needs no format_options entry.

    Define three `scope="session"` fixtures: `real_pdf_path`, `real_docx_path`, `real_html_path`. Each calls a private `_ensure_fixture(filename)` helper that: creates FIXTURES_DIR if not exists, returns the cached Path if the file exists, otherwise calls `urllib.request.urlretrieve(url, target)` in a try/except that returns `None` on any exception (per D-27).

    FILE 4 — tests/adapters/extractor/test_docling_adapter.py:
    Start with `from __future__ import annotations`. Import `Path` from pathlib, `pytest`, `Mock` from `unittest.mock`. Import `RawInput`, `RawDocument` from `selection_maid.domain.models`. Import `ExtractionError`, `ExtractionTimeoutError`, `UnsupportedFormatError` from `selection_maid.errors`. Import `DoclingAdapter`, `build_docling_adapter` from `selection_maid.adapters.extractor`.

    Define class `TestDoclingAdapterUnit` with a single placeholder: `def test_placeholder(self) -> None: pass`. This class will be populated in Plans 02-02 and 02-04.

    Define class `TestDoclingAdapterIntegration` with a single placeholder: `def test_placeholder(self) -> None: pass`. This class will be populated in Plans 02-02, 02-03, and 02-05.

    FILE 5 — tests/fixtures/.gitkeep:
    Empty file. Ensures the `tests/fixtures/` directory exists in the repo for the download-on-demand fixtures.

    After creating all files, verify mypy passes on the src/ directory with strict mode.

    FILE 6 — .gitignore (append-only):
    Append the following lines to .gitignore (create the file if it does not exist). Per D-26, the fixtures directory must be gitignored:
      tests/fixtures/
      tests/fixtures/*.pdf
      tests/fixtures/*.docx
      tests/fixtures/*.html
    Do NOT overwrite any existing .gitignore content — append only.
  </action>
  <verify>
    <automated>uv run ruff check src/ tests/adapters/extractor/ && uv run mypy src/ --strict && uv run pytest tests/adapters/extractor/test_docling_adapter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/selection_maid/adapters/extractor/docling.py` contains class `DoclingAdapter`
    - `src/selection_maid/adapters/extractor/docling.py` contains `SUPPORTED_MIME_TYPES: frozenset[str]` with exactly the three PDF/DOCX/HTML MIME strings
    - `src/selection_maid/adapters/extractor/docling.py` does NOT have `from docling` at module top level (only inside `if TYPE_CHECKING:`)
    - `src/selection_maid/adapters/extractor/__init__.py` contains `__all__ = ["DoclingAdapter", "build_docling_adapter"]`
    - `tests/adapters/extractor/conftest.py` contains four session fixtures: `real_converter`, `real_pdf_path`, `real_docx_path`, `real_html_path`
    - `tests/adapters/extractor/conftest.py` does NOT have `from docling` at module top level (imports inside `real_converter` fixture body only)
    - `tests/adapters/extractor/test_docling_adapter.py` contains classes `TestDoclingAdapterUnit` and `TestDoclingAdapterIntegration`
    - `tests/fixtures/.gitkeep` exists
    - `.gitignore` contains `tests/fixtures/` (per D-26)
    - `uv run mypy src/ --strict` exits 0
    - `uv run pytest tests/adapters/extractor/test_docling_adapter.py -x -q` exits 0 (placeholders pass)
    - `uv run python -c "from selection_maid.adapters.extractor import DoclingAdapter, build_docling_adapter"` exits 0 without triggering Docling model loading (import is fast)
  </acceptance_criteria>
  <done>DoclingAdapter skeleton is importable and mypy-clean; test infrastructure files exist with session fixtures and empty test scaffolds; tests/fixtures/ directory exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependency install → project venv | uv pulls docling and torch from PyPI and pytorch-cpu index; packages enter the project trust boundary here |
| Test fixture download → tests/fixtures/ | urllib.request downloads from public URLs; files enter test fixture directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | pyproject.toml pytorch-cpu index | mitigate | Use explicit=true on [[tool.uv.index]] so torch is only fetched from pytorch-cpu, not from PyPI fallback; prevents a malicious torch on PyPI shadowing the CPU wheel |
| T-02-02 | Information Disclosure | TYPE_CHECKING guard in docling.py | mitigate | Docling imports inside `if TYPE_CHECKING:` prevent Docling runtime loading in non-extractor modules; import graph boundary enforced |
| T-02-03 | DoS | test fixture download | accept | Downloads are from known stable URLs (w3.org, calibre-ebook.com); any failure skips tests gracefully per D-27; no CI block |
| T-02-SC | Tampering | uv add docling (supply chain) | mitigate | Package legitimacy audit completed in RESEARCH.md: docling [OK] — IBM/LF AI provenance, 7.1M/month downloads, 36M total, first published 2024-07-15 |
</threat_model>

<verification>
After completing both tasks:
- `uv run python -c "import docling"` exits 0
- `uv run python -c "from selection_maid.adapters.extractor import DoclingAdapter"` exits 0 without loading Docling models (fast import)
- `uv run mypy src/ --strict` exits 0
- `uv run pytest tests/adapters/extractor/ -x -q` exits 0 (two placeholder tests pass)
- `tests/fixtures/.gitkeep` exists at that path
</verification>

<success_criteria>
- docling>=2.95.0 installed via uv with CPU-only torch (no CUDA wheels)
- DoclingAdapter class in `src/selection_maid/adapters/extractor/docling.py` with SUPPORTED_MIME_TYPES constant, TYPE_CHECKING guard, constructor injection, and extract() stub that raises UnsupportedFormatError for unknown MIME types
- build_docling_adapter factory function exported from `adapters/extractor/__init__.py`
- conftest.py with four session fixtures that load Docling only when fixtures are invoked
- test_docling_adapter.py with empty test classes that pass pytest
- mypy strict passes on src/
</success_criteria>

<output>
Create `.planning/phases/02-docling-extraction-adapter/02-01-SUMMARY.md` when done
</output>
