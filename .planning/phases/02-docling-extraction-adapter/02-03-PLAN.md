---
phase: "02"
plan: "03"
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-04"
files_modified:
  - tests/adapters/extractor/test_docling_adapter.py
autonomous: true
requirements:
  - EXT-04
  - EXT-05
  - EXT-06
  - EXT-07

must_haves:
  truths:
    - "A real PDF with text content returns Markdown containing at least one heading marker (# )"
    - "A real DOCX with tables returns Markdown containing GFM table syntax (| and ---)"
    - "A real HTML document returns Markdown containing list markers (- or 1. )"
    - "Known H2 heading-flattening limitation is documented in the heading test with a comment referencing Docling issue #1023"
    - "Code block test documents the EXT-07 behavior: either asserts backtick presence or documents known limitation for DOCX code detection"
    - "All four Markdown structure tests skip gracefully when fixtures are unavailable"
  artifacts:
    - path: "tests/adapters/extractor/test_docling_adapter.py"
      provides: "Markdown structure tests: test_headings_in_pdf, test_tables_in_docx, test_lists_in_html, test_code_blocks"
  key_links:
    - from: "test_headings_in_pdf"
      to: "RawDocument.content"
      via: "assert '# ' in result.content"
      pattern: "# "
    - from: "test_tables_in_docx"
      to: "RawDocument.content"
      via: "assert '|' in result.content and '---' in result.content"
      pattern: "\\|.*---"
---

<objective>
Add Markdown structure validation tests for EXT-04 (headings), EXT-05 (GFM tables), EXT-06 (lists), and EXT-07 (code blocks). These tests use the real DocumentConverter and downloaded fixtures to verify that Docling's export_to_markdown() produces the expected structural markers in its output. Known limitations (H2 flattening, DOCX code detection) are documented with comments rather than causing test failures.

Purpose: Validates that the extraction adapter preserves document structure — the most observable quality property from a RAG consumer's perspective. Failures here indicate regression in Docling output mapping.

Output: Four new integration tests in TestDoclingAdapterIntegration added to test_docling_adapter.py.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-CONTEXT.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-RESEARCH.md

<interfaces>
<!-- State of test file after Plan 02-02 -->
From tests/adapters/extractor/test_docling_adapter.py (after Plan 02-02):
  class TestDoclingAdapterUnit:
      test_unsupported_format_raises
      test_extraction_error_wraps_converter_exception
      test_raw_document_filename_matches_input

  class TestDoclingAdapterIntegration:
      test_pdf_extraction(real_converter, real_pdf_path)
      test_docx_extraction(real_converter, real_docx_path)
      test_html_extraction(real_converter, real_html_path)

  -- New methods to ADD to TestDoclingAdapterIntegration:
      test_headings_in_pdf(real_converter, real_pdf_path)
      test_tables_in_docx(real_converter, real_docx_path)
      test_lists_in_html(real_converter, real_html_path)
      test_code_blocks(real_converter, real_pdf_path)

From tests/adapters/extractor/conftest.py:
  real_converter: scope="session" -> DocumentConverter
  real_pdf_path: scope="session" -> Path | None
  real_docx_path: scope="session" -> Path | None
  real_html_path: scope="session" -> Path | None

Known limitations (from RESEARCH.md):
  Pitfall 2: H2 heading flattening (issue #1023) — all headings exported as ## regardless of original level
  Pitfall: DOCX code detection uncertainty — "code-formatted" paragraphs may not be recognized as CodeItem
  EXT-07 assertion strategy: check for triple backtick in content if available; document limitation if absent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Markdown structure tests for EXT-04, EXT-05, EXT-06, EXT-07</name>
  <files>tests/adapters/extractor/test_docling_adapter.py</files>
  <read_first>
    - tests/adapters/extractor/test_docling_adapter.py (current state after Plan 02-02 — read to find exact insertion point in TestDoclingAdapterIntegration)
    - tests/adapters/extractor/conftest.py (fixture signatures to use)
    - .planning/phases/02-docling-extraction-adapter/02-RESEARCH.md (Pitfall 2: H2 flattening; Open Question 3: DOCX code blocks; EXT-07 code block serialization behavior)
    - .planning/phases/02-docling-extraction-adapter/02-CONTEXT.md (D-28: heading test strategy — check headings exist, not exact H levels)
  </read_first>
  <behavior>
    - test_headings_in_pdf: extract from real PDF → assert at least one heading marker "# " present in content; MUST include a comment: "# Known limitation: Docling issue #1023 — all headings exported as H2; checking heading presence only, not level"
    - test_tables_in_docx: extract from real DOCX → assert "|" in result.content and "---" in result.content (GFM table markers); skips if real_docx_path is None
    - test_lists_in_html: extract from real HTML → assert "- " in result.content or "* " in result.content or "1." in result.content (at least one list marker present); skips if real_html_path is None
    - test_code_blocks: extract from real PDF → assert "```" in result.content OR (if backticks not found) assert True with a comment documenting the limitation; skips if real_pdf_path is None; MUST include comment referencing EXT-07 and Docling CodeItem behavior
  </behavior>
  <action>
    Add four test methods to the TestDoclingAdapterIntegration class in test_docling_adapter.py. Do NOT modify TestDoclingAdapterUnit or any existing methods.

    METHOD 1 — test_headings_in_pdf(self, real_converter, real_pdf_path):
    Skip guard first: `if real_pdf_path is None: pytest.skip("Integration fixtures unavailable — skipping")`.
    Build DoclingAdapter(converter=real_converter). Extract with RawInput(path=real_pdf_path, filename="sample.pdf", mime_type="application/pdf").
    Assert `"# " in result.content` with the known-limitation comment about Docling issue #1023 above the assertion line.
    The assertion checks for "# " (hash space) which matches any heading level (## , ### also contain "# ").

    METHOD 2 — test_tables_in_docx(self, real_converter, real_docx_path):
    Skip guard: `if real_docx_path is None: pytest.skip(...)`.
    Extract with DOCX MIME type. Assert `"|" in result.content` (GFM table columns use pipe separator). Assert `"---" in result.content` (GFM table separator row). If DOCX fixture does not have tables, this test will fail — document in a comment that the sample.docx is expected to contain at least one table.

    METHOD 3 — test_lists_in_html(self, real_converter, real_html_path):
    Skip guard: `if real_html_path is None: pytest.skip(...)`.
    Extract with HTML MIME type. Assert that at least one list marker is present: use `any(marker in result.content for marker in ["- ", "* ", "1. "])`.

    METHOD 4 — test_code_blocks(self, real_converter, real_pdf_path):
    Skip guard: `if real_pdf_path is None: pytest.skip(...)`.
    Extract with PDF MIME type. The w3.org sample PDF may or may not contain code blocks. Use a conditional assertion with comment:
    If "```" in result.content: assert True (code blocks found and formatted correctly per EXT-07).
    Else: add a pytest.skip or just pass with a comment: "# EXT-07: No code blocks detected in sample.pdf; Docling CodeItem serializes code as triple backticks when present; limitation documented — test passes vacuously for this fixture"
    This approach avoids a false test failure when the fixture simply has no code content, while still verifying the behavior when code IS present.
  </action>
  <verify>
    <automated>uv run pytest tests/adapters/extractor/test_docling_adapter.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/adapters/extractor/test_docling_adapter.py` contains `test_headings_in_pdf`, `test_tables_in_docx`, `test_lists_in_html`, `test_code_blocks` inside `TestDoclingAdapterIntegration`
    - `test_headings_in_pdf` contains comment referencing "issue #1023" or "H2" heading limitation
    - `test_code_blocks` contains comment referencing "EXT-07" or "CodeItem" or "triple backtick"
    - Every new test method begins with `if real_*_path is None: pytest.skip(...)`
    - `uv run pytest tests/adapters/extractor/test_docling_adapter.py -x -q --tb=short` exits 0 (all tests pass or skip, never error)
    - `uv run pytest tests/ -x -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>EXT-04, EXT-05, EXT-06, EXT-07 have integration test coverage; known limitations are documented in test comments; all tests pass or skip cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Downloaded fixture content → test assertions | Fixture files from external URLs are used as test inputs; content is from trusted public sources (w3.org, calibre-ebook.com) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | Fixture files from external URLs | accept | Fixtures used only in tests; any malicious content would affect only the test process, not production; download failures skip tests gracefully |
| T-02-SC | Tampering | No new package installs | accept | This plan adds no new dependencies; only test logic added |
</threat_model>

<verification>
After completing the task:
- `uv run pytest tests/adapters/extractor/test_docling_adapter.py -x -q --tb=short` exits 0
- Four new test methods present in TestDoclingAdapterIntegration (grep: test_headings_in_pdf, test_tables_in_docx, test_lists_in_html, test_code_blocks)
- `uv run pytest tests/ -x -q` exits 0
</verification>

<success_criteria>
- Markdown structure tests for all four EXT-04..EXT-07 requirements added to TestDoclingAdapterIntegration
- Known limitations (H2 flattening, code block detection uncertainty) documented in test comments, not masked as passing assertions
- All tests pass or skip — no test errors
</success_criteria>

<output>
Create `.planning/phases/02-docling-extraction-adapter/02-03-SUMMARY.md` when done
</output>
