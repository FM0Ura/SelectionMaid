---
phase: "02"
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - tests/adapters/extractor/test_docling_adapter.py
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03

must_haves:
  truths:
    - "DoclingAdapter.extract() raises ExtractionTimeoutError when conversion exceeds timeout_seconds"
    - "No type from the docling.* namespace appears in any import outside src/selection_maid/adapters/extractor/"
    - "uv run mypy src/ --strict exits 0 with zero errors"
    - "Unit tests for timeout and boundary violations exist and always pass without internet"
  artifacts:
    - path: "tests/adapters/extractor/test_docling_adapter.py"
      provides: "Unit tests: test_timeout_raises_extraction_timeout_error, test_domain_error_propagates_unchanged; mypy boundary verification"
  key_links:
    - from: "test_timeout_raises_extraction_timeout_error"
      to: "DoclingAdapter.extract()"
      via: "mock concurrent.futures.TimeoutError → ExtractionTimeoutError"
      pattern: "ExtractionTimeoutError"
    - from: "uv run mypy src/ --strict"
      to: "src/selection_maid/adapters/extractor/docling.py"
      via: "TYPE_CHECKING guard prevents docling.* types from propagating"
      pattern: "TYPE_CHECKING"
---

<objective>
Add unit tests that verify the timeout translation (concurrent.futures.TimeoutError → ExtractionTimeoutError) and domain error pass-through, then run the mypy boundary check that confirms no docling.* type crosses out of the adapters/extractor/ module. This plan closes the error-handling and boundary-enforcement requirements.

Purpose: The timeout mechanism was implemented in Plan 02-02's extract() body. This plan proves the mechanism works correctly with targeted unit tests and confirms the architectural boundary (ARCH-01 / EXT decision from CONTEXT.md) is enforced by the type checker.

Output: Two new unit tests in TestDoclingAdapterUnit; mypy strict verification; ARCH-01 boundary confirmed.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-CONTEXT.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-RESEARCH.md

<interfaces>
<!-- State of files after Plan 02-02 -->

From src/selection_maid/adapters/extractor/docling.py (after Plan 02-02):
  - extract() uses ThreadPoolExecutor with future.result(timeout=self._timeout_seconds)
  - Catches concurrent.futures.TimeoutError → raises ExtractionTimeoutError
  - Catches SelectionMaidError → re-raises unchanged
  - Catches Exception → raises ExtractionError
  - TYPE_CHECKING guard: "from docling.document_converter import DocumentConverter" only under TYPE_CHECKING

From tests/adapters/extractor/test_docling_adapter.py (after Plan 02-02):
  class TestDoclingAdapterUnit:
      test_unsupported_format_raises
      test_extraction_error_wraps_converter_exception
      test_raw_document_filename_matches_input
  -- New unit tests to ADD:
      test_timeout_raises_extraction_timeout_error
      test_domain_error_propagates_unchanged

From src/selection_maid/errors.py:
  ExtractionTimeoutError(message, cause=None) -- code "EXT-003"
  SelectionMaidError(message, cause=None) -- base; must propagate unchanged

From pyproject.toml:
  [tool.mypy] strict = true, exclude = ["tests/"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add timeout and domain-error-propagation unit tests; verify mypy boundary</name>
  <files>tests/adapters/extractor/test_docling_adapter.py</files>
  <read_first>
    - tests/adapters/extractor/test_docling_adapter.py (current state after Plan 02-02 — read to find exact insertion point in TestDoclingAdapterUnit)
    - src/selection_maid/adapters/extractor/docling.py (confirm concurrent.futures.TimeoutError catch location before writing mock)
    - tests/domain/test_service.py (test_domain_error_propagates_unchanged pattern: inline class that raises domain error, assert it propagates)
    - src/selection_maid/errors.py (ExtractionTimeoutError, SelectionMaidError signatures)
    - .planning/phases/02-docling-extraction-adapter/02-RESEARCH.md (Pattern 2: timeout mechanism; Pitfall 5: per-call executor; D-24/D-25 decision)
    - .planning/phases/02-docling-extraction-adapter/02-CONTEXT.md (D-24: lingering thread accepted; D-25: test with smaller timeout)
  </read_first>
  <behavior>
    - test_timeout_raises_extraction_timeout_error: DoclingAdapter with timeout_seconds=1 (D-25 strategy) where converter.convert() blocks for longer than 1 second → raises ExtractionTimeoutError (not TimeoutError or concurrent.futures.TimeoutError)
    - test_domain_error_propagates_unchanged: DoclingAdapter where converter.convert() raises ExtractionError("domain error") → extract() re-raises ExtractionError unchanged (not wrapped in another ExtractionError)
  </behavior>
  <action>
    Add two test methods to TestDoclingAdapterUnit. Do NOT modify TestDoclingAdapterIntegration or any existing methods.

    METHOD 1 — test_timeout_raises_extraction_timeout_error(self):
    Use `unittest.mock.Mock()` for converter. Set `converter.convert.side_effect` to a function that sleeps and never returns, OR use a simpler approach: patch `concurrent.futures.Future.result` to raise `concurrent.futures.TimeoutError`. The simplest correct approach:
    Import `concurrent.futures` in the test module. Set `converter.convert.side_effect = lambda path: __import__('time').sleep(10)` to make it block. Construct `DoclingAdapter(converter=converter, timeout_seconds=1)`. Call `adapter.extract(RawInput(path=Path("/tmp/test.pdf"), filename="test.pdf", mime_type="application/pdf"))`. Assert `pytest.raises(ExtractionTimeoutError)`. Note: this test will take ~1 second to run (the timeout fires). That is acceptable per D-25.

    METHOD 2 — test_domain_error_propagates_unchanged(self):
    Copy the pattern from `tests/domain/test_service.py::TestExceptionWrapping::test_domain_error_propagates_unchanged`. Use Mock() converter where `converter.convert.side_effect = ExtractionError("already a domain error")`. Construct DoclingAdapter(converter=mock, timeout_seconds=5). Assert `pytest.raises(ExtractionError)` — and importantly, assert the error message is still "already a domain error" (not wrapped in a new ExtractionError message). Use `with pytest.raises(ExtractionError) as exc_info:` and assert `exc_info.value.message == "already a domain error"`.

    After adding tests, run the mypy boundary check to confirm no docling type leaks outside adapters/extractor/:
    `uv run mypy src/ --strict`
    If mypy passes, the boundary is enforced. Document the result in the task acceptance.
  </action>
  <verify>
    <automated>uv run pytest tests/adapters/extractor/test_docling_adapter.py::TestDoclingAdapterUnit -x -q --tb=short && uv run mypy src/ --strict</automated>
  </verify>
  <acceptance_criteria>
    - `tests/adapters/extractor/test_docling_adapter.py` contains `test_timeout_raises_extraction_timeout_error` in TestDoclingAdapterUnit
    - `tests/adapters/extractor/test_docling_adapter.py` contains `test_domain_error_propagates_unchanged` in TestDoclingAdapterUnit
    - `uv run pytest tests/adapters/extractor/test_docling_adapter.py::TestDoclingAdapterUnit::test_timeout_raises_extraction_timeout_error -x` exits 0
    - `uv run pytest tests/adapters/extractor/test_docling_adapter.py::TestDoclingAdapterUnit::test_domain_error_propagates_unchanged -x` exits 0
    - `uv run mypy src/ --strict` exits 0 with no errors mentioning "docling" outside of "adapters/extractor"
    - `uv run pytest tests/ -x -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Timeout mechanism verified by unit test; domain error pass-through verified; mypy strict confirms no Docling type leaks outside adapters/extractor/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapters/extractor/ → domain layer | The type boundary enforced by TYPE_CHECKING guard; mypy strict verifies this boundary |
| concurrent.futures timeout → caller thread | TimeoutError must be caught and translated before it reaches the domain layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | Docling internal types leaking to domain | mitigate | TYPE_CHECKING guard in docling.py + mypy --strict verification in this plan's acceptance criteria; any leak detected as mypy error |
| T-02-10 | DoS | Lingering thread after timeout | accept | Accepted per D-24 for v1 low-traffic model; thread cannot access shared state after extract() returns; no persistent side effect except temporary CPU usage |
| T-02-SC | Tampering | No new package installs | accept | This plan adds no new dependencies |
</threat_model>

<verification>
After completing the task:
- `uv run pytest tests/adapters/extractor/test_docling_adapter.py::TestDoclingAdapterUnit -x -q` exits 0 (5 unit tests total)
- `uv run mypy src/ --strict` exits 0
- `uv run pytest tests/ -x -q` exits 0
</verification>

<success_criteria>
- Timeout unit test proves ExtractionTimeoutError is raised when conversion exceeds timeout_seconds
- Domain error propagation test proves SelectionMaidError subclasses pass through unwrapped
- mypy strict confirms ARCH-01 boundary: no docling.* types outside adapters/extractor/
</success_criteria>

<output>
Create `.planning/phases/02-docling-extraction-adapter/02-04-SUMMARY.md` when done
</output>
