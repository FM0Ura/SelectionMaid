---
phase: "02"
plan: "05"
type: execute
wave: 5
depends_on:
  - "02-03"
  - "02-04"
files_modified:
  - tests/adapters/extractor/test_docling_adapter.py
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
  - EXT-05
  - EXT-06
  - EXT-07

must_haves:
  truths:
    - "Full adapter test suite runs and passes: unit tests always, integration tests when fixtures are available"
    - "ExtractionService.process() works end-to-end with DoclingAdapter as the real extractor (other ports stubbed)"
    - "DocumentConverter is used exactly once per test session via the real_converter fixture (singleton behavior)"
    - "Phase gate passes: uv run pytest tests/ -q && uv run mypy src/ --strict both exit 0"
  artifacts:
    - path: "tests/adapters/extractor/test_docling_adapter.py"
      provides: "End-to-end service integration test: test_service_with_docling_adapter; singleton test: test_converter_singleton_behavior"
  key_links:
    - from: "test_service_with_docling_adapter"
      to: "ExtractionService.process()"
      via: "DoclingAdapter as real extractor, stub filter/chunker/enricher"
      pattern: "ExtractionService.*DoclingAdapter"
    - from: "real_converter fixture"
      to: "DocumentConverter"
      via: "scope='session' — single instance per test session"
      pattern: "scope.*session"
---

<objective>
Run the full adapter test suite and add two final integration tests: (1) an end-to-end ExtractionService.process() call with DoclingAdapter as the real extractor and stub adapters for the other three ports, and (2) a singleton behavior verification confirming the real_converter fixture produces a single DocumentConverter instance across all tests. This is the phase gate plan.

Purpose: Validates that DoclingAdapter plugs correctly into the full hexagonal pipeline — the final integration check before Phase 2 is marked complete.

Output: Two new tests in TestDoclingAdapterIntegration; phase gate green (all tests pass, mypy strict passes).
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/ROADMAP.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-CONTEXT.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.planning/phases/02-docling-extraction-adapter/02-RESEARCH.md

<interfaces>
<!-- State of all files after Plans 02-01 through 02-04 -->

From src/selection_maid/service.py:
  class ExtractionService:
      def __init__(self, extractor: ExtractorPort, filter_: FilterPort, chunker: ChunkerPort, enricher: MetadataEnricherPort) -> None
      def process(self, input: RawInput) -> ExtractionResult

From tests/stubs/adapters.py:
  StubFilter() -- satisfies FilterPort.filter(RawDocument) -> RawDocument
  StubChunker() -- satisfies ChunkerPort.chunk(str) -> list[DocumentChunk]
  StubEnricher() -- satisfies MetadataEnricherPort.enrich(RawDocument, list[DocumentChunk]) -> DocumentMetadata

From tests/adapters/extractor/conftest.py:
  real_converter: scope="session" -- single DocumentConverter instance per test session
  real_pdf_path: scope="session" -> Path | None

From src/selection_maid/domain/models.py:
  ExtractionResult(metadata: DocumentMetadata, chunks: tuple[DocumentChunk, ...])
  DocumentMetadata(title, author, language, document_type, page_count, chunk_count, ingestion_date)

From tests/adapters/extractor/test_docling_adapter.py (after Plans 02-02, 02-03, 02-04):
  class TestDoclingAdapterIntegration with 7 existing methods
  -- New methods to ADD:
      test_service_with_docling_adapter(real_converter, real_pdf_path)
      test_converter_singleton_behavior(real_converter)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add end-to-end service integration test and singleton verification</name>
  <files>tests/adapters/extractor/test_docling_adapter.py</files>
  <read_first>
    - tests/adapters/extractor/test_docling_adapter.py (current state after Plans 02-02 through 02-04 — read all existing tests before adding)
    - tests/domain/test_service.py (TestPipeline: test_process_returns_extraction_result pattern to replicate with real extractor)
    - tests/stubs/adapters.py (StubFilter, StubChunker, StubEnricher class names and import path)
    - src/selection_maid/service.py (ExtractionService constructor signature)
    - src/selection_maid/domain/models.py (ExtractionResult field names)
    - tests/adapters/extractor/conftest.py (real_converter fixture: scope="session" confirms singleton pattern)
    - .planning/phases/02-docling-extraction-adapter/02-CONTEXT.md (D-21: adapter receives converter pre-built; integration test pattern)
    - .planning/phases/02-docling-extraction-adapter/02-VALIDATION.md (02-05-01 test spec: full adapter suite)
  </read_first>
  <behavior>
    - test_service_with_docling_adapter: ExtractionService(DoclingAdapter(real_converter), StubFilter(), StubChunker(), StubEnricher()).process(pdf_raw_input) returns ExtractionResult with non-None metadata and at least one chunk; skips if real_pdf_path is None
    - test_converter_singleton_behavior: calling real_converter fixture twice in the same session returns the same object (id(real_converter_1) == id(real_converter_2)); this verifies the scope="session" singleton contract
  </behavior>
  <action>
    Add two test methods to TestDoclingAdapterIntegration. Do NOT modify TestDoclingAdapterUnit or any existing methods.

    METHOD 1 — test_service_with_docling_adapter(self, real_converter, real_pdf_path):
    Skip guard: `if real_pdf_path is None: pytest.skip("Integration fixtures unavailable — skipping")`.
    Import StubFilter, StubChunker, StubEnricher from tests.stubs.adapters inside the test or at the module top (follow existing import convention in the file). Import ExtractionService from selection_maid.service. Import ExtractionResult from selection_maid.domain.models.
    Construct `DoclingAdapter(converter=real_converter)`. Construct `ExtractionService(adapter, StubFilter(), StubChunker(), StubEnricher())`. Call `result = service.process(RawInput(path=real_pdf_path, filename="sample.pdf", mime_type="application/pdf"))`. Assert `isinstance(result, ExtractionResult)`. Assert `result.metadata is not None`. Assert `len(result.chunks) >= 1`.

    METHOD 2 — test_converter_singleton_behavior(self, real_converter):
    This test verifies the singleton contract without needing fixture files. Two references to the real_converter fixture in the same session must be the same object. In pytest, session-scoped fixtures are cached — any test that requests `real_converter` gets the same instance. Write: `adapter1 = DoclingAdapter(converter=real_converter)`. `adapter2 = DoclingAdapter(converter=real_converter)`. Assert `adapter1._converter is adapter2._converter`. This confirms both adapters hold the same DocumentConverter instance.

    After adding tests, run the full phase gate check (both commands must exit 0).
  </action>
  <verify>
    <automated>uv run pytest tests/ -q && uv run mypy src/ --strict</automated>
  </verify>
  <acceptance_criteria>
    - `tests/adapters/extractor/test_docling_adapter.py` contains `test_service_with_docling_adapter` in TestDoclingAdapterIntegration
    - `tests/adapters/extractor/test_docling_adapter.py` contains `test_converter_singleton_behavior` in TestDoclingAdapterIntegration
    - `uv run pytest tests/ -q` exits 0 (all tests pass or skip — no failures, no errors)
    - `uv run mypy src/ --strict` exits 0 with 0 errors
    - Unit tests (TestDoclingAdapterUnit) pass without internet: `uv run pytest tests/adapters/extractor/test_docling_adapter.py::TestDoclingAdapterUnit -q` exits 0 with 5 passing tests
    - At least one integration test passes when fixtures are available (not all skip) — if ALL integration tests skip, document why in the summary
  </acceptance_criteria>
  <done>Phase gate green: full test suite passes (unit) or skips gracefully (integration without internet); mypy strict clean; ExtractionService + DoclingAdapter pipeline verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExtractionService → DoclingAdapter → Docling | The full hexagonal pipeline; domain service delegates to the real adapter in integration test |
| test process → production src/ | Integration test exercises real production code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | DoS | ExtractionService.process() with real Docling in test | accept | Test uses scope="session" converter (one load per session); test converts only one small PDF; bounded resource usage |
| T-02-12 | Information Disclosure | RawDocument.content from downloaded fixture in test output | accept | test output is local only; no PII in public fixture files (w3.org WAI PDF); not a production concern |
| T-02-SC | Tampering | No new package installs | accept | This plan adds no new dependencies |
</threat_model>

<verification>
Phase gate after Plan 02-05 completes:

1. Full suite: `uv run pytest tests/ -q` exits 0
2. Type safety: `uv run mypy src/ --strict` exits 0
3. Unit isolation: `uv run pytest tests/adapters/extractor/test_docling_adapter.py::TestDoclingAdapterUnit -q` shows 5 passing tests
4. Module boundary: `uv run python -c "from selection_maid.adapters.extractor import DoclingAdapter"` completes in under 2 seconds (no Docling model loading on import)
5. Phase 2 success criteria check:
   - EXT-01: PDF extraction test passes (or skips with fixtures available)
   - EXT-02: DOCX extraction test passes (or skips)
   - EXT-03: HTML extraction test passes (or skips)
   - EXT-04: Heading presence verified in test_headings_in_pdf
   - EXT-05: GFM table markers verified in test_tables_in_docx
   - EXT-06: List markers verified in test_lists_in_html
   - EXT-07: Code block behavior documented in test_code_blocks
   - Singleton: test_converter_singleton_behavior passes
   - Boundary: mypy strict exits 0 (no Docling type leaks)
</verification>

<success_criteria>
- All 5 unit tests always pass (no internet required)
- Integration tests pass when fixtures available; skip gracefully when not
- ExtractionService + DoclingAdapter + StubFilter + StubChunker + StubEnricher pipeline produces ExtractionResult
- mypy strict exits 0 — ARCH-01 Docling boundary enforced
- Phase 2 COMPLETE: all 7 requirements (EXT-01..EXT-07) have test coverage
</success_criteria>

<output>
Create `.planning/phases/02-docling-extraction-adapter/02-05-SUMMARY.md` when done
</output>
