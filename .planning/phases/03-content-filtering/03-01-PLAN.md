---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/selection_maid/config.py
  - config.toml
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03]
must_haves:
  truths:
    - "get_config() returns valid GlobalConfig even if config.toml is missing (per D-38)"
    - "GlobalConfig holds default values (min_repeat=3, max_line_len=80) by default (per D-38)"
    - "get_config() successfully overrides defaults when config.toml exists (per D-40)"
  artifacts:
    - path: "src/selection_maid/config.py"
      provides: "Global configuration management (D-38)"
    - path: "config.toml"
      provides: "Environment-specific settings (D-40)"
---

<objective>
Implement central configuration management in selection_maid.config reading from config.toml at the project root.

Purpose: Provide a unified way to configure adapter thresholds (like min_repeat for filters) with hardcoded defaults.
Output: selection_maid/config.py and config.toml.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-content-filtering/03-CONTEXT.md
@.planning/phases/03-content-filtering/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configuration module</name>
  <files>src/selection_maid/config.py</files>
  <read_first>
    - .planning/phases/03-content-filtering/03-CONTEXT.md (D-38, D-39, D-40)
    - .planning/phases/03-content-filtering/03-RESEARCH.md (Standard Stack: tomllib)
  </read_first>
  <action>
    Create src/selection_maid/config.py. Per D-38 and D-39, it must:
    - Use tomllib (stdlib) to read config.toml from the project root (opened in binary mode 'rb' per Pitfall 1).
    - Provide a FilterConfig dataclass with fields: min_repeat (int, default 3) and max_line_len (int, default 80).
    - Provide a GlobalConfig dataclass holding a filter field of type FilterConfig.
    - Export a get_config() function that attempts to read the TOML file and returns GlobalConfig, falling back to defaults if the file or specific keys are missing.
    - Avoid raising exceptions if config.toml is missing — use defaults (D-38).
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.config import get_config; print(get_config().filter.min_repeat)"</automated>
  </verify>
  <acceptance_criteria>
    - src/selection_maid/config.py contains import tomllib
    - src/selection_maid/config.py contains class FilterConfig
    - src/selection_maid/config.py contains def get_config()
    - Running the verify command returns "3" (default value)
  </acceptance_criteria>
  <done>Configuration module exists and handles missing config.toml gracefully (D-38).</done>
</task>

<task type="auto">
  <name>Task 2: Create default config.toml</name>
  <files>config.toml</files>
  <read_first>
    - .planning/phases/03-content-filtering/03-CONTEXT.md (D-40)
  </read_first>
  <action>
    Create config.toml at the project root with the [filter] section containing min_repeat = 3 and max_line_len = 80 (per D-40).
  </action>
  <verify>
    <automated>grep -A 2 "\[filter\]" config.toml</automated>
  </verify>
  <acceptance_criteria>
    - config.toml exists in root
    - config.toml contains [filter] section
    - config.toml contains min_repeat = 3
  </acceptance_criteria>
  <done>config.toml exists with initial filter settings (D-40).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→config | config.toml is an untrusted local file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | config.toml | accept | File is local; assuming environment integrity. Default values provide fallback. |
</threat_model>

<verification>
Verify config module reads from file:
1. Modify config.toml min_repeat to 5.
2. Run `python3 -c "from selection_maid.config import get_config; print(get_config().filter.min_repeat)"`.
3. Confirm output is "5".
4. Revert to 3.
</verification>

<success_criteria>
- selection_maid/config.py implemented with tomllib (D-38).
- config.toml created with filter settings (D-40).
- get_config() returns expected values (from file or defaults).
</success_criteria>

<output>
Create .planning/phases/03-content-filtering/03-01-SUMMARY.md when done
</output>
