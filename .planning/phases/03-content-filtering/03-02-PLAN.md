---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/selection_maid/adapters/filter/heuristic.py
  - tests/adapters/filter/test_heuristic_filter.py
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03]
must_haves:
  truths:
    - "Repeated headers/footers (3+ times) are removed"
    - "Isolated page numbers (Arabic, Roman, hyphenated) are removed"
    - "Excessive newlines (3+) are compressed to single blank line"
    - "Headings and tables are preserved"
  artifacts:
    - path: "src/selection_maid/adapters/filter/heuristic.py"
      provides: "HeuristicFilter implementation"
    - path: "tests/adapters/filter/test_heuristic_filter.py"
      provides: "Unit tests for filtering rules"
---

<objective>
Implement HeuristicFilter in adapters/filter/heuristic.py and its unit tests.

Purpose: Remove structural noise from Markdown content before chunking.
Output: HeuristicFilter class and passing unit tests.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-content-filtering/03-CONTEXT.md
@.planning/phases/03-content-filtering/03-RESEARCH.md
@.planning/phases/03-content-filtering/03-PATTERNS.md
@src/selection_maid/domain/ports.py
@src/selection_maid/domain/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold HeuristicFilter and Test Suite</name>
  <files>src/selection_maid/adapters/filter/heuristic.py, tests/adapters/filter/test_heuristic_filter.py</files>
  <read_first>
    - .planning/phases/03-content-filtering/03-CONTEXT.md (D-41, D-42, D-43)
    - .planning/phases/03-content-filtering/03-PATTERNS.md (HeuristicFilter structure, Test classes)
    - src/selection_maid/domain/ports.py (FilterPort)
  </read_first>
  <behavior>
    - TestFILT01Headers: Should remove lines <= 80 chars that repeat 3+ times, but NOT headings (#) or table lines (|).
    - TestFILT02PageNumbers: Should remove "1", "iv", "- 10 -", but NOT numbers within sentences.
    - TestFILT03Whitespace: Should turn "\n\n\n" into "\n\n".
    - TestContentPreservation: Should ensure H1, H2, and table rows are kept.
  </behavior>
  <action>
    - Create src/selection_maid/adapters/filter/heuristic.py with HeuristicFilter class skeleton implementing FilterPort.
    - Implement __init__(self, min_repeat=3, max_line_len=80).
    - Implement filter(self, document: RawDocument) -> RawDocument with D-16 exception wrapping and D-06 replace(document, content=...).
    - Create tests/adapters/filter/test_heuristic_filter.py with rule-based test classes (D-43) and inline fixtures (D-42).
    - Verify tests fail (RED).
  </action>
  <verify>
    <automated>uv run pytest tests/adapters/filter/test_heuristic_filter.py</automated>
  </verify>
  <acceptance_criteria>
    - HeuristicFilter exists in src/selection_maid/adapters/filter/heuristic.py
    - filter() method signature matches FilterPort
    - tests/adapters/filter/test_heuristic_filter.py exists with at least 4 test classes
    - pytest command runs and shows failures
  </acceptance_criteria>
  <done>Scaffold complete and test suite defined (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Filtering Rules</name>
  <files>src/selection_maid/adapters/filter/heuristic.py</files>
  <read_first>
    - .planning/phases/03-content-filtering/03-CONTEXT.md (D-31 to D-37)
    - .planning/phases/03-content-filtering/03-RESEARCH.md (Code Examples: Regex, Whitespace)
    - .planning/phases/03-content-filtering/03-PATTERNS.md (Frequency Analysis Pattern)
  </read_first>
  <behavior>
    - All tests from Task 1 pass (GREEN).
  </behavior>
  <action>
    Implement private methods in HeuristicFilter to apply rules in order (D-32 to D-37):
    1. Frequency analysis for headers/footers (D-32, D-33). Use collections.Counter.
    2. Isolated page number removal using Regex (D-35, D-36).
    3. Whitespace compression (D-37) using re.sub(r"\n{3,}", "\n\n").
    Ensure all logic is wrapped in a try/except block that relanses unexpected errors as FilterError (D-16).
  </action>
  <verify>
    <automated>uv run pytest tests/adapters/filter/test_heuristic_filter.py</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/adapters/filter/test_heuristic_filter.py passes (100%)
    - HeuristicFilter.filter() uses dataclasses.replace to return the document
    - Implementation uses standard library (re, collections.Counter) only
  </acceptance_criteria>
  <done>Filtering logic implemented and verified (GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document→filter | RawDocument.content contains arbitrary Markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of Service | Regex matching | mitigate | Use non-backtracking regex for page numbers; apply whitespace compression last. |
</threat_model>

<verification>
Full unit test suite for HeuristicFilter:
`uv run pytest tests/adapters/filter/test_heuristic_filter.py`
</verification>

<success_criteria>
- HeuristicFilter correctly removes headers/footers, page numbers, and whitespace.
- Legitimate content (headings, tables) is preserved.
- Implementation matches FilterPort and handles errors via FilterError.
</success_criteria>

<output>
Create .planning/phases/03-content-filtering/03-02-SUMMARY.md when done
</output>
