---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/selection_maid/adapters/filter/heuristic.py
  - src/selection_maid/adapters/filter/__init__.py
  - tests/domain/test_service.py
autonomous: true
requirements: [ARCH-02, FILT-01, FILT-02, FILT-03]
must_haves:
  truths:
    - "HeuristicFilter is exported via selection_maid.adapters.filter (D-41)"
    - "build_heuristic_filter factory creates a functional instance using config (D-39)"
    - "ExtractionService.process() correctly invokes the filter to clean noise"
  artifacts:
    - path: "src/selection_maid/adapters/filter/__init__.py"
      provides: "Filter adapter subpackage exports (D-41)"
  key_links:
    - from: "src/selection_maid/service.py"
      to: "HeuristicFilter"
      via: "Dependency Injection"
---

<objective>
Provide factory function for HeuristicFilter and verify its integration within the ExtractionService pipeline.

Purpose: Finalize the adapter and ensure it is properly wired for use.
Output: build_heuristic_filter factory and integration verification.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-content-filtering/03-CONTEXT.md
@.planning/phases/03-content-filtering/03-PATTERNS.md
@src/selection_maid/service.py
@src/selection_maid/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Factory and Exports</name>
  <files>src/selection_maid/adapters/filter/heuristic.py, src/selection_maid/adapters/filter/__init__.py</files>
  <read_first>
    - .planning/phases/03-content-filtering/03-CONTEXT.md (D-39, D-41)
    - .planning/phases/03-content-filtering/03-PATTERNS.md (Factory function pattern, Export pattern)
    - src/selection_maid/config.py (get_config)
  </read_first>
  <action>
    - Add build_heuristic_filter(min_repeat: int = None, max_line_len: int = None) factory function to heuristic.py.
    - The factory function MUST use selection_maid.config.get_config() to retrieve default values if min_repeat or max_line_len are None (per D-39).
    - Update src/selection_maid/adapters/filter/__init__.py to export HeuristicFilter and build_heuristic_filter (D-41).
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.adapters.filter import build_heuristic_filter; print(build_heuristic_filter())"</automated>
  </verify>
  <acceptance_criteria>
    - heuristic.py contains build_heuristic_filter
    - build_heuristic_filter calls get_config() when params are missing (D-39)
    - adapters/filter/__init__.py exports the filter and factory (D-41)
    - verify command runs without ImportError
  </acceptance_criteria>
  <done>Adapter factory and exports complete using centralized config (D-39).</done>
</task>

<task type="auto">
  <name>Task 2: Verify Service Integration</name>
  <files>tests/domain/test_service.py</files>
  <read_first>
    - src/selection_maid/service.py (ExtractionService.process)
    - tests/domain/test_service.py (Existing service tests)
  </read_first>
  <action>
    Update tests/domain/test_service.py (or add a new test case) to verify that ExtractionService.process correctly invokes the filter.
    Note: ExtractionService already injects FilterPort. This task ensures that using HeuristicFilter in the pipeline works as expected.
    Scenario:
    - Create ExtractionService with a real HeuristicFilter and other ports as stubs.
    - Process a RawInput that results in a RawDocument with noise (e.g., repeated lines).
    - Assert the final ExtractionResult chunks or the content (if accessible) is clean.
  </action>
  <verify>
    <automated>uv run pytest tests/domain/test_service.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/domain/test_service.py contains a test case using HeuristicFilter
    - pytest tests/domain/test_service.py passes
  </acceptance_criteria>
  <done>Service integration verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

...

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Availability | Service integration | mitigate | Integration tests ensure the pipeline doesn't crash when filtering. |
</threat_model>

<verification>
Verify integration:
`uv run pytest tests/domain/test_service.py`
</verification>

<success_criteria>
- Factory function provided and exported.
- Factory function uses centralized config defaults per D-39.
- ExtractionService successfully uses HeuristicFilter to clean content during the pipeline.
</success_criteria>

<output>
Create .planning/phases/03-content-filtering/03-03-SUMMARY.md when done
</output>
