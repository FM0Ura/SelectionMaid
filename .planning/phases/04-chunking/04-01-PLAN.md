---
phase: 04-chunking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/selection_maid/config.py, src/selection_maid/adapters/chunker/markdown.py]
autonomous: true
requirements: [CHUNK-01, CHUNK-02]
user_setup: []

must_haves:
  truths:
    - "Tiktoken dependency is installed in the project"
    - "GlobalConfig includes a chunker section with max_tokens setting"
    - "MarkdownChunker class exists and follows the factory pattern build_markdown_chunker"
  artifacts:
    - path: "src/selection_maid/config.py"
      provides: "ChunkerConfig dataclass and GlobalConfig extension"
    - path: "src/selection_maid/adapters/chunker/markdown.py"
      provides: "MarkdownChunker skeleton and factory function"
  key_links:
    - from: "src/selection_maid/config.py"
      to: "config.toml"
      via: "tomllib parsing of [chunker] section"
---

<objective>
Configure the environment and create the architectural skeleton for the Chunking adapter.
This includes adding the tiktoken dependency, extending the centralized configuration,
and creating the file structure for the MarkdownChunker following established project patterns.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-chunking/04-CONTEXT.md
@src/selection_maid/config.py
@src/selection_maid/domain/ports.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tiktoken dependency</name>
  <files>pyproject.toml</files>
  <action>
    Add `tiktoken` to the project dependencies using `uv add tiktoken`.
    This library is required for the fixed-size chunking fallback (D-49, D-50).
  </action>
  <verify>
    <automated>grep "tiktoken" pyproject.toml</automated>
  </verify>
  <done>tiktoken added to pyproject.toml and installed in the virtual environment</done>
</task>

<task type="auto">
  <name>Task 2: Extend GlobalConfig with ChunkerConfig</name>
  <files>src/selection_maid/config.py</files>
  <action>
    1. Define `ChunkerConfig` dataclass with `max_tokens: int = 512` (D-51).
    2. Add `chunker: ChunkerConfig = field(default_factory=ChunkerConfig)` to `GlobalConfig`.
    3. Update `get_config()` to parse the `[chunker]` section from TOML if it exists.
    Follow the pattern used for `FilterConfig` in the same file.
  </action>
  <verify>
    <automated>pytest -c /dev/null -k "not test" --doctest-modules src/selection_maid/config.py || python3 -c "from selection_maid.config import get_config; cfg = get_config(); print(cfg.chunker.max_tokens)"</automated>
  </verify>
  <done>Configuration extended to support chunker settings</done>
</task>

<task type="auto">
  <name>Task 3: Create MarkdownChunker skeleton and factory</name>
  <files>src/selection_maid/adapters/chunker/markdown.py</files>
  <action>
    1. Create `MarkdownChunker` class implementing `ChunkerPort` (Protocol from domain).
    2. Constructor should accept `max_tokens: int` (D-57).
    3. Implement skeleton `chunk(self, content: str) -> list[DocumentChunk]` that currently raises `NotImplementedError` or returns an empty list.
    4. Apply exception wrapping: any internal error should be caught and raised as `ChunkingError` (D-16).
    5. Implement `build_markdown_chunker(config: ChunkerConfig) -> MarkdownChunker` factory (D-57).
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.adapters.chunker.markdown import MarkdownChunker, build_markdown_chunker; from selection_maid.config import ChunkerConfig; assert isinstance(build_markdown_chunker(ChunkerConfig()), MarkdownChunker)"</automated>
  </verify>
  <done>MarkdownChunker skeleton and factory created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → GlobalConfig | Untrusted local configuration file parsed into dataclasses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | config.toml | mitigate | Use safe `tomllib.load` and explicit type casting in `get_config()` |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for tiktoken |
</threat_model>

<verification>
Verify that the configuration can be read and the MarkdownChunker factory can be instantiated.
</verification>

<success_criteria>
1. `uv add tiktoken` succeeds.
2. `get_config().chunker.max_tokens` returns 512 by default.
3. `build_markdown_chunker` returns a valid `MarkdownChunker` instance.
</success_criteria>

<output>
Create `.planning/phases/04-chunking/04-01-SUMMARY.md` when done
</output>
