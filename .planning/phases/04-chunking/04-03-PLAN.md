---
phase: 04-chunking
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [src/selection_maid/adapters/chunker/markdown.py, tests/adapters/chunker/test_markdown_chunker.py, tests/domain/test_service.py]
autonomous: true
requirements: [CHUNK-02, CHUNK-03]
user_setup: []

must_haves:
  truths:
    - "Document without headings is split using fixed-size token budget (tiktoken)"
    - "Fallback strategy respects paragraph boundaries"
    - "chunk_index and total_chunks are consistent across all chunks"
    - "ExtractionService correctly integrates the new MarkdownChunker"
  artifacts:
    - path: "src/selection_maid/adapters/chunker/markdown.py"
      provides: "Fixed-size fallback logic with tiktoken"
    - path: "tests/domain/test_service.py"
      provides: "Integration test with ExtractionService"
  key_links:
    - from: "src/selection_maid/adapters/chunker/markdown.py"
      to: "tiktoken"
      via: "token count calculation"
---

<objective>
Implement the fallback strategy for documents without headings using tiktoken for
fixed-size chunking. Ensure consistency in chunk indexing and total counts, and
verify the end-to-end integration with the ExtractionService.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-chunking/04-CONTEXT.md
@src/selection_maid/adapters/chunker/markdown.py
@src/selection_maid/service.py
@tests/domain/test_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fixed-size fallback with tiktoken</name>
  <files>src/selection_maid/adapters/chunker/markdown.py, tests/adapters/chunker/test_markdown_chunker.py</files>
  <behavior>
    - Test 1: Fallback activates when no H1/H2 headings are present (D-49).
    - Test 2: Uses `tiktoken` with `cl100k_base` encoding (D-50).
    - Test 3: Respects `max_tokens` (default 512) (D-51).
    - Test 4: Respects paragraph boundaries (double newline) - accumulates paragraphs until `max_tokens` reached (D-52).
  </behavior>
  <action>
    1. Initialize tiktoken encoder in `__init__`.
    2. In `chunk()`, detect if no headings exist.
    3. If no headings, implement paragraph-aware token accumulation.
    4. Populate `chunk_index` and `total_chunks` consistently after all chunks are generated.
  </action>
  <verify>
    <automated>pytest tests/adapters/chunker/test_markdown_chunker.py</automated>
  </verify>
  <done>Fixed-size fallback implemented and tested</done>
</task>

<task type="auto">
  <name>Task 2: ExtractionService integration test</name>
  <files>tests/domain/test_service.py</files>
  <action>
    Add `TestMarkdownChunkerIntegration` to `tests/domain/test_service.py`.
    The test should:
    1. Create a `MarkdownChunker` using `build_markdown_chunker`.
    2. Wire it into `ExtractionService` along with real `HeuristicFilter` and stubs for extractor/enricher.
    3. Verify that a document with headings is correctly chunked by the service.
    4. Verify that metadata fields like `chunk_index` and `total_chunks` are present and consistent in the final `ExtractionResult`.
  </action>
  <verify>
    <automated>pytest tests/domain/test_service.py</automated>
  </verify>
  <done>ExtractionService integrated with MarkdownChunker and verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExtractionService → MarkdownChunker | Internal boundary between core service and adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | tiktoken | mitigate | Lock tiktoken version in uv.lock and use verified encoding name |
</threat_model>

<verification>
Run all tests including unit tests for the adapter and integration tests for the service.
</verification>

<success_criteria>
1. Documents without headings are correctly chunked by token budget.
2. `chunk_index` and `total_chunks` are accurate and consistent.
3. `ExtractionService` pipeline works end-to-end with the real chunker.
4. All success criteria for Phase 4 are met.
</success_criteria>

<output>
Create `.planning/phases/04-chunking/04-03-SUMMARY.md` when done
</output>
