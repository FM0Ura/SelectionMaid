---
phase: 05-metadata-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/selection_maid/domain/models.py, tests/stubs/adapters.py, tests/domain/test_models.py, tests/domain/test_service.py, pyproject.toml, src/selection_maid/config.py]
autonomous: true
requirements: [META-01, ARCH-01]

must_haves:
  truths:
    - "DocumentMetadata contains doc_id, source_filename, ingested_at, and doc_type"
    - "Existing tests pass with the new schema"
    - "GlobalConfig includes enricher section"
  artifacts:
    - path: "src/selection_maid/domain/models.py"
      provides: "Updated DocumentMetadata dataclass"
    - path: "src/selection_maid/config.py"
      provides: "EnricherConfig and updated get_config()"
---

<objective>
Update the domain model to include missing metadata fields (doc_id, source_filename, ingested_at, doc_type) and prepare the infrastructure for the enricher adapter. This handles the breaking changes in the domain schema early.

Purpose: Align domain models with ROADMAP requirements and ensure a clean foundation for implementation.
Output: Updated models, fixed tests, and environment ready for langdetect.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-metadata-enrichment/05-CONTEXT.md
@src/selection_maid/domain/models.py
@src/selection_maid/config.py
@tests/stubs/adapters.py
@tests/domain/test_models.py
@tests/domain/test_service.py

<interfaces>
From src/selection_maid/domain/models.py:
```python
@dataclass(frozen=True)
class DocumentMetadata:
    title: str
    author: str
    language: str
    document_type: str
    page_count: int
    chunk_count: int
    ingestion_date: datetime
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Domain Model and Stubs</name>
  <files>src/selection_maid/domain/models.py, tests/stubs/adapters.py</files>
  <action>
    Modify `DocumentMetadata` in `src/selection_maid/domain/models.py` per D-68:
    - Add `doc_id: str`
    - Add `source_filename: str`
    - Rename `ingestion_date` -> `ingested_at`
    - Rename `document_type` -> `doc_type`
    - Verify field count is exactly 9.

    Update `StubEnricher` in `tests/stubs/adapters.py` to match the new schema:
    - Pass dummy `doc_id` (e.g., "stub-id") and `source_filename`.
    - Use the new field names.
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.domain.models import DocumentMetadata; import dataclasses; assert len(dataclasses.fields(DocumentMetadata)) == 9"</automated>
  </verify>
  <done>DocumentMetadata schema updated and stubs aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Existing Tests for Schema Changes</name>
  <files>tests/domain/test_models.py, tests/domain/test_service.py</files>
  <action>
    Update tests to accommodate the `DocumentMetadata` renaming and new fields:
    - In `tests/domain/test_models.py`: Update `TestDocumentMetadata` to check for 9 fields and use the new names. Update `_make_metadata` in `TestExtractionResult`.
    - In `tests/domain/test_service.py`: Update `test_metadata_has_all_required_fields` to check for the new fields.
    Ensure all tests in these files pass.
  </action>
  <verify>
    <automated>pytest tests/domain/test_models.py tests/domain/test_service.py</automated>
  </verify>
  <done>Breaking changes resolved in the test suite.</done>
</task>

<task type="auto">
  <name>Task 3: Infrastructure and Config Setup</name>
  <files>pyproject.toml, src/selection_maid/config.py</files>
  <action>
    1. Install `langdetect`: Run `uv add langdetect`.
    2. Update `src/selection_maid/config.py` per D-72, D-73:
       - Add `EnricherConfig` dataclass (empty for now).
       - Add `enricher: EnricherConfig` to `GlobalConfig`.
       - Update `get_config()` to initialize `enricher`.
  </action>
  <verify>
    <automated>grep "langdetect" pyproject.toml && python3 -c "from selection_maid.config import get_config; assert hasattr(get_config(), 'enricher')"</automated>
  </verify>
  <done>Dependencies installed and config expanded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models.py | Internal domain boundary; schema changes affect all components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for langdetect |
</threat_model>

<verification>
Check that all domain tests pass and mypy strict is happy with the model changes.
</verification>

<success_criteria>
`pytest tests/domain/` passes and `langdetect` is present in `pyproject.toml`.
</success_criteria>

<output>
Create `.planning/phases/05-metadata-enrichment/05-01-SUMMARY.md` when done
</output>
