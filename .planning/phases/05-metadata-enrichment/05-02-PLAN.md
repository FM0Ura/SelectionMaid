---
phase: 05-metadata-enrichment
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/selection_maid/adapters/enricher/default.py]
autonomous: true
requirements: [META-01, META-02, META-03]

must_haves:
  truths:
    - "MetadataEnricher detects language (PT, EN, ES) with confidence threshold"
    - "MetadataEnricher infers doc_type from heading keywords"
    - "MetadataEnricher extracts title from first H1"
    - "MetadataEnricher generates unique doc_id and sets ingested_at"
  artifacts:
    - path: "src/selection_maid/adapters/enricher/default.py"
      provides: "MetadataEnricher adapter implementation"
---

<objective>
Implement the `MetadataEnricher` adapter. This adapter is responsible for detecting the document language, inferring the document type, and filling in all required metadata fields.

Purpose: Automate metadata extraction to enrich the final document output.
Output: Functional MetadataEnricher adapter and factory function.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-metadata-enrichment/05-CONTEXT.md
@src/selection_maid/domain/models.py
@src/selection_maid/domain/ports.py
@src/selection_maid/config.py

<interfaces>
From src/selection_maid/domain/ports.py:
```python
class MetadataEnricherPort(Protocol):
    def enrich(
        self, raw: RawDocument, chunks: list[DocumentChunk]
    ) -> DocumentMetadata: ...
```

Updated DocumentMetadata from Plan 01:
```python
@dataclass(frozen=True)
class DocumentMetadata:
    doc_id: str
    source_filename: str
    title: str
    author: str
    language: str
    doc_type: str
    page_count: int
    chunk_count: int
    ingested_at: datetime
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeleton and Language Detection</name>
  <files>src/selection_maid/adapters/enricher/default.py</files>
  <action>
    Create `src/selection_maid/adapters/enricher/default.py`:
    - Define `MetadataEnricher` class implementing `MetadataEnricherPort`.
    - Implement `_detect_language(content: str) -> str` using `langdetect.detect_langs`.
    - Apply threshold >= 0.8 per D-61; fallback to "und" on low confidence or error (D-62).
    - Cache langdetect initialization if necessary (D-60).
  </action>
  <verify>
    <automated>python3 -c "import langdetect; from selection_maid.adapters.enricher.default import MetadataEnricher; assert MetadataEnricher"</automated>
  </verify>
  <done>Enricher skeleton and language detection logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Doc Type Inference and Title Extraction</name>
  <files>src/selection_maid/adapters/enricher/default.py</files>
  <action>
    Implement heuristics in `MetadataEnricher`:
    - `_infer_doc_type(content: str) -> str`: Use keywords in headings (lines starting with `#`) for `legal`, `presentation`, `form`, `report` per D-64. Fallback to `other` (D-65).
    - `_extract_title(content: str) -> str`: Use regex `^# (.+)` on the first matching line (D-66).
    - Keywords for `doc_type` should include PT, EN, and ES variations as defined in D-64.
  </action>
  <verify>
    <automated>grep "_infer_doc_type" src/selection_maid/adapters/enricher/default.py && grep "_extract_title" src/selection_maid/adapters/enricher/default.py</automated>
  </verify>
  <done>Heuristic-based inference logic implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Complete Field Mapping and Factory</name>
  <files>src/selection_maid/adapters/enricher/default.py</files>
  <action>
    Complete the `enrich()` method:
    - `doc_id`: `uuid.uuid4()` (D-69).
    - `source_filename`: `raw.filename` (D-70).
    - `author`: `""` (D-67).
    - `ingested_at`: `datetime.now(timezone.utc)` (ROADMAP criterion 4).
    - `page_count`: `raw.page_count`.
    - `chunk_count`: `len(chunks)`.
    - Implement `build_metadata_enricher(config: EnricherConfig) -> MetadataEnricher` (D-72).
  </action>
  <verify>
    <automated>grep "build_metadata_enricher" src/selection_maid/adapters/enricher/default.py</automated>
  </verify>
  <done>Enricher implementation complete and factory exposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw.content | Untrusted Markdown input from previous stages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Metadata field values | accept | Metadata inferred from content already in memory; no external leak. |
| T-05-02-02 | Denial of Service | langdetect on huge docs | mitigate | langdetect is typically fast, but we should monitor for very large Markdown blobs in Phase 7. |
</threat_model>

<verification>
Check mypy strict on the new file.
</verification>

<success_criteria>
`MetadataEnricher` correctly implements `MetadataEnricherPort` and handles all fields.
</success_criteria>

<output>
Create `.planning/phases/05-metadata-enrichment/05-02-SUMMARY.md` when done
</output>
