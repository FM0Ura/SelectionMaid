---
phase: 05-metadata-enrichment
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [tests/adapters/enricher/test_metadata_enricher.py]
autonomous: true
requirements: [META-01, META-02, META-03]

must_haves:
  truths:
    - "MetadataEnricher passes unit tests with diverse inputs"
    - "Language detection works for Portuguese, English, and Spanish"
    - "MetadataEnricher correctly handles missing H1 and other edge cases"
  artifacts:
    - path: "tests/adapters/enricher/test_metadata_enricher.py"
      provides: "Comprehensive unit tests for the enricher"
---

<objective>
Verify the `MetadataEnricher` implementation with comprehensive unit tests and integration checks. Ensure the success criteria for Phase 5 are fully met, specifically language detection accuracy and metadata field completeness.

Purpose: Guarantee quality and correctness of document enrichment.
Output: Validated MetadataEnricher adapter ready for production use.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-metadata-enrichment/05-CONTEXT.md
@src/selection_maid/adapters/enricher/default.py
@src/selection_maid/domain/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit Tests for MetadataEnricher</name>
  <files>tests/adapters/enricher/test_metadata_enricher.py</files>
  <action>
    Create `tests/adapters/enricher/test_metadata_enricher.py`:
    - Test `_detect_language` with PT, EN, ES snippets.
    - Test `_detect_language` threshold behavior (mocking langdetect if needed).
    - Test `_infer_doc_type` with various heading keywords (legal, report, etc.).
    - Test `_extract_title` with and without H1.
    - Test `enrich()` returns a valid `DocumentMetadata` with all 9 fields.
  </action>
  <verify>
    <automated>pytest tests/adapters/enricher/test_metadata_enricher.py</automated>
  </verify>
  <done>Unit tests pass for all enricher features.</done>
</task>

<task type="auto">
  <name>Task 2: Multilingual and Edge Case Verification</name>
  <files>tests/adapters/enricher/test_metadata_enricher.py</files>
  <action>
    Add complex test cases to `tests/adapters/enricher/test_metadata_enricher.py`:
    - Very short text (should return "und").
    - Text with conflicting keywords for `doc_type` (verify priority/default).
    - Document with empty content.
    - Verify `ingested_at` is recent (within seconds of test execution).
    - Verify `doc_id` is a valid UUID v4.
  </action>
  <verify>
    <automated>pytest tests/adapters/enricher/test_metadata_enricher.py</automated>
  </verify>
  <done>Enricher is resilient to edge cases and multilingual documents.</done>
</task>

<task type="auto">
  <name>Task 3: Phase Gate and Integration Check</name>
  <files>tests/domain/test_service.py</files>
  <action>
    Run the full suite to ensure `ExtractionService` works correctly with the real enricher if integrated (or ensure `StubEnricher` still works for basic pipeline tests).
    Check success criteria:
    - `language` returns ISO 639-1.
    - `doc_type` from closed vocabulary.
    - All `DocumentMetadata` fields present.
  </action>
  <verify>
    <automated>pytest tests/ && mypy src/ selection_maid/</automated>
  </verify>
  <done>Phase 5 complete and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MetadataEnricher | Unit testing boundary; ensures logic correctness under controlled inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | Test data | accept | Local test data only. |
</threat_model>

<verification>
Ensure all 3 success criteria from ROADMAP for Phase 5 are met.
</verification>

<success_criteria>
`pytest tests/` passes. All metadata fields are correctly populated in tests.
</success_criteria>

<output>
Create `.planning/phases/05-metadata-enrichment/05-03-SUMMARY.md` when done
</output>
