---
phase: 06-http-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/selection_maid/adapters/http/schemas.py]
autonomous: true
requirements: [API-01, API-02]
must_haves:
  truths:
    - "Pydantic schemas match domain dataclasses exactly (D-75)"
    - "Metadata dates are serialized as ISO 8601 strings (D-76)"
    - "Schemas are decoupled from domain core (D-74)"
  artifacts:
    - path: "src/selection_maid/adapters/http/schemas.py"
      provides: "Request/Response validation and serialization"
---

<objective>
Define Pydantic v2 schemas for the HTTP API layer.

Purpose: Provide formal request/response contracts for FastAPI, ensuring automatic JSON serialization and validation while keeping the domain core isolated (no Pydantic in domain).
Output: `src/selection_maid/adapters/http/schemas.py` with `ExtractionResponse` and `HealthResponse`.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-http-api-layer/06-CONTEXT.md
@.planning/phases/06-http-api-layer/06-RESEARCH.md
@src/selection_maid/domain/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Chunk and Metadata schemas</name>
  <files>src/selection_maid/adapters/http/schemas.py</files>
  <behavior>
    - ChunkSchema must include all 8 fields from DocumentChunk (D-75).
    - MetadataSchema must include all 9 fields from DocumentMetadata (D-75).
    - Both must use ConfigDict(from_attributes=True) for model_validate support (D-77).
    - ingested_at must be datetime type (Pydantic handles ISO 8601 automatically).
  </behavior>
  <action>
    Create src/selection_maid/adapters/http/schemas.py.
    Implement ChunkSchema (BaseModel) and MetadataSchema (BaseModel).
    Ensure field names and types exactly match domain/models.py.
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.adapters.http.schemas import ChunkSchema, MetadataSchema; print('OK')"</automated>
  </verify>
  <done>Schemas defined with correct fields and Pydantic configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Define ExtractionResponse and HealthResponse</name>
  <files>src/selection_maid/adapters/http/schemas.py</files>
  <action>
    Define ExtractionResponse:
    - metadata: MetadataSchema
    - chunks: list[ChunkSchema] (per D-77)
    - model_config = ConfigDict(from_attributes=True)

    Define HealthResponse (per D-78):
    - status: str (default "ok")
    - rss_mb: float
    - uptime_seconds: float
    - version: str
  </action>
  <verify>
    <automated>python3 -c "from selection_maid.adapters.http.schemas import ExtractionResponse, HealthResponse; print('OK')"</automated>
  </verify>
  <done>Response schemas ready for router integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Domain → HTTP | Data conversion from internal dataclasses to external JSON schemas |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | schemas.py | mitigate | Strictly define schema fields to prevent accidental leakage of internal state (per D-75) |
</threat_model>

<verification>
Check if `ExtractionResponse.model_validate()` correctly handles a mock `ExtractionResult` dataclass.
</verification>

<success_criteria>
- [ ] `schemas.py` exists with all specified models.
- [ ] No Pydantic imports in `domain/` or `service.py`.
- [ ] `from_attributes=True` is set on all relevant schemas.
</success_criteria>

<output>
Create `.planning/phases/06-http-api-layer/06-01-SUMMARY.md` when done
</output>
