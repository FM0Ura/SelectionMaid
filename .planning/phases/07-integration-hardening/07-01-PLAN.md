---
phase: 07-integration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/selection_maid/adapters/extractor/docling.py",
  "src/selection_maid/adapters/http/router.py",
  "tests/fixtures/adversarial/corrupt.pdf",
  "tests/fixtures/adversarial/empty.pdf",
  "tests/fixtures/adversarial/protected.pdf",
  "tests/fixtures/adversarial/large_sample.pdf",
  "tests/fixtures/adversarial/spoofed.pdf",
  "pyproject.toml"
]
autonomous: true
requirements: ["HARD-01", "HARD-02", "HARD-03", "HARD-04"]

must_haves:
  truths:
    - "DoclingAdapter is thread-safe via internal Lock"
    - "DoclingAdapter proactively unloads backends and triggers GC"
    - "Adversarial fixtures exist in tests/fixtures/adversarial/"
    - "Tempfiles are created with a unique 'selectionmaid_' prefix"
  artifacts:
    - path: "src/selection_maid/adapters/extractor/docling.py"
      provides: "Serialized and memory-hardened extraction"
    - path: "tests/fixtures/adversarial/protected.pdf"
      provides: "Encrypted PDF for testing"
    - path: "tests/fixtures/adversarial/large_sample.pdf"
      provides: "40MB+ PDF for stress testing"
---

<objective>
Generate adversarial fixtures and harden the DoclingAdapter and Router for production-level reliability.

Purpose: 
1. Prevent native process crashes by serializing access to non-thread-safe Docling components.
2. Mitigate memory growth by proactively releasing resources and triggering garbage collection.
3. Provide a standard "dirty" fixture set for resilience testing.
4. Enable precise resource auditing by using a unique tempfile prefix.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integration-hardening/07-CONTEXT.md
@.planning/phases/07-integration-hardening/07-RESEARCH.md
@src/selection_maid/adapters/extractor/docling.py
@src/selection_maid/adapters/http/router.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pypdf and generate adversarial fixtures</name>
  <files>pyproject.toml, tests/fixtures/adversarial/*</files>
  <action>
    1. Add `pypdf` to development dependencies: `uv add --dev pypdf`.
    2. Create `tests/fixtures/adversarial/` directory.
    3. Generate the following fixtures (use a script or simple commands):
       - `corrupt.pdf`: 1KB of random bytes.
       - `empty.pdf`: 0 bytes.
       - `spoofed.pdf`: Plain text file "I am a text file" renamed to .pdf.
       - `protected.pdf`: Use `pypdf` to encrypt `tests/fixtures/sample.pdf` with a password.
       - `large_sample.pdf`: Concatenate `tests/fixtures/sample.pdf` content until file size > 40MB.
  </action>
  <verify>
    <automated>ls -lh tests/fixtures/adversarial/ && [ -s tests/fixtures/adversarial/large_sample.pdf ] && [ $(stat -c %s tests/fixtures/adversarial/large_sample.pdf) -gt 40000000 ]</automated>
  </verify>
  <done>All 5 adversarial fixtures exist and meet size/format requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Harden DoclingAdapter (Locking &amp; GC)</name>
  <files>src/selection_maid/adapters/extractor/docling.py</files>
  <action>
    1. Import `threading` and `gc`.
    2. In `DoclingAdapter.__init__`, initialize `self._lock = threading.Lock()`.
    3. In `DoclingAdapter.extract()`:
       - Wrap the `ThreadPoolExecutor` and `future.result()` calls in `with self._lock:`.
       - Wrap the entire extraction logic in a `try/finally` block.
       - In `finally`:
         - Check if `result` (the Docling conversion result) exists.
         - If it has `input._backend`, call `result.input._backend.unload()`.
         - Explicitly `del result`.
         - Call `gc.collect()`.
    4. Ensure `_build_raw_document` is called BEFORE the backend is unloaded.
  </action>
  <verify>
    <automated>pytest tests/adapters/extractor/test_docling_adapter.py</automated>
  </verify>
  <done>DoclingAdapter implements serialization and proactive memory management. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Harden Router tempfile prefix</name>
  <files>src/selection_maid/adapters/http/router.py</files>
  <action>
    Update `tempfile.NamedTemporaryFile` call in the `ingest` handler to include `prefix="selectionmaid_"`. 
    This allows the test suite to precisely audit for leaked files in `/tmp` without accidentally checking files from other processes.
  </action>
  <verify>
    <automated>grep 'prefix="selectionmaid_"' src/selection_maid/adapters/http/router.py</automated>
  </verify>
  <done>Tempfiles generated by the router are now uniquely identifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> API | Untrusted files (corrupt, bombs) enter here. |
| Adapter -> Docling | Thread-safety boundary; native crashes risk here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Denial of Service | DoclingAdapter | mitigate | Use threading.Lock to prevent concurrent access process crashes. |
| T-07-02 | Denial of Service | Extraction Pipeline | mitigate | Use gc.collect() and backend.unload() to prevent OOM. |
| T-07-03 | Tampering | Tempfile Audit | mitigate | Use unique 'selectionmaid_' prefix to ensure cleanup verification. |
</threat_model>

<verification>
1. Run `pytest tests/adapters/extractor/test_docling_adapter.py` to ensure core extraction still works.
2. Verify all files in `tests/fixtures/adversarial/` have correct sizes and properties.
</verification>

<success_criteria>
- DoclingAdapter is thread-safe and memory-hardened.
- All adversarial fixtures are generated.
- Router uses unique tempfile prefix.
</success_criteria>

<output>
Create `.planning/phases/07-integration-hardening/07-01-SUMMARY.md` when done
</output>
