---
phase: 07-integration-hardening
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: ["tests/adapters/http/test_integration.py"]
autonomous: true
requirements: ["HARD-01", "HARD-03", "HARD-04", "API-01", "API-03"]

must_haves:
  truths:
    - "Server handles adversarial fixtures with correct HTTP status codes"
    - "Server handles 5 simultaneous requests without failure"
    - "All tempfiles are strictly cleaned up after requests"
    - "GET /health remains responsive after extraction failures"
  artifacts:
    - path: "tests/adapters/http/test_integration.py"
      provides: "End-to-end integration and concurrency stress testing"
---

<objective>
Implement end-to-end integration tests that verify service reliability under adversarial input and concurrent load.

Purpose:
1. Ensure the API gracefully handles "dirty" inputs without crashing or leaking resources.
2. Verify thread-safety and concurrency limits of the extraction pipeline.
3. Audit tempfile cleanup to prevent disk exhaustion.
4. Confirm system liveness after catastrophic failures.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integration-hardening/07-CONTEXT.md
@.planning/phases/07-integration-hardening/07-RESEARCH.md
@tests/adapters/http/test_router.py
@src/selection_maid/adapters/http/router.py
@tests/fixtures/adversarial/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement E2E Integration and Liveness tests</name>
  <files>tests/adapters/http/test_integration.py</files>
  <action>
    Create `tests/adapters/http/test_integration.py` using `httpx.AsyncClient` and a real `app` (reusing the `real_converter` fixture from `tests/adapters/extractor/conftest.py`).
    1. Implement a test that iterates through all fixtures:
       - `tests/fixtures/sample.pdf` -> Expect HTTP 200.
       - `tests/fixtures/adversarial/corrupt.pdf` -> Expect HTTP 500 (EXT-001).
       - `tests/fixtures/adversarial/empty.pdf` -> Expect HTTP 422 (UPLOAD-002) - magic bytes fail on 0 bytes.
       - `tests/fixtures/adversarial/protected.pdf` -> Expect HTTP 500 (EXT-001 - Docling error).
       - `tests/fixtures/adversarial/spoofed.pdf` -> Expect HTTP 422 (UPLOAD-002).
       - `tests/fixtures/adversarial/large_sample.pdf` -> Expect HTTP 200 (unless it hits 50MB default limit, verify config).
    2. Implement "Liveness" check: After a failing request (e.g. `corrupt.pdf`), immediately call `GET /health` and verify it returns HTTP 200.
  </action>
  <verify>
    <automated>pytest tests/adapters/http/test_integration.py -k "integration or liveness"</automated>
  </verify>
  <done>E2E and Liveness tests pass with real Docling models.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Concurrency and Cleanup Audit tests</name>
  <files>tests/adapters/http/test_integration.py</files>
  <action>
    1. Implement `test_concurrency_stress`:
       - Use `asyncio.gather` to send 5 simultaneous `POST /ingest` requests for `sample.pdf`.
       - Verify all 5 return HTTP 200.
       - Verify no process crashes occur.
    2. Implement `test_tempfile_cleanup_audit`:
       - Record the count of files matching `/tmp/selectionmaid_*` before the request.
       - Perform a request (or a batch of requests).
       - Verify the count of files matching `/tmp/selectionmaid_*` returns to 0 (or the initial baseline) after requests finish.
  </action>
  <verify>
    <automated>pytest tests/adapters/http/test_integration.py -k "concurrency or cleanup"</automated>
  </verify>
  <done>Concurrency stress and tempfile audit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | End-to-end verification of the primary untrusted boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Denial of Service | API Layer | mitigate | Verify that concurrent load (5 reqs) doesn't cause deadlock or crash. |
| T-07-05 | Denial of Service | /tmp storage | mitigate | Verify zero leaked 'selectionmaid_*' tempfiles after processing. |
</threat_model>

<verification>
Run `pytest tests/adapters/http/test_integration.py` to execute the full suite.
</verification>

<success_criteria>
- Integration tests cover all adversarial cases.
- 5-request concurrency test passes.
- Tempfile audit confirms zero leaks.
</success_criteria>

<output>
Create `.planning/phases/07-integration-hardening/07-02-SUMMARY.md` when done
</output>
