---
phase: 07-integration-hardening
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: ["tests/test_memory_regression.py"]
autonomous: true
requirements: ["HARD-02", "API-02"]

must_haves:
  truths:
    - "RSS after 20 conversions is less than 2x the initial RSS"
    - "Memory release logic (GC/unload) is effective under repetition"
  artifacts:
    - path: "tests/test_memory_regression.py"
      provides: "Long-running memory leak audit"
---

<objective>
Audit the extraction pipeline for memory leaks over long-running sessions.

Purpose:
1. Verify that proactive memory management (gc.collect and backend unloading) prevents unbounded RAM growth.
2. Confirm that Docling instances can be reused for multiple conversions without significant degradation.
3. Establish a baseline for production memory stability.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-integration-hardening/07-CONTEXT.md
@.planning/phases/07-integration-hardening/07-RESEARCH.md
@src/selection_maid/adapters/extractor/docling.py
@src/selection_maid/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Memory Regression test</name>
  <files>tests/test_memory_regression.py</files>
  <action>
    Create `tests/test_memory_regression.py`.
    1. Implement `test_memory_leak_audit`:
       - Use the `real_converter` fixture.
       - Measure initial RSS using `psutil.Process().memory_info().rss`.
       - Perform 20 consecutive extractions of `tests/fixtures/sample.pdf` using `DoclingAdapter`.
       - Measure final RSS.
       - Assert `RSS_final <= 2 * RSS_initial` (per D-94).
    2. Log the RSS values (initial, peak, final) for visibility.
  </action>
  <verify>
    <automated>pytest tests/test_memory_regression.py</automated>
  </verify>
  <done>Memory regression test confirms stability over 20 conversions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process -> RAM | Internal resource consumption boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Denial of Service | Memory | mitigate | Regression test ensures RSS doesn't double over 20 calls (D-94). |
</threat_model>

<verification>
Run `pytest tests/test_memory_regression.py` and inspect the output RSS logs.
</verification>

<success_criteria>
- Memory regression test passes the 2x RSS rule.
- No unexpected memory spikes during the 20-conversion batch.
</success_criteria>

<output>
Create `.planning/phases/07-integration-hardening/07-03-SUMMARY.md` when done
</output>
