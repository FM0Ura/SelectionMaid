---
phase: 08-backend-cors-project-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/selection_maid/adapters/http/app.py, tests/adapters/http/test_cors.py]
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "OPTIONS preflight request from http://localhost:5173 returns 200 OK"
    - "CORS headers (Access-Control-Allow-Origin) match http://localhost:5173"
    - "Allowed methods include POST and OPTIONS"
  artifacts:
    - path: "tests/adapters/http/test_cors.py"
      provides: "Integration tests for CORS policy"
    - path: "src/selection_maid/adapters/http/app.py"
      provides: "FastAPI app with CORSMiddleware"
  key_links:
    - from: "src/selection_maid/adapters/http/app.py"
      to: "fastapi.middleware.cors.CORSMiddleware"
      via: "app.add_middleware"
---

<objective>
Configure Cross-Origin Resource Sharing (CORS) in the FastAPI backend to allow the Vue 3 SPA (running at http://localhost:5173) to communicate with the API.

Purpose: Enable local development of the frontend without browser-enforced security blocks.
Output: Backend with restricted CORS policy and verifying tests.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-backend-cors-project-scaffold/08-CONTEXT.md
@.planning/phases/08-backend-cors-project-scaffold/08-RESEARCH.md
@src/selection_maid/adapters/http/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CORS integration test</name>
  <files>tests/adapters/http/test_cors.py</files>
  <behavior>
    - Test 1: OPTIONS / with Origin: http://localhost:5173 returns 200 and Access-Control-Allow-Origin: http://localhost:5173 (fails initially).
    - Test 2: OPTIONS / with Origin: http://malicious.com returns 400 or no CORS headers (standard FastAPI behavior is to reject or not include headers).
    - Test 3: POST /ingest with Origin: http://localhost:5173 returns Access-Control-Allow-Origin header.
  </behavior>
  <action>Create an integration test using FastAPI TestClient to verify the CORS policy. Use 'selection_maid.adapters.http.app:app' as the app instance. Verify preflight OPTIONS and actual POST requests per D-01, D-02, and D-03.</action>
  <verify>
    <automated>pytest tests/adapters/http/test_cors.py</automated>
  </verify>
  <done>Failing test created and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CORSMiddleware in create_app</name>
  <files>src/selection_maid/adapters/http/app.py</files>
  <action>
    Import CORSMiddleware from fastapi.middleware.cors.
    In create_app(), add the middleware to the app instance using app.add_middleware().
    Apply the following configuration per D-01, D-02, and D-03:
    - allow_origins=["http://localhost:5173"]
    - allow_methods=["POST", "OPTIONS"]
    - allow_headers=["*"]
    - allow_credentials=True
  </action>
  <verify>
    <automated>pytest tests/adapters/http/test_cors.py</automated>
  </verify>
  <done>Middleware added and tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI | Cross-origin requests are governed by the CORS policy defined here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | CORSMiddleware | mitigate | Restrict allow_origins to specific local dev URL per D-01 (no wildcards). |
| T-08-02 | Security Bypass | CORSMiddleware | mitigate | Limit allow_methods to POST and OPTIONS only per D-02. |
</threat_model>

<verification>
Run `pytest tests/adapters/http/test_cors.py` to ensure the CORS policy is correctly enforced.
</verification>

<success_criteria>
Backend CORS integration test passes, confirming that only the expected local origin and methods are allowed.
</success_criteria>

<output>
Create `.planning/phases/08-backend-cors-project-scaffold/08-01-SUMMARY.md` when done
</output>
