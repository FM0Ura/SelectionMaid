---
phase: 08-backend-cors-project-scaffold
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/App.vue
  - frontend/index.html
  - frontend/src/assets/index.css
  - frontend/components.json
  - frontend/src/lib/utils.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03]

must_haves:
  truths:
    - "Vite dev server starts and serves Vue 3 app at http://localhost:5173"
    - "App.vue renders 'SelectionMaid is ready.' heading"
    - "App.vue renders a shadcn-vue Button with 'Upload Document' text"
    - "The <html> element in index.html has class='dark' for fixed dark mode"
    - "Vite proxy rewrites /api/ingest to http://localhost:8000/ingest"
  artifacts:
    - path: "frontend/package.json"
      provides: "Vue 3, Vite, Tailwind v4 dependencies"
    - path: "frontend/vite.config.ts"
      provides: "Vite configuration with Tailwind v4 and /api proxy"
    - path: "frontend/src/App.vue"
      provides: "Proof-of-life UI with shadcn-vue component"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8000"
      via: "server.proxy"
    - from: "frontend/src/assets/index.css"
      to: "tailwindcss"
      via: "@import"
---

<objective>
Scaffold the frontend project using Vue 3, Vite, TypeScript, Tailwind CSS v4, and shadcn-vue. Establish a proof-of-life page with a dark-mode button and configure a development proxy to the backend.

Purpose: Create the foundational frontend environment and toolchain for v2.0.
Output: Working SPA scaffold in 'frontend/' directory with verified dark-mode rendering.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-backend-cors-project-scaffold/08-CONTEXT.md
@.planning/phases/08-backend-cors-project-scaffold/08-RESEARCH.md
@.planning/phases/08-backend-cors-project-scaffold/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + Vue + Tailwind v4</name>
  <files>frontend/package.json, frontend/vite.config.ts</files>
  <action>
    1. Run 'npm create vite@latest frontend -- --template vue-ts' (per D-04, D-05).
    2. Enter 'frontend' directory and run 'npm install'.
    3. Install Tailwind CSS v4 and its Vite plugin: 'npm install tailwindcss @tailwindcss/vite'.
    4. Install Node types for path resolution: 'npm install -D @types/node'.
    5. Update 'frontend/src/assets/index.css' (or create it) with '@import "tailwindcss";'.
    6. Update 'frontend/vite.config.ts' to include the Tailwind plugin: 'import tailwindcss from "@tailwindcss/vite"'.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Vite project initialized with Tailwind v4.</done>
</task>

<task type="auto">
  <name>Task 2: Setup shadcn-vue and Proxy</name>
  <files>frontend/components.json, frontend/vite.config.ts, frontend/src/components/ui/button.vue</files>
  <action>
    1. Initialize shadcn-vue: 'npx shadcn-vue@latest init'. Follow defaults (D-09). Ensure '@/' alias is configured to './src'.
    2. Install the Button component: 'npx shadcn-vue@latest add button' (D-10).
    3. Configure 'frontend/vite.config.ts' with the following (D-06, D-07):
       - Alias: '@' to 'path.resolve(__dirname, "./src")'.
       - Server Proxy: '/api' -> 'http://localhost:8000', with 'rewrite: (path) => path.replace(/^\/api/, "")'.
  </action>
  <verify>
    <automated>test -f frontend/components.json && test -f frontend/src/components/ui/button/Button.vue</automated>
  </verify>
  <done>shadcn-vue initialized and Vite proxy configured.</done>
</task>

<task type="auto">
  <name>Task 3: Proof-of-Life and Dark Mode</name>
  <files>frontend/index.html, frontend/src/App.vue</files>
  <action>
    1. Update 'frontend/index.html': add 'class="dark"' to the 'html' tag (D-11).
    2. Update 'frontend/src/App.vue' per 08-UI-SPEC.md:
       - Clear boilerplate.
       - Render a centered layout (flex, min-h-screen, bg-background).
       - Add heading: "SelectionMaid is ready." (text-2xl font-semibold text-foreground).
       - Add shadcn-vue Button: "Upload Document" (default variant).
  </action>
  <verify>
    <automated>grep 'class="dark"' frontend/index.html && grep 'Upload Document' frontend/src/App.vue</automated>
  </verify>
  <done>Proof-of-life UI rendered with fixed dark mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite Proxy → Backend | Internal proxy handles request forwarding during development. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] (none found in research). |
| T-08-04 | Information Disclosure | Vite Proxy | mitigate | Hardcode backend target to localhost:8000; only forward /api path. |
</threat_model>

<verification>
Start backend: `uvicorn selection_maid.adapters.http.app:app --port 8000`
Start frontend: `cd frontend && npm run dev`
Visit `http://localhost:5173` and verify "SelectionMaid is ready." and the button are visible in dark mode.
Check console for any proxy or CORS errors.
</verification>

<success_criteria>
A Vue 3 project exists in 'frontend/' with Tailwind v4 and shadcn-vue, showing a dark-mode button and configured to proxy /api to the backend.
</success_criteria>

<output>
Create `.planning/phases/08-backend-cors-project-scaffold/08-02-SUMMARY.md` when done
</output>
