---
phase: 09-typescript-types-api-layer-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/vitest.config.ts, frontend/src/types/api.ts]
autonomous: true
requirements: [PROC-01]

must_haves:
  truths:
    - "Vitest is configured and running in the frontend directory"
    - "TypeScript interfaces match backend ExtractionResponse, MetadataSchema, and ChunkSchema"
    - "UploadState discriminated union is defined covering all 6 required states (including dragging)"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Testing configuration"
    - path: "frontend/src/types/api.ts"
      provides: "Type safety for API communication and state management"
---

<objective>
Setup the testing infrastructure and define the core TypeScript contracts that will govern the frontend-backend communication and state management.

Purpose: Ensure a solid foundation for testing and type safety before implementing logic.
Output: Vitest configuration, updated package.json, and central type definitions.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-typescript-types-api-layer-state-machine/09-RESEARCH.md
@.planning/phases/09-typescript-types-api-layer-state-machine/09-CONTEXT.md
@src/selection_maid/adapters/http/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Testing Infrastructure</name>
  <files>frontend/package.json, frontend/vitest.config.ts</files>
  <action>
    1. Install testing dependencies in the `frontend` directory: `npm install -D vitest @vue/test-utils jsdom`.
    2. Create `frontend/vitest.config.ts` configured for Vue testing with `jsdom` environment.
    3. Update `frontend/package.json` to include `"test:unit": "vitest"` and `"test:ui": "vitest --ui"` in the `scripts` section.
    4. Per D-06, we will be testing the state machine, so having a solid test runner is critical.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit -- --run</automated>
  </verify>
  <done>Vitest is installed and can be executed from the command line in the frontend directory.</done>
</task>

<task type="auto">
  <name>Task 2: Define TypeScript API Types and State Machine</name>
  <files>frontend/src/types/api.ts</files>
  <action>
    1. Create `frontend/src/types/api.ts`.
    2. Implement `Chunk`, `DocumentMetadata`, and `ExtractionResponse` interfaces matching `schemas.py` exactly (per D-02).
       - Note: `ingested_at` is a string (ISO 8601) in TS, as it's serialized from Python datetime.
    3. Implement `UploadStatus` ('idle' | 'dragging' | 'uploading' | 'processing' | 'success' | 'error') per D-06 and ROADMAP.
    4. Implement `UploadState` as a Discriminated Union (per D-05 and Research Pattern 1) to prevent impossible states.
       - 'idle': {}
       - 'dragging': {}
       - 'uploading': { progress: number }
       - 'processing': {}
       - 'success': { data: ExtractionResponse }
       - 'error': { message: string, code?: string }
  </action>
  <verify>
    <automated>cd frontend && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <done>Types are defined and the project compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Frontend | API responses must be validated against TS interfaces to ensure runtime safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
| T-09-01 | Information Disclosure | src/types/api.ts | accept | Interface definitions do not contain secrets. |
</threat_model>

<verification>
Run `npm run test:unit` and verify that the test runner initializes correctly.
Check `src/types/api.ts` for completeness against `schemas.py`.
</verification>

<success_criteria>
1. `vitest` is available and configured in the `frontend` directory.
2. `src/types/api.ts` contains all necessary interfaces for Phase 9.
3. The codebase compiles with the new types.
</success_criteria>

<output>
Create `.planning/phases/09-typescript-types-api-layer-state-machine/09-01-SUMMARY.md` when done
</output>
