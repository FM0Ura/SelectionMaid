---
phase: 09-typescript-types-api-layer-state-machine
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [frontend/src/lib/validators.ts, frontend/src/lib/validators.spec.ts, frontend/src/api/errors.ts, frontend/src/api/errors.spec.ts, frontend/src/api/ingest.ts]
autonomous: true
requirements: [UPL-03, PROC-03]

must_haves:
  truths:
    - "File validation correctly rejects files > 50MB and non-PDF/DOCX/HTML types"
    - "postIngest uses FormData and returns typed ExtractionResponse"
    - "AbortSignal.timeout is set to 130s in fetch call"
    - "Error mapper correctly translates AbortError to a user-friendly 130s timeout message"
  artifacts:
    - path: "frontend/src/lib/validators.ts"
      provides: "Pre-flight file validation"
    - path: "frontend/src/api/errors.ts"
      provides: "Human-readable error mapping"
    - path: "frontend/src/api/ingest.ts"
      provides: "API client for document ingestion"
---

<objective>
Implement the core network communication and file validation logic, ensuring that files are validated before upload and that API errors (including timeouts) are mapped to user-friendly messages.

Purpose: Protect the backend from invalid uploads and provide clear feedback on network failures.
Output: Validator functions, error mapping utility, and API client.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-typescript-types-api-layer-state-machine/09-RESEARCH.md
@.planning/phases/09-typescript-types-api-layer-state-machine/09-CONTEXT.md
@src/selection_maid/adapters/http/error_map.py
@frontend/src/types/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement File Validation</name>
  <files>frontend/src/lib/validators.ts, frontend/src/lib/validators.spec.ts</files>
  <behavior>
    - validateFile(file: File) returns null if valid.
    - validateFile(file: File) returns error message if size > 50MB.
    - validateFile(file: File) returns error message if MIME type is not application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document, or text/html.
  </behavior>
  <action>
    1. Create `frontend/src/lib/validators.ts` implementing `validateFile` (per D-07, D-08).
    2. Create `frontend/src/lib/validators.spec.ts` with tests for size and type limits.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/validators.spec.ts</automated>
  </verify>
  <done>File validation logic is implemented and verified with unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Error Mapping and API Layer</name>
  <files>frontend/src/api/errors.ts, frontend/src/api/errors.spec.ts, frontend/src/api/ingest.ts</files>
  <behavior>
    - mapApiError(AbortError) returns message about 130s timeout.
    - mapApiError(413) returns "Arquivo muito grande".
    - mapApiError(415) returns "Formato não suportado".
    - postIngest(file) sends FormData and returns ExtractionResponse.
    - postIngest(file) uses AbortSignal.timeout(130000).
  </behavior>
  <action>
    1. Create `frontend/src/api/errors.ts` implementing `mapApiError` based on `error_map.py` and D-03, D-04.
    2. Create `frontend/src/api/errors.spec.ts` testing various error scenarios.
    3. Create `frontend/src/api/ingest.ts` with `postIngest` function (per D-01 and Research Code Example 2).
       - Ensure `AbortSignal.timeout(130000)` is used.
       - Use native `fetch` and handle `!response.ok` by throwing errors that `mapApiError` can process.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/api/errors.spec.ts</automated>
  </verify>
  <done>API layer and error mapping are implemented and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Frontend | Files are validated client-side to prevent unnecessary large uploads (DoS mitigation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Availability | fetch | mitigate | AbortSignal.timeout(130s) prevents hanging requests. |
| T-09-03 | Denial of Service | validators.ts | mitigate | 50MB size limit enforced before upload. |
</threat_model>

<verification>
Verify that `validateFile` correctly flags a 51MB file as invalid.
Verify that `mapApiError` returns the specific timeout message for `AbortError`.
</verification>

<success_criteria>
1. File validation logic correctly implements UPL-03.
2. API client correctly implements PROC-03 (timeout).
3. Error messages match the requirements defined in CONTEXT.md.
</success_criteria>

<output>
Create `.planning/phases/09-typescript-types-api-layer-state-machine/09-02-SUMMARY.md` when done
</output>
