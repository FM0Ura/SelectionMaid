---
phase: 09-typescript-types-api-layer-state-machine
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: [frontend/src/composables/useUpload.ts, frontend/src/composables/useUpload.spec.ts]
autonomous: true
requirements: [PROC-01]

must_haves:
  truths:
    - "useUpload composable manages a reactive state machine with 5 distinct states"
    - "Only valid state transitions are allowed (e.g., idle -> uploading -> processing -> success)"
    - "Upload and processing states are correctly distinguished per D-06"
    - "Failed uploads transition to the error state with the mapped error message"
  artifacts:
    - path: "frontend/src/composables/useUpload.ts"
      provides: "Reactive upload logic and state management"
    - path: "frontend/src/composables/useUpload.spec.ts"
      provides: "Automated verification of the upload lifecycle"
---

<objective>
Implement the `useUpload` composable, which encapsulates the reactive state machine and orchestrates the file validation and API ingestion.

Purpose: Provide a clean, tested interface for UI components to interact with the upload process.
Output: useUpload composable and comprehensive unit tests.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-typescript-types-api-layer-state-machine/09-RESEARCH.md
@.planning/phases/09-typescript-types-api-layer-state-machine/09-CONTEXT.md
@frontend/src/types/api.ts
@frontend/src/lib/validators.ts
@frontend/src/api/ingest.ts
@frontend/src/api/errors.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement useUpload Composable</name>
  <files>frontend/src/composables/useUpload.ts, frontend/src/composables/useUpload.spec.ts</files>
  <behavior>
    - Initial state is 'idle'.
    - startUpload(file) validates file first; if invalid, transitions to 'error' (or returns error).
    - Valid file transitions state to 'uploading'.
    - After postIngest starts, transitions to 'processing' (semantically, after body is sent).
    - Success transitions to 'success' with data.
    - Failure transitions to 'error' with mapped message.
    - reset() returns state to 'idle'.
  </behavior>
  <action>
    1. Create `frontend/src/composables/useUpload.ts` implementing the state machine (per D-05, D-06).
       - Import `UploadState` from `src/types/api.ts`.
       - Import `validateFile` from `src/lib/validators.ts`.
       - Import `postIngest` from `src/api/ingest.ts`.
       - Import `mapApiError` from `src/api/errors.ts`.
    2. Create `frontend/src/composables/useUpload.spec.ts` with comprehensive tests for all transitions.
       - Mock `src/api/ingest.ts` to simulate success and various error conditions (including timeout).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/composables/useUpload.spec.ts</automated>
  </verify>
  <done>useUpload composable is fully implemented and passes all state transition tests.</done>
</task>

<task type="auto">
  <name>Task 2: Final Verification of Phase 9 Requirements</name>
  <files>frontend/package.json</files>
  <action>
    1. Run all tests in the frontend directory to ensure no regressions.
    2. Verify that `npm run build` (or `vue-tsc`) succeeds, confirming all types are correctly wired.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit -- --run && npm run build</automated>
  </verify>
  <done>Phase 9 requirements are verified through automated tests and build check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composable → API | The composable acts as the controller, ensuring the API is called only with valid inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | useUpload.ts | mitigate | Discriminated union prevents jumping into 'success' state without data. |
</threat_model>

<verification>
Run all unit tests: `cd frontend && npm run test:unit`.
Confirm all 5 states are reachable and correctly handled in `useUpload.spec.ts`.
</verification>

<success_criteria>
1. `useUpload` state machine correctly implements PROC-01 and PROC-03.
2. All unit tests pass with >90% coverage of the composable logic.
3. The frontend project builds without TypeScript errors.
</success_criteria>

<output>
Create `.planning/phases/09-typescript-types-api-layer-state-machine/09-03-SUMMARY.md` when done
</output>
