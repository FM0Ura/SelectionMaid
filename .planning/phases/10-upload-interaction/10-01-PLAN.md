---
phase: 10-upload-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/components/ui/card/Card.vue",
  "frontend/src/components/ui/alert/Alert.vue",
  "frontend/src/components/upload/ErrorBanner.vue",
  "frontend/src/components/upload/__tests__/ErrorBanner.spec.ts",
  "frontend/src/components/upload/DropZone.vue",
  "frontend/src/components/upload/__tests__/DropZone.spec.ts"
]
autonomous: true
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "ErrorBanner displays messages from useUpload state"
    - "DropZone renders different states (idle, error) based on useUpload"
  artifacts:
    - path: "frontend/src/components/upload/ErrorBanner.vue"
      provides: "UI for structured error display"
    - path: "frontend/src/components/upload/DropZone.vue"
      provides: "Main container for upload interaction"
---

<objective>
Setup the foundation for the upload interaction by adding necessary UI primitives (Card, Alert) and implementing the ErrorBanner and a functional skeleton of the DropZone component.

Purpose: To provide the structural components and error handling UI required for the upload flow.
Output: Shadcn UI components added, ErrorBanner implemented with tests, and DropZone skeleton ready for logic.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-upload-interaction/10-CONTEXT.md
@.planning/phases/10-upload-interaction/10-RESEARCH.md
@frontend/src/composables/useUpload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Shadcn UI components and implement ErrorBanner</name>
  <files>
    frontend/src/components/ui/card/Card.vue,
    frontend/src/components/ui/alert/Alert.vue,
    frontend/src/components/upload/ErrorBanner.vue,
    frontend/src/components/upload/__tests__/ErrorBanner.spec.ts
  </files>
  <action>
    1. Add Shadcn UI components 'card' and 'alert' using `npx shadcn-vue@latest add card alert` in the `frontend` directory.
    2. Create `ErrorBanner.vue` using the Shadcn Alert component. It should accept a 'message' prop and have a 'Retry' button that emits a 'retry' event.
    3. Create `ErrorBanner.spec.ts` to verify the message is displayed and the retry event is emitted.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit src/components/upload/__tests__/ErrorBanner.spec.ts</automated>
  </verify>
  <done>ErrorBanner component implemented and tested; Card and Alert UI components available.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DropZone skeleton with state integration</name>
  <files>
    frontend/src/components/upload/DropZone.vue,
    frontend/src/components/upload/__tests__/DropZone.spec.ts
  </files>
  <action>
    1. Create `DropZone.vue` as a Shadcn Card.
    2. Integrate `useUpload` to drive the UI.
    3. Implement conditional rendering for 'idle' (Welcome CTA), 'dragging' (active pulse state placeholder), and 'error' (using ErrorBanner) states.
    4. Create `DropZone.spec.ts` to verify it renders the correct state content (Welcome text for idle, ErrorBanner for error, and active state indicators for dragging).
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit src/components/upload/__tests__/DropZone.spec.ts</automated>
  </verify>
  <done>DropZone skeleton reacts correctly to useUpload states including 'dragging'; unit tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component Props | ErrorBanner receives untrusted error messages from the state (mapped from API). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | ErrorBanner | mitigate | Ensure mapApiError (in useUpload) sanitizes raw backend errors before display. |
</threat_model>

<verification>
Check that `ErrorBanner` and `DropZone` components exist and their unit tests pass.
Confirm `frontend/src/components/ui/card` and `frontend/src/components/ui/alert` are present.
</verification>

<success_criteria>
1. ErrorBanner unit tests pass (rendering message, emitting retry).
2. DropZone skeleton unit tests pass (state-driven rendering).
</success_criteria>

<output>
Create `.planning/phases/10-upload-interaction/10-01-SUMMARY.md` when done
</output>
