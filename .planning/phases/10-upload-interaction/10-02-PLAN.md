---
phase: 10-upload-interaction
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [
  "frontend/src/components/upload/DropOverlay.vue",
  "frontend/src/components/upload/DropZone.vue",
  "frontend/src/App.vue"
]
autonomous: true
requirements: [UPL-01, UPL-02]
must_haves:
  truths:
    - "Dragging a file over the card shows the DropOverlay"
    - "Dropping a file starts the upload via useUpload"
    - "Clicking 'Select File' opens file picker and starts upload"
    - "DropZone is integrated into the App's main layout"
  artifacts:
    - path: "frontend/src/components/upload/DropOverlay.vue"
      provides: "Animated visual feedback during drag"
  key_links:
    - from: "frontend/src/components/upload/DropZone.vue"
      to: "@vueuse/core/useDropZone"
      via: "D&D logic integration"
---

<objective>
Implement the core interaction logic including drag-and-drop feedback, file selection, and animations. Finally, integrate the DropZone into the main application.

Purpose: To complete the user interaction loop for document submission.
Output: Animated DropOverlay, full DropZone logic, and App.vue integration.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-upload-interaction/10-CONTEXT.md
@.planning/phases/10-upload-interaction/10-RESEARCH.md
@frontend/src/composables/useUpload.ts
@frontend/src/components/upload/DropZone.vue
@frontend/src/components/upload/ErrorBanner.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DropOverlay and Pulse Animation</name>
  <files>
    frontend/src/components/upload/DropOverlay.vue,
    frontend/src/components/upload/DropZone.vue,
    frontend/src/components/upload/__tests__/DropZone.spec.ts
  </files>
  <action>
    1. Create `DropOverlay.vue` using `motion-v` for a smooth fade-in/out transition. It should display a "Drop to Upload" message.
    2. In `DropZone.vue`, add the pulse animation to the Card border using `motion-v` when the 'dragging' state is active (D-02).
    3. Conditionally render `DropOverlay` inside `DropZone` when the user is dragging a file over it.
    4. Update `DropZone.spec.ts` to verify that `DropOverlay` is present when in the `dragging` state.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit src/components/upload/__tests__/DropZone.spec.ts</automated>
  </verify>
  <done>Animated overlay and pulse feedback implemented; verified by component tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Interaction Logic and Integrate in App.vue</name>
  <files>
    frontend/src/components/upload/DropZone.vue,
    frontend/src/App.vue,
    frontend/src/components/upload/__tests__/DropZone.spec.ts
  </files>
  <action>
    1. In `DropZone.vue`, implement `useDropZone` from `@vueuse/core`.
    2. Add logic to handle drops: if multiple files are dropped, set an error message (D-04); otherwise, call `startUpload(file)`.
    3. Implement the manual file picker using a hidden input triggered by a Shadcn Button.
    4. Synchronize the `useUpload` dragging state with the `isOverDropZone` value from the hook.
    5. Update `DropZone.spec.ts` to mock `useUpload` and verify:
       - `startUpload` is called with a single file.
       - A "multiple files" error is triggered when more than one file is dropped.
    6. Replace the placeholder button in `App.vue` with the new `DropZone` component.
  </action>
  <verify>
    <automated>cd frontend && npm run test:unit src/components/upload/__tests__/DropZone.spec.ts</automated>
  </verify>
  <done>Full drag-and-drop and file picker logic implemented; D-04 verified; component integrated into App.vue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File Input | Browser file picker and drag events provide File objects to the application. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Denial of Service | DropZone | mitigate | Client-side validation of file size (50MB) and count (1) before upload starts. |
</threat_model>

<verification>
Manual verification:
1. Drag a file over the DropZone -> see pulse and overlay.
2. Drop a file -> see upload start.
3. Drag multiple files -> see error message.
4. Click "Select File" -> picker opens and selection starts upload.
</verification>

<success_criteria>
1. Drag-and-drop works with visual feedback.
2. File picker works.
3. Error handling for multiple files is active.
4. DropZone is visible and functional in the main app.
</success_criteria>

<output>
Create `.planning/phases/10-upload-interaction/10-02-SUMMARY.md` when done
</output>
