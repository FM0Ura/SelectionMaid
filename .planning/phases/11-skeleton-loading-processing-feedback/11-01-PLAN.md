---
phase: 11-skeleton-loading-processing-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/composables/useUpload.ts, frontend/src/composables/useUpload.spec.ts, frontend/src/style.css, frontend/src/components/upload/SkeletonChunk.vue, frontend/src/components/upload/ProcessingCard.vue, frontend/package.json]
autonomous: true
requirements: [PROC-02]

must_haves:
  truths:
    - "useUpload exposes a reactive elapsedSeconds ref"
    - "Timer increments every second only while status is 'processing'"
    - "SkeletonChunk component renders with a shimmer animation"
    - "ProcessingCard displays the elapsed timer in MM:SS format"
  artifacts:
    - path: "frontend/src/components/upload/SkeletonChunk.vue"
      provides: "Shimmering placeholder card"
    - path: "frontend/src/components/upload/ProcessingCard.vue"
      provides: "Compact processing status with timer"
  key_links:
    - from: "useUpload.ts"
      to: "@vueuse/core"
      via: "useIntervalFn"
---

<objective>
Implement the foundation for processing feedback: a reactive timer in the state machine and the atomic skeleton/status components.

Purpose: Eliminate perceived dead time by providing a live counter and structural placeholders.
Output: Extended useUpload composable, SkeletonChunk, and ProcessingCard components.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-skeleton-loading-processing-feedback/11-CONTEXT.md
@.planning/phases/11-skeleton-loading-processing-feedback/11-UI-SPEC.md
@.planning/phases/11-skeleton-loading-processing-feedback/11-RESEARCH.md
@frontend/src/composables/useUpload.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend useUpload with Processing Timer</name>
  <files>frontend/src/composables/useUpload.ts, frontend/src/composables/useUpload.spec.ts</files>
  <behavior>
    - When status transitions to 'processing', elapsedSeconds resets to 0 and starts incrementing every 1000ms.
    - When status is NOT 'processing', the timer is paused.
    - elapsedSeconds is exposed as a readonly ref.
  </behavior>
  <action>
    - Import `useIntervalFn` from `@vueuse/core`.
    - Add `elapsedSeconds` ref initialized to 0.
    - Setup `useIntervalFn` to increment `elapsedSeconds`.
    - Add a `watch` on `state.value.status` (or use logic in `startUpload`) to `resume()` and reset the timer when entering 'processing', and `pause()` otherwise.
    - Update `useUpload.spec.ts` to verify timer behavior (use `vi.useFakeTimers()` to test increments).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/composables/useUpload.spec.ts</automated>
  </verify>
  <done>useUpload exposes elapsedSeconds and it increments correctly during the processing state.</done>
</task>

<task type="auto">
  <name>Task 2: Setup Shimmer Animation and Skeleton</name>
  <files>frontend/src/style.css, frontend/package.json</files>
  <action>
    - Install shadcn-vue skeleton: `cd frontend && npx shadcn-vue@latest add skeleton`.
    - Define the `shimmer` keyframes and animation in `frontend/src/style.css` using Tailwind v4 `@theme` block as per RESEARCH.md.
    - Define a `.shimmer-gradient` utility class in `style.css` using `oklch(0.205 0 0)` base and `oklch(0.922 0 0 / 0.1)` highlight.
  </action>
  <verify>
    <automated>grep -q "@keyframes shimmer" frontend/src/style.css && ls frontend/src/components/ui/skeleton</automated>
  </verify>
  <done>Shimmer animation is defined in CSS and shadcn skeleton component is installed.</done>
</task>

<task type="auto">
  <name>Task 3: Create SkeletonChunk and ProcessingCard</name>
  <files>frontend/src/components/upload/SkeletonChunk.vue, frontend/src/components/upload/ProcessingCard.vue</files>
  <action>
    - Create `SkeletonChunk.vue`: A card with `oklch(0.205 0 0)` background, containing 2-3 skeleton lines with the `.shimmer-gradient animate-shimmer` classes. Match layout of future result cards (D-02).
    - Create `ProcessingCard.vue`: Receives `elapsedSeconds` prop. Displays "Processando documento..." (font-weight 600) and the timer in `MM:SS` format (accent color `oklch(0.922 0 0)`).
    - Use Lucide `Loader2` icon (animate-spin) as a secondary indicator.
  </action>
  <verify>
    <automated>ls frontend/src/components/upload/SkeletonChunk.vue frontend/src/components/upload/ProcessingCard.vue</automated>
  </verify>
  <done>Atomic feedback components are created following UI-SPEC.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client State | `elapsedSeconds` is internal UI state, not used for security decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | useUpload.ts | mitigate | Use fake timers in Vitest to ensure logic correctness. |
</threat_model>

<verification>
`cd frontend && npm run test:unit`
</verification>

<success_criteria>
- useUpload timer tests pass with fake timers.
- SkeletonChunk and ProcessingCard exist and follow visual specs.
</success_criteria>

<output>
Create `.planning/phases/11-skeleton-loading-processing-feedback/11-01-SUMMARY.md` when done
</output>
