---
phase: 11-skeleton-loading-processing-feedback
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [frontend/src/components/upload/DropZone.vue, frontend/src/App.vue]
autonomous: true
requirements: [PROC-02]

must_haves:
  truths:
    - "DropZone card reduces size and padding when status is 'processing'"
    - "Skeletons appear below the minimized card during processing"
    - "Transition between idle and processing states is smooth via motion-v"
  artifacts:
    - path: "frontend/src/components/upload/DropZone.vue"
      provides: "Minimized state for processing"
  key_links:
    - from: "DropZone.vue"
      to: "motion-v"
      via: "layout prop"
---

<objective>
Refactor the DropZone and App layout to support the "minimize" transition and display the skeleton placeholders.

Purpose: Provide a continuous and polished visual flow from upload to backend processing.
Output: Refactored DropZone with layout animation and integrated skeleton list in App.vue.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-skeleton-loading-processing-feedback/11-CONTEXT.md
@.planning/phases/11-skeleton-loading-processing-feedback/11-UI-SPEC.md
@.planning/phases/11-skeleton-loading-processing-feedback/11-RESEARCH.md
@.planning/phases/11-skeleton-loading-processing-feedback/11-VALIDATION.md
@frontend/src/components/upload/DropZone.vue
@frontend/src/App.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor DropZone for Minimize State</name>
  <files>frontend/src/components/upload/DropZone.vue</files>
  <action>
    - Import `motion` from `motion-v`.
    - Change the root element of `DropZone.vue` to `motion.div`.
    - Apply the `layout` prop to the root `motion.div`.
    - Bind dynamic classes based on `state.status === 'processing'`:
      - idle/dragging: `min-h-96 p-8`
      - processing: `min-h-48 p-6` (compact per UI-SPEC)
    - Conditionally render `ProcessingCard` when status is `processing`, and the current upload UI otherwise.
    - Ensure the transition is smooth by letting `motion-v` interpolate layout shifts.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/upload/__tests__/DropZone.spec.ts</automated>
  </verify>
  <done>DropZone correctly transitions to a compact state during processing.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Skeleton List in App.vue</name>
  <files>frontend/src/App.vue</files>
  <action>
    - Import `SkeletonChunk` and `useUpload`.
    - Get `state` from `useUpload`.
    - Add a `v-if="state.status === 'processing'"` container below the `<DropZone />`.
    - Render 3-5 `SkeletonChunk` components inside this container (D-02).
    - Use `initial: { opacity: 0, y: 20 }, animate: { opacity: 1, y: 0 }` via `motion-v` or simple CSS transitions for the skeletons appearance.
    - Wrap the list in a container with `w-full max-w-2xl mt-8 space-y-4` to match the design.
  </action>
  <verify>
    <automated>grep -q "SkeletonChunk" frontend/src/App.vue</automated>
  </verify>
  <done>App.vue displays the skeleton list during document processing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component Props | Components receive state from `useUpload` composable; no direct external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Information Disclosure | App.vue | accept | Skeleton data is placeholder only; no real data leaked. |
</threat_model>

<verification>
`cd frontend && npm run test:unit`
</verification>

<success_criteria>
- DropZone test suite passes.
- Visual check: status change to processing triggers minimize + skeletons.
</success_criteria>

<output>
Create `.planning/phases/11-skeleton-loading-processing-feedback/11-02-SUMMARY.md` when done
</output>
