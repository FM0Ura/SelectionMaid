---
phase: 12-result-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/src/assets/index.css, frontend/src/lib/formatters.ts, frontend/src/components/result/MarkdownRenderer.vue]
autonomous: true
requirements: [RES-01]
must_haves:
  truths:
    - "Markdown content is rendered as HTML (headings, bold, etc.) instead of raw strings."
    - "Rendered HTML is sanitized to prevent XSS (no script tags allowed)."
    - "Metadata values (sizes, durations) are formatted for human readability."
  artifacts:
    - path: "frontend/src/lib/formatters.ts"
      provides: "Formatting utilities for document metadata"
    - path: "frontend/src/components/result/MarkdownRenderer.vue"
      provides: "Safe Markdown to HTML conversion"
---

<objective>
Setup the foundation for result display by installing necessary dependencies, creating formatting utilities, and implementing a secure Markdown renderer.

Purpose: Ensure that extracted text is readable and metadata is properly presented while maintaining security against malicious content.
Output: Project dependencies updated, formatting library created, and MarkdownRenderer component ready for use.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-result-display/12-CONTEXT.md
@.planning/phases/12-result-display/12-RESEARCH.md
@frontend/src/assets/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure Tailwind Typography</name>
  <files>frontend/package.json, frontend/src/assets/index.css</files>
  <action>
    Install core rendering dependencies: markdown-it, dompurify, and @tailwindcss/typography.
    Also install their types as devDependencies.
    Update 'frontend/src/assets/index.css' to include the typography plugin for Tailwind v4 using '@plugin "@tailwindcss/typography";'.
  </action>
  <verify>
    <automated>cd frontend && npm list markdown-it dompurify @tailwindcss/typography && grep -q "@plugin \"@tailwindcss/typography\"" src/assets/index.css</automated>
  </verify>
  <done>Dependencies installed and Tailwind Typography configured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement formatting utilities</name>
  <files>frontend/src/lib/formatters.ts, frontend/src/lib/formatters.spec.ts</files>
  <behavior>
    - formatFileSize: 1024 -> "1.0 KB", 1048576 -> "1.0 MB"
    - formatDuration: 1.5 -> "1.5s", 0.5 -> "< 1s"
    - formatDate: ISO string -> "DD/MM/YY HH:mm" (pt-BR format)
  </behavior>
  <action>
    Create 'frontend/src/lib/formatters.ts' with utility functions for file size, duration, and date formatting as defined in RESEARCH.md.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/formatters.spec.ts</automated>
  </verify>
  <done>Formatting utilities implemented and verified with tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement secure MarkdownRenderer component</name>
  <files>frontend/src/components/result/MarkdownRenderer.vue, frontend/src/components/result/MarkdownRenderer.spec.ts</files>
  <behavior>
    - Renders "# Title" as <h1>Title</h1>
    - Renders "**bold**" as <strong>bold</strong>
    - Sanitizes malicious HTML: "<script>alert(1)</script>" should be stripped or escaped, not executed.
    - Applies 'prose prose-invert max-w-none' classes for styling.
  </behavior>
  <action>
    Implement 'MarkdownRenderer.vue' using markdown-it for parsing and dompurify for sanitization.
    Ensure 'html: false' is set in markdown-it options for extra security.
    Wrap the output in a div with Tailwind Typography classes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/result/MarkdownRenderer.spec.ts</automated>
  </verify>
  <done>MarkdownRenderer component implemented with security verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Frontend | Extraction results (Markdown) are untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | MarkdownRenderer | mitigate | Use DOMPurify to sanitize HTML output from markdown-it. |
| T-12-02 | Information Disclosure | Metadata Display | accept | Metadata is derived from the uploaded file; no PII unless present in file. |
| T-12-SC | Tampering | npm installs | mitigate | Verified packages in RESEARCH.md. Use package-lock.json. |
</threat_model>

<verification>
Check that `MarkdownRenderer` successfully renders a complex Markdown string and that `formatters.ts` handles various edge cases (0 bytes, 0 seconds).
</verification>

<success_criteria>
1. `npm list` shows all 3 dependencies installed.
2. `vitest` passes for formatters and MarkdownRenderer.
3. No XSS possible through MarkdownRenderer (verified by test).
</success_criteria>

<output>
Create `.planning/phases/12-result-display/12-01-SUMMARY.md` when done
</output>
