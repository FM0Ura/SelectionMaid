---
phase: 12-result-display
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: [frontend/src/components/result/MetadataCard.vue, frontend/src/components/result/ChunkCard.vue]
autonomous: true
requirements: [RES-03, RES-04]
must_haves:
  truths:
    - "Metadata card displays document type, language, title, and processing time."
    - "Chunk card displays section title, page range, and word count."
    - "Copy button in chunk card copies raw text to clipboard."
    - "Copy button provides visual feedback ('Copied!') after successful copy."
  artifacts:
    - path: "frontend/src/components/result/MetadataCard.vue"
      provides: "Overview of document processing results"
    - path: "frontend/src/components/result/ChunkCard.vue"
      provides: "Individual chunk display with copy functionality"
---

<objective>
Implement the core display components for document results: a metadata summary card and detailed chunk cards with copy-to-clipboard functionality.

Purpose: Provide users with a structured and actionable view of the extraction results.
Output: MetadataCard and ChunkCard components ready for integration.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-result-display/12-CONTEXT.md
@.planning/phases/12-result-display/12-RESEARCH.md
@frontend/src/types/api.ts
@frontend/src/lib/formatters.ts
@frontend/src/components/result/MarkdownRenderer.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MetadataCard component</name>
  <files>frontend/src/components/result/MetadataCard.vue, frontend/src/components/result/MetadataCard.spec.ts</files>
  <behavior>
    - Displays 'DocumentMetadata' fields correctly.
    - Uses 'formatters.ts' for type-specific formatting.
    - Displays 'elapsedSeconds' passed as prop.
    - Uses shadcn Card component for styling.
  </behavior>
  <action>
    Implement 'MetadataCard.vue' to display doc_type, language, title, page_count, chunk_count, and processing time.
    Follow the minimal dark mode style.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/result/MetadataCard.spec.ts</automated>
  </verify>
  <done>MetadataCard component implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ChunkCard component with copy feedback</name>
  <files>frontend/src/components/result/ChunkCard.vue, frontend/src/components/result/ChunkCard.spec.ts</files>
  <behavior>
    - Renders content using 'MarkdownRenderer'.
    - Displays 'section_title' if present.
    - Displays page range (e.g., "Pgs 1-2") and word count.
    - Copy button calls 'useClipboard' and shows "Copied!" for 2 seconds.
  </behavior>
  <action>
    Implement 'ChunkCard.vue' following the pattern in RESEARCH.md.
    Use Lucide icons (Copy, Check) for the copy button.
    Ensure 'word_count' and page range are clearly visible as context.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/result/ChunkCard.spec.ts</automated>
  </verify>
  <done>ChunkCard component implemented with copy-to-clipboard functionality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component Props → UI | Components receive data from the success state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information Disclosure | Clipboard | accept | User explicitly triggers copy action. |
| T-12-04 | Tampering | Props Injection | mitigate | MarkdownRenderer handles sanitization of the 'content' prop in ChunkCard. |
</threat_model>

<verification>
Verify that components render correctly with mock data and that the copy button correctly interacts with the Clipboard API (verified in unit tests with vitest mocks).
</verification>

<success_criteria>
1. MetadataCard shows all expected fields correctly formatted.
2. ChunkCard renders markdown and provides copy feedback.
3. Both components follow the established dark mode theme.
</success_criteria>

<output>
Create `.planning/phases/12-result-display/12-02-SUMMARY.md` when done
</output>
