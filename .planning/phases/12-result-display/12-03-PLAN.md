---
phase: 12-result-display
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified: [frontend/src/components/result/ResultView.vue, frontend/src/App.vue]
autonomous: true
requirements: [RES-01, RES-03, RES-04]
must_haves:
  truths:
    - "Result view is displayed when the upload status is 'success'."
    - "Metadata card and chunk cards are rendered with real API data."
    - "Clicking 'Novo Upload' (reset) returns the user to the idle state."
    - "DropZone is hidden when results are displayed."
  artifacts:
    - path: "frontend/src/components/result/ResultView.vue"
      provides: "Full result screen layout and reset coordination"
  key_links:
    - from: "frontend/src/App.vue"
      to: "frontend/src/components/result/ResultView.vue"
      via: "v-if success state check"
---

<objective>
Integrate the result components into a unified ResultView and wire it into the main App layout, ensuring the user can view results and reset the state for a new upload.

Purpose: Complete the document extraction flow from a user perspective.
Output: Fully functional result display in the SPA.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-result-display/12-CONTEXT.md
@frontend/src/App.vue
@frontend/src/composables/useUpload.ts
@frontend/src/components/result/MetadataCard.vue
@frontend/src/components/result/ChunkCard.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ResultView container</name>
  <files>frontend/src/components/result/ResultView.vue, frontend/src/components/result/ResultView.spec.ts</files>
  <behavior>
    - Accepts 'data' (ExtractionResponse) and 'elapsedSeconds' as props.
    - Renders MetadataCard at the top.
    - Renders a list of ChunkCard components for all chunks.
    - Includes a "Novo Upload" button that emits a 'reset' event.
    - Uses a vertical layout with spacing between cards.
  </behavior>
  <action>
    Implement 'ResultView.vue' as the main container for results.
    Ensure the layout is responsive and center-aligned like the rest of the app.
    Add the reset button with a Lucide 'RefreshCw' or 'ArrowLeft' icon.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/result/ResultView.spec.ts</automated>
  </verify>
  <done>ResultView container implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ResultView into App.vue</name>
  <files>frontend/src/App.vue</files>
  <action>
    Modify 'App.vue' to conditionally render 'ResultView' when 'upload.state.value.status === "success"'.
    Pass 'upload.state.value.data' and 'upload.elapsedSeconds.value' to 'ResultView'.
    Connect the '@reset' event from 'ResultView' to 'upload.reset()'.
    Ensure 'DropZone' and the processing skeletons are hidden when 'ResultView' is active.
  </action>
  <verify>
    <automated>grep -q "ResultView" frontend/src/App.vue && grep -q "v-else-if=\"upload.state.value.status === 'success'\"" frontend/src/App.vue</automated>
  </verify>
  <done>App.vue integrated with the result display flow.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Functional verification of the result display</name>
  <what-built>End-to-end result display flow.</what-built>
  <how-to-verify>
    1. Run 'npm run dev' in 'frontend'.
    2. Upload a sample PDF/DOCX file.
    3. Wait for processing to complete.
    4. Verify the metadata card appears with correct info.
    5. Verify chunks are rendered with Markdown formatting.
    6. Click a copy button and verify the text is in the clipboard.
    7. Click 'Novo Upload' and verify it returns to the drop zone.
  </how-to-verify>
  <resume-signal>Results are displayed correctly and reset works.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ResultView → App | State transitions (reset) affect the whole app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Denial of Service | ResultView | accept | Large number of chunks might slow down rendering, but Phase 13 will address stagger. |
</threat_model>

<verification>
Check that the transition from 'processing' to 'success' is seamless and that all data from the API is accurately represented in the UI.
</verification>

<success_criteria>
1. User can successfully complete an upload and see results.
2. Metadata and chunks are visually correct.
3. Reset button works as expected.
</success_criteria>

<output>
Create `.planning/phases/12-result-display/12-03-SUMMARY.md` when done
</output>
