---
phase: 13-animation-view-transitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/App.vue, frontend/src/components/upload/DropZone.vue, frontend/src/components/upload/DropOverlay.vue]
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Dragging a file over DropZone shows glassmorphism (backdrop-blur) and a glowing border"
    - "Clicking 'Reset' or 'Novo Upload' triggers a global fade-out of results and fade-in of upload view"
  artifacts:
    - path: "frontend/src/App.vue"
      provides: "Global AnimatePresence for view transitions"
    - path: "frontend/src/components/upload/DropZone.vue"
      provides: "Glow and Glassmorphism state implementation"
  key_links:
    - from: "frontend/src/App.vue"
      to: "motion-v"
      via: "AnimatePresence and motion.div"
---

<objective>
Implement global view transitions for the reset flow and upgrade the DropZone with advanced visual effects for the drag-active state.

Purpose: Improve the overall feel of the application by making state changes smooth and the primary interaction area more responsive.
Output: Updated App.vue with AnimatePresence and upgraded DropZone component.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-animation-view-transitions/13-CONTEXT.md
@frontend/src/App.vue
@frontend/src/components/upload/DropZone.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrate Global View Transitions in App.vue</name>
  <files>frontend/src/App.vue</files>
  <action>
    Implement D-05 (Global Fade Out/In) using `AnimatePresence` and `motion.div`.
    - Wrap the conditional rendering of the upload section and `ResultView` with `<AnimatePresence mode="wait">`.
    - Use `motion.div` for both sections with `initial="{ opacity: 0 }"`, `animate="{ opacity: 1 }"`, and `exit="{ opacity: 0 }"` variants.
    - Ensure unique `key` attributes are set for each branch to trigger the transition.
    - Adjust duration to approximately 0.3s for a snappy feel.
  </action>
  <verify>
    <automated>Check if AnimatePresence and keys are present in App.vue</automated>
    <human-check>Click 'Reset' in the UI and confirm the smooth global fade transition.</human-check>
  </verify>
  <done>App.vue orchestrates smooth transitions between upload and result states.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade DropZone with Glassmorphism and Glow</name>
  <files>frontend/src/components/upload/DropZone.vue, frontend/src/components/upload/DropOverlay.vue</files>
  <action>
    Implement D-04 (Glass + Glow) for the `dragging` state.
    - In `DropZone.vue`, update the `animate` prop of the main `motion.div`.
    - When `dragging`, apply `backdrop-blur-md` and a custom glow effect.
    - Use Tailwind v4 OKLCH for the glow: `oklch(0.623 0.214 259.815 / 0.2)` (primary-like glow).
    - Update `DropOverlay.vue` if needed to complement the glass effect (e.g., subtle transparency adjustments).
    - Reference Research Insight: Use spring or smooth duration for the glow pulse.
  </action>
  <verify>
    <automated>grep -E "backdrop-blur|oklch" frontend/src/components/upload/DropZone.vue</automated>
    <human-check>Drag a file over the DropZone and verify the glassmorphism blur and the glowing border effect.</human-check>
  </verify>
  <done>DropZone provides high-fidelity visual feedback during drag-and-drop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI Components | No external input data processed in these visual-only changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | npm packages | mitigate | SLOPCHECK + blocking human checkpoint for motion-v |
</threat_model>

<verification>
- View transitions in App.vue function correctly with mode="wait".
- DropZone visual upgrades are only active during the 'dragging' state.
</verification>

<success_criteria>
- Global Fade Out/In (D-05) implemented in App.vue.
- Glassmorphism + Glow (D-04) implemented in DropZone.vue.
</success_criteria>

<output>
Create `.planning/phases/13-animation-view-transitions/13-01-SUMMARY.md` when done
</output>
