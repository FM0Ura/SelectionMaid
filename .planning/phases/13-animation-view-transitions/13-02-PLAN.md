---
phase: 13-animation-view-transitions
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified: [frontend/src/components/upload/ProcessingCard.vue, frontend/src/components/result/MetadataCard.vue, frontend/src/components/result/ResultView.vue, frontend/src/components/result/ChunkCard.vue]
autonomous: true
requirements: [RES-02, NAV-01]

must_haves:
  truths:
    - "Processing card morphs into the Metadata card when extraction completes"
    - "Result chunks appear in a staggered 'Slide Up + Fade' sequence"
  artifacts:
    - path: "frontend/src/components/upload/ProcessingCard.vue"
      provides: "layoutId for morphing transition"
    - path: "frontend/src/components/result/MetadataCard.vue"
      provides: "layoutId for morphing transition"
    - path: "frontend/src/components/result/ResultView.vue"
      provides: "Stagger orchestration for chunk list"
  key_links:
    - from: "ProcessingCard"
      to: "MetadataCard"
      via: "motion-v layoutId='metadata-card'"
---

<objective>
Implement high-fidelity transitions between the processing state and the result state, including layout morphing and staggered content reveal.

Purpose: Provide a "premium" feel to the content reveal, making the transition between processing and results feel like a single continuous flow.
Output: Morphing transitions for the metadata card and staggered entry for the chunk list.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-animation-view-transitions/13-CONTEXT.md
@frontend/src/components/upload/ProcessingCard.vue
@frontend/src/components/result/MetadataCard.vue
@frontend/src/components/result/ResultView.vue
@frontend/src/components/result/ChunkCard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Layout Morphing for the Metadata Card</name>
  <files>frontend/src/components/upload/ProcessingCard.vue, frontend/src/components/result/MetadataCard.vue</files>
  <action>
    Implement D-03 (Layout Morph) by assigning a shared `layoutId` to both cards.
    - Wrap the content of `ProcessingCard.vue` in a `motion.div` with `layoutId="metadata-card"`.
    - Wrap the content of `MetadataCard.vue` in a `motion.div` with `layoutId="metadata-card"`.
    - Reference Research Insight: Use spring settings (stiffness: 260, damping: 30) for the layout transition.
    - Ensure both components are handled within the global `AnimatePresence` in `App.vue` (from Plan 01) to allow the morphing during the state swap.
  </action>
  <verify>
    <automated>grep "layoutId=\"metadata-card\"" frontend/src/components/upload/ProcessingCard.vue frontend/src/components/result/MetadataCard.vue</automated>
    <human-check>Watch the transition from processing to results; the small processing indicator should appear to expand/transform into the metadata card.</human-check>
  </verify>
  <done>Smooth layout morphing transition implemented between processing and success states.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Staggered Result Reveal</name>
  <files>frontend/src/components/result/ResultView.vue, frontend/src/components/result/ChunkCard.vue</files>
  <action>
    Implement D-01/D-02 (Staggered Slide Up + Fade) for the chunk list.
    - In `ResultView.vue`, update the chunks container to be a `motion.div` with parent variants that orchestrate `staggerChildren` (0.05s-0.1s delay).
    - In `ChunkCard.vue`, convert the root or main wrapper to a `motion.div` with child variants for `hidden` (opacity 0, y: 20) and `show` (opacity 1, y: 0).
    - Ensure the `MetadataCard` also follows a similar entry animation (either as part of the stagger or immediately preceding it).
  </action>
  <verify>
    <automated>grep "staggerChildren" frontend/src/components/result/ResultView.vue</automated>
    <human-check>Verify that chunk cards appear one-by-one with a cascade effect after the metadata card is revealed.</human-check>
  </verify>
  <done>Chunk list reveals itself with an elegant staggered animation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI Components | No external input data processed in these visual-only changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | npm packages | mitigate | SLOPCHECK + blocking human checkpoint for motion-v |
</threat_model>

<verification>
- LayoutId "metadata-card" is shared correctly between components.
- Stagger delay is within the 0.05s-0.1s range.
- All animations are performant and do not cause layout thrashing.
</verification>

<success_criteria>
- Layout Morphing (D-03) implemented.
- Staggered Slide Up + Fade (D-01, D-02) implemented.
</success_criteria>

<output>
Create `.planning/phases/13-animation-view-transitions/13-02-SUMMARY.md` when done
</output>
