---
phase: 14
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/result/MarkdownRenderer.vue
  - frontend/src/components/result/MarkdownRenderer.spec.ts
autonomous: true
requirements: [RES-01]

user_setup: []

must_haves:
  truths:
    - "Fenced code blocks in Markdown chunks have syntax highlighting (hljs-* classes on spans)"
    - "Wide tables scroll horizontally inside their card — no overflow clipping"
    - "All links rendered by MarkdownRenderer open in a new tab with noopener noreferrer"
    - "Inline code has purple background and styled text, without leading/trailing backtick artifacts"
    - "Code block pre background is purple-tinted (prose-pre:bg-purple-950/60), not github-dark's default"
  artifacts:
    - path: "frontend/src/components/result/MarkdownRenderer.vue"
      provides: "Updated markdown-it instance with highlightjs plugin + renderer overrides + prose classes"
      contains: "markdown-it-highlightjs"
    - path: "frontend/src/components/result/MarkdownRenderer.spec.ts"
      provides: "Tests for table scroll, link target, syntax highlight output"
      contains: "overflow-x-auto"
  key_links:
    - from: "frontend/src/components/result/MarkdownRenderer.vue"
      to: "markdown-it-highlightjs"
      via: "markdown.use(highlightjs, { auto: true, code: true, inline: false })"
      pattern: "use.*highlightjs"
    - from: "frontend/src/components/result/MarkdownRenderer.vue"
      to: "highlight.js/styles/github-dark.css"
      via: "CSS import at top of script"
      pattern: "import.*github-dark"
---

<objective>
Install markdown-it-highlightjs and highlight.js, then upgrade MarkdownRenderer.vue with syntax highlighting, table scroll wrapper, link target overrides, and updated prose classes. Extend the spec with targeted tests for the new behaviors.

Purpose: The MarkdownRenderer currently renders tables without scroll, code without highlight, and links without new-tab behavior. This plan fixes all three output formatting issues per D-10 through D-15.

Output: Updated MarkdownRenderer.vue with the full set of formatting features; extended MarkdownRenderer.spec.ts with table, link, and syntax highlight tests.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-CONTEXT.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-RESEARCH.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md

<interfaces>
<!-- Current MarkdownRenderer.vue full file: -->
<!-- script: imports DOMPurify, MarkdownIt, computed. Creates markdown = new MarkdownIt({html:false,linkify:true,typographer:true}). -->
<!-- sanitizedHtml computed = DOMPurify.sanitize(markdown.render(props.content)) -->
<!-- template: <div class="prose prose-invert max-w-none text-foreground prose-headings:text-foreground prose-a:text-primary prose-strong:text-foreground prose-code:text-foreground prose-pre:border prose-pre:border-border prose-pre:bg-muted" v-html="sanitizedHtml" /> -->

<!-- markdown-it-highlightjs API (per RESEARCH.md Critical Technical Finding #1): -->
<!-- import highlightjs from 'markdown-it-highlightjs' -->
<!-- markdown.use(highlightjs, { auto: true, code: true, inline: false }) -->
<!-- Plugin uses full hljs bundle automatically — no hljs instance needed -->

<!-- renderer.rules.link_open override (per RESEARCH.md finding #4): -->
<!-- tokens[idx].attrSet('target', '_blank') and attrSet('rel', 'noopener noreferrer') -->

<!-- renderer.rules.table_open/close (per RESEARCH.md finding #5): -->
<!-- table_open returns '<div class="overflow-x-auto"><table>' -->
<!-- table_close returns '</table></div>' -->

<!-- DOMPurify: class attr is in DEFAULT_ALLOWED_ATTR — hljs classes will NOT be stripped -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install markdown-it-highlightjs and highlight.js</name>
  <files>
    frontend/package.json
    frontend/package-lock.json
  </files>
  <read_first>
    - frontend/package.json — check current dependencies to confirm these packages are absent
    - .planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-RESEARCH.md — Package Legitimacy Audit section confirming [ASSUMED] verdict and approved status
  </read_first>
  <behavior>
    - After install: import 'markdown-it-highlightjs' resolves without TypeScript error
    - After install: import 'highlight.js/styles/github-dark.css' resolves in Vite
    - package.json dependencies include "markdown-it-highlightjs" and "highlight.js"
  </behavior>
  <action>
    Run: cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm install markdown-it-highlightjs highlight.js

    These packages are pre-approved in RESEARCH.md Package Legitimacy Audit (both marked [ASSUMED] due to slopcheck PyPI ecosystem mismatch, confirmed legitimate via npm view and GitHub source).

    After install, verify both appear in the "dependencies" section of package.json (not devDependencies — they are runtime dependencies used in the browser).
  </action>
  <verify>
    <automated>grep -c '"markdown-it-highlightjs"' /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/package.json</automated>
    <automated>grep -c '"highlight.js"' /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/package.json</automated>
  </verify>
  <done>
    - frontend/package.json dependencies section includes "markdown-it-highlightjs" and "highlight.js"
    - node_modules/markdown-it-highlightjs directory exists
    - node_modules/highlight.js/styles/github-dark.css exists
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade MarkdownRenderer.vue with highlightjs, table scroll, link target, and prose overrides</name>
  <files>
    frontend/src/components/result/MarkdownRenderer.vue
    frontend/src/components/result/MarkdownRenderer.spec.ts
  </files>
  <read_first>
    - frontend/src/components/result/MarkdownRenderer.vue — read the complete file (25 lines) to see current imports, markdown instance, and template prose classes
    - frontend/src/components/result/MarkdownRenderer.spec.ts — read the complete file (37 lines) to see existing test structure and avoid breaking existing tests
    - .planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md — the MarkdownRenderer section with the full updated script and template patterns
  </read_first>
  <behavior>
    - Test: table markdown "| A | B |\n|---|---|\n| 1 | 2 |" → rendered wrapper.find('.overflow-x-auto').exists() is true
    - Test: table markdown → wrapper.find('.overflow-x-auto table').exists() is true
    - Test: link markdown "[Link](https://example.com)" → wrapper.find('a').attributes('target') === '_blank'
    - Test: link markdown → wrapper.find('a').attributes('rel') === 'noopener noreferrer'
    - Test: fenced code "```python\nprint('hello')\n```" → wrapper.find('code.hljs').exists() is true
    - Existing tests continue to pass (heading/emphasis rendering, XSS sanitization, prose class check)
  </behavior>
  <action>
    Replace the complete content of frontend/src/components/result/MarkdownRenderer.vue with the updated version from 14-PATTERNS.md:

    Script changes (keep defineProps at the same position, replace everything else in script):
    - Add import for markdown-it Token/Options/Renderer types: `import type { Token, Options, Renderer } from 'markdown-it'`
    - Add `import highlightjs from 'markdown-it-highlightjs'`
    - Add `import 'highlight.js/styles/github-dark.css'` after the highlightjs import
    - Chain `.use(highlightjs, { auto: true, code: true, inline: false })` on the MarkdownIt constructor
    - Add defaultLinkOpen fallback and renderer.rules.link_open override that calls attrSet('target', '_blank') and attrSet('rel', 'noopener noreferrer') — assign at module scope after the markdown instance
    - Add renderer.rules.table_open returning '<div class="overflow-x-auto"><table>' and renderer.rules.table_close returning '</table></div>' — at module scope after link_open

    Template changes — replace the existing prose div class attribute with the full class list from 14-PATTERNS.md:
    "prose prose-invert prose-sm max-w-none text-foreground prose-headings:text-foreground prose-a:text-purple-400 prose-a:no-underline prose-a:hover:underline prose-strong:text-foreground prose-code:bg-purple-950/40 prose-code:text-purple-200 prose-code:rounded prose-code:px-1 prose-code:before:content-none prose-code:after:content-none prose-pre:bg-purple-950/60 prose-pre:border prose-pre:border-purple-900/40 prose-table:border prose-table:border-purple-900/40 prose-thead:bg-purple-950/60 prose-img:max-w-full prose-img:h-auto"

    Note on prose-code backtick fix: if `prose-code:before:content-none prose-code:after:content-none` does not suppress backticks at runtime (Tailwind v4 chaining limitation), fall back to adding to style.css: `.prose code::before, .prose code::after { content: none; }`. Prefer the Tailwind class approach first.

    Extend frontend/src/components/result/MarkdownRenderer.spec.ts — add three new it() blocks inside the existing describe('MarkdownRenderer') block (after the existing three tests), covering the three behaviors listed above. Mock URL.createObjectURL is NOT needed for MarkdownRenderer tests. No new vi.mock() needed — DOMPurify and markdown-it work in jsdom.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit -- --reporter=verbose src/components/result/MarkdownRenderer.spec.ts 2>&1 | tail -20</automated>
    <automated>grep -c "markdown-it-highlightjs" /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/src/components/result/MarkdownRenderer.vue</automated>
  </verify>
  <done>
    - All 6 MarkdownRenderer tests pass (3 existing + 3 new)
    - MarkdownRenderer.vue imports markdown-it-highlightjs and highlight.js/styles/github-dark.css
    - MarkdownRenderer.vue has renderer.rules.table_open wrapping tables in overflow-x-auto div
    - MarkdownRenderer.vue has renderer.rules.link_open adding target=_blank and rel=noopener noreferrer
    - Template class includes prose-pre:bg-purple-950/60 and prose-code:bg-purple-950/40
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown-it rendered HTML → DOM | DOMPurify sanitizes all markdown-it output before v-html insertion |
| highlight.js span classes → DOM | class attr is in DOMPurify DEFAULT_ALLOWED_ATTR — no stripping occurs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm installs (markdown-it-highlightjs, highlight.js) | mitigate | Both packages pre-approved in RESEARCH.md Package Legitimacy Audit; confirmed via npm view and GitHub source repos; no postinstall scripts; slopcheck false-positive on PyPI ecosystem |
| T-14-02 | Tampering | XSS via hljs class attributes | accept | DOMPurify.sanitize() is already called; class is in DEFAULT_ALLOWED_ATTR but script injection via class attribute is impossible per DOM spec |
| T-14-03 | Tampering | Open redirect via markdown link target=_blank | accept | target=_blank is standard browser behavior; rel="noopener noreferrer" is applied (mitigates reverse tabnapping); no user URL input reaches the renderer |
</threat_model>

<verification>
cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit 2>&1 | tail -10
</verification>

<success_criteria>
- npm install adds markdown-it-highlightjs and highlight.js to package.json dependencies
- All 6 MarkdownRenderer tests pass
- MarkdownRenderer.vue contains markdown-it-highlightjs import and .use(highlightjs) call
- MarkdownRenderer.vue contains renderer.rules.table_open with overflow-x-auto wrapper
- MarkdownRenderer.vue contains renderer.rules.link_open with target=_blank
- MarkdownRenderer.vue template div class includes prose-pre:bg-purple-950/60
- npm run test:unit passes without regressions
</success_criteria>

<output>
Create `.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-02-SUMMARY.md` when done
</output>
