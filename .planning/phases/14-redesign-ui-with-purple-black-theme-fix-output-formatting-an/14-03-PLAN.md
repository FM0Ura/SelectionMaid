---
phase: 14
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/formatters.ts
  - frontend/src/lib/formatters.spec.ts
autonomous: true
requirements: [RES-03]

must_haves:
  truths:
    - "slugifyFilename('Calendário de Provas 2026.pdf') returns 'calendario-de-provas-2026'"
    - "slugifyFilename strips file extension and produces only lowercase alphanumeric chars and hyphens"
    - "slugifyFilename collapses multiple non-alphanumeric sequences into a single hyphen"
    - "slugifyFilename trims leading and trailing hyphens"
  artifacts:
    - path: "frontend/src/lib/formatters.ts"
      provides: "slugifyFilename export function"
      contains: "export function slugifyFilename"
    - path: "frontend/src/lib/formatters.spec.ts"
      provides: "describe block with 4 test cases for slugifyFilename"
      contains: "describe('slugifyFilename'"
  key_links:
    - from: "frontend/src/components/result/ResultView.vue (Plan 04)"
      to: "frontend/src/lib/formatters.ts"
      via: "import { slugifyFilename } from '@/lib/formatters'"
      pattern: "slugifyFilename"
    - from: "frontend/src/components/result/ChunkCard.vue (Plan 05)"
      to: "frontend/src/lib/formatters.ts"
      via: "import { slugifyFilename } from '@/lib/formatters'"
      pattern: "slugifyFilename"
---

<objective>
Add the slugifyFilename utility function to formatters.ts and extend formatters.spec.ts with a describe block covering the slug transform behavior including Portuguese diacritic stripping.

Purpose: This utility is a shared dependency for both the global download (Plan 04 — ResultView) and the per-chunk download (Plan 05 — ChunkCard). It must exist in Wave 1 so Plans 04 and 05 can import it in Wave 2.

Output: slugifyFilename function exported from formatters.ts, validated by 4 passing test cases including Portuguese diacritics.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-CONTEXT.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-UI-SPEC.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md

<interfaces>
<!-- Existing formatters.ts exports (append slugifyFilename after line 45): -->
<!-- formatFileSize(bytes: number): string -->
<!-- formatDuration(seconds: number): string -->
<!-- formatDate(isoString: string): string -->
<!-- formatPageRange(pageStart: number, pageEnd: number): string -->

<!-- Test file structure (formatters.spec.ts): -->
<!-- Each function gets its own describe block with it() cases -->
<!-- No vi.mock needed — pure function tests -->
<!-- Import pattern: import { ..., slugifyFilename } from './formatters' -->

<!-- slugifyFilename contract (from UI-SPEC.md): -->
<!-- Input: filename string with extension -->
<!-- Output: lowercase ASCII slug, no extension, no diacritics -->
<!-- Steps: strip extension → lowercase → NFD normalize → strip combining marks (U+0300-U+036F) → replace non-alphanumeric with '-' → collapse multiple '-' → trim leading/trailing '-' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add slugifyFilename to formatters.ts and extend formatters.spec.ts</name>
  <files>
    frontend/src/lib/formatters.ts
    frontend/src/lib/formatters.spec.ts
  </files>
  <read_first>
    - frontend/src/lib/formatters.ts — read the complete file (46 lines) to see all existing exports and the end of file where the function will be appended
    - frontend/src/lib/formatters.spec.ts — read the complete file (31 lines) to see existing describe structure; the new describe block goes at the end of the file
    - .planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-UI-SPEC.md — Download Feature section "File Name Pattern" for the slugifyFilename implementation and example
    - .planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md — formatters.ts section with the exact implementation and test cases
  </read_first>
  <behavior>
    - slugifyFilename('report.pdf') → 'report'
    - slugifyFilename('Calendário de Provas 2026.pdf') → 'calendario-de-provas-2026'
    - slugifyFilename('my  file--name.docx') → 'my-file-name'
    - slugifyFilename('-bad-name-.pdf') → 'bad-name'
  </behavior>
  <action>
    Append slugifyFilename to frontend/src/lib/formatters.ts after the existing formatPageRange function (after line 45). The function signature is `export function slugifyFilename(filename: string): string`. Implementation:
    1. Strip extension: `const base = filename.replace(/\.[^.]+$/, '')`
    2. Chain: `.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '')`

    IMPORTANT: Use the explicit Unicode escape form `̀-ͯ` for the combining diacriticals range — this is safer than literal Unicode characters in TypeScript source and avoids any encoding issues. This range covers all Portuguese accents (à, á, â, ã, é, ê, í, ó, ô, õ, ú, ç and their uppercase variants).

    Extend frontend/src/lib/formatters.spec.ts — add `slugifyFilename` to the import statement on line 2, then append a new `describe('slugifyFilename', () => { ... })` block at the end of the file with exactly four it() cases corresponding to the four behaviors listed above.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit -- --reporter=verbose src/lib/formatters.spec.ts 2>&1 | tail -20</automated>
    <automated>grep -c "export function slugifyFilename" /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/src/lib/formatters.ts</automated>
  </verify>
  <done>
    - formatters.ts exports slugifyFilename function using .normalize('NFD').replace(/[̀-ͯ]/g, '') for diacritic stripping
    - All 4 new slugifyFilename tests pass in addition to the existing 4 formatter tests
    - Total formatters.spec.ts test count: 8 (4 existing + 4 new)
    - npm run test:unit passes without regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slugifyFilename output → anchor.download attribute | Output is used as the download filename attribute; must be safe for filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No npm installs in this plan — pure TypeScript function |
| T-14-04 | Tampering | Malicious filename in download attribute | mitigate | slugifyFilename strips all non-alphanumeric characters; output is restricted to [a-z0-9-]; path traversal sequences like "../" become "---" then collapse to "-", making them safe for use as download attribute values |
</threat_model>

<verification>
cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit -- src/lib/formatters.spec.ts 2>&1 | tail -10
</verification>

<success_criteria>
- formatters.ts contains `export function slugifyFilename(filename: string): string`
- slugifyFilename uses ̀-ͯ Unicode escape (not literal combining chars)
- formatters.spec.ts contains describe('slugifyFilename') with 4 it() test cases
- All 8 formatter tests pass (4 existing + 4 new slugifyFilename tests)
- slugifyFilename('Calendário de Provas 2026.pdf') returns 'calendario-de-provas-2026'
</success_criteria>

<output>
Create `.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-03-SUMMARY.md` when done
</output>
