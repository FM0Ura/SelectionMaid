---
phase: 14
plan: 04
type: execute
wave: 2
depends_on: [14-03]
files_modified:
  - frontend/src/components/result/ResultView.vue
  - frontend/src/components/result/ResultView.spec.ts
autonomous: true
requirements: [RES-03]

must_haves:
  truths:
    - "ResultView header has a 'Download .MD' button between the heading block and 'Novo Upload'"
    - "Clicking Download .MD triggers a browser file download named '{slug}-chunks.md'"
    - "Download button shows CheckIcon and label 'Baixado!' for 1.5s after click, then reverts"
    - "ResultView.spec.ts test 'emits reset when the new upload button is clicked' still passes"
    - "Downloaded .md file starts with YAML front-matter containing title, language, doc_type, ingested_at, chunk_count"
  artifacts:
    - path: "frontend/src/components/result/ResultView.vue"
      provides: "Global download button in header + buildMarkdownContent + downloadAll functions"
      contains: "buildMarkdownContent"
    - path: "frontend/src/components/result/ResultView.spec.ts"
      provides: "Updated selector for reset test + new test for Download .MD button"
      contains: "aria-label=\"Fazer novo upload\""
  key_links:
    - from: "frontend/src/components/result/ResultView.vue"
      to: "frontend/src/lib/formatters.ts"
      via: "import { slugifyFilename } from '@/lib/formatters'"
      pattern: "slugifyFilename"
    - from: "frontend/src/components/result/ResultView.vue"
      to: "URL.createObjectURL / anchor.download"
      via: "downloadAll() function using Blob API"
      pattern: "createObjectURL"
---

<objective>
Add the global "Download .MD" button to ResultView.vue header, implement buildMarkdownContent and downloadAll functions (YAML front-matter + chunk separator format per D-19), update ResultView.spec.ts to fix the broken button selector and add a download button test.

Purpose: Delivers the global download feature (D-16, D-17, D-18, D-19). Depends on Plan 03 for slugifyFilename. Wave 2 plan — can run in parallel with Plans 05 and 06.

Output: Updated ResultView.vue with download logic; updated ResultView.spec.ts with selector fix and download test.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-CONTEXT.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-UI-SPEC.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md

<interfaces>
<!-- ExtractionResponse type (from frontend/src/types/api.ts): -->
<!-- metadata: DocumentMetadata { doc_id, source_filename, title, author, language, doc_type, page_count, chunk_count, ingested_at } -->
<!-- chunks: Chunk[] { chunk_id, content, page_start, page_end, section_title, chunk_index, total_chunks, word_count } -->

<!-- Current ResultView.vue header (lines 30-39): -->
<!-- flex container with: heading block (p + h1) and a single Button variant="outline" @click="$emit('reset')" -->
<!-- Current test (ResultView.spec.ts line 65): wrapper.get('button').trigger('click') — MUST be updated -->

<!-- Generated .md format (D-19, from UI-SPEC.md): -->
<!-- YAML front-matter: --- title: lang: doc_type: ingested_at: chunk_count: --- -->
<!-- Then for each chunk: # Chunk N \n <!-- pages: ... | words: ... | section: ... --> \n \n {content} \n \n --- \n \n -->

<!-- Download button spec (UI-SPEC.md ResultView Header section): -->
<!-- Button variant="outline" size="sm" with DownloadIcon + label "Download .MD" -->
<!-- aria-label: "Baixar todos os chunks como arquivo Markdown" (idle) / "Download concluído" (post-click) -->
<!-- Novo Upload button needs aria-label="Fazer novo upload" for the updated test selector -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add global download button and logic to ResultView.vue; fix ResultView.spec.ts</name>
  <files>
    frontend/src/components/result/ResultView.vue
    frontend/src/components/result/ResultView.spec.ts
  </files>
  <read_first>
    - frontend/src/components/result/ResultView.vue — read the complete file (54 lines) to see current imports, defineProps/defineEmits, chunkListVariants, and template header structure
    - frontend/src/components/result/ResultView.spec.ts — read the complete file (69 lines) to understand the existing test structure, data fixture, and the line 65 selector that must be updated
    - .planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md — ResultView.vue section with full updated imports, download logic, and header template
    - frontend/src/types/api.ts — ExtractionResponse, DocumentMetadata, Chunk interfaces (for buildMarkdownContent)
  </read_first>
  <behavior>
    - After clicking Download button: wrapper.find('[aria-label="Download concluído"]').exists() is true (or text contains 'Baixado!')
    - Clicking "Novo Upload" button (aria-label="Fazer novo upload") emits 'reset'
    - ResultView renders a button with aria-label "Baixar todos os chunks como arquivo Markdown"
    - buildMarkdownContent output starts with '---\n' (YAML front-matter)
    - buildMarkdownContent output contains 'chunk_count: 2' for 2-chunk fixture
  </behavior>
  <action>
    Update frontend/src/components/result/ResultView.vue:

    1. Script imports: add `Check, Download` to lucide-vue-next import (replace `ArrowLeft` with `ArrowLeft, Check, Download`), add `ref` from 'vue', add `import { slugifyFilename } from '@/lib/formatters'`.

    2. After defineEmits block, add: `const downloaded = ref(false)`, then the `buildMarkdownContent(data: ExtractionResponse): string` function, then the `downloadAll(data: ExtractionResponse): void` function. Both functions follow the exact pattern in 14-PATTERNS.md ResultView section. The front-matter uses joined array: ['---', 'title: ' + (meta.title || meta.source_filename), 'language: ' + meta.language, 'doc_type: ' + meta.doc_type, 'ingested_at: ' + meta.ingested_at, 'chunk_count: ' + meta.chunk_count, '---', ''].join('\n'). Chunks separated by '---\n\n' with a '# Chunk N\n' header and HTML comment for page/word/section metadata. After blob download: `downloaded.value = true; setTimeout(() => { downloaded.value = false }, 1500)`.

    3. Template header: wrap the existing Novo Upload Button in a `<div class="flex items-center gap-2">`. Before the Novo Upload button inside that div, insert the Download .MD Button: `variant="outline" size="sm"`, aria-label binding toggles between "Baixar todos os chunks como arquivo Markdown" and "Download concluído" based on `downloaded`, @click="downloadAll(data)". Shows CheckIcon (v-if="downloaded") or DownloadIcon (v-else) + text "{{ downloaded ? 'Baixado!' : 'Download .MD' }}". Add aria-label="Fazer novo upload" to the existing Novo Upload button (needed for test selector).

    Update frontend/src/components/result/ResultView.spec.ts:

    1. Add `vi.stubGlobal('URL', { createObjectURL: vi.fn(() => 'blob:mock'), revokeObjectURL: vi.fn() })` at the top of the describe block (or in a beforeEach).
    2. Fix line 65: change `wrapper.get('button').trigger('click')` to `wrapper.get('[aria-label="Fazer novo upload"]').trigger('click')` — this targets the Novo Upload button specifically.
    3. Add a new it() test: mounts ResultView, finds the button with aria-label "Baixar todos os chunks como arquivo Markdown", triggers click, then asserts the button text contains 'Baixado!' or aria-label changes to 'Download concluído'.
    4. Add `import { vi } from 'vitest'` if not already imported.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit -- --reporter=verbose src/components/result/ResultView.spec.ts 2>&1 | tail -20</automated>
    <automated>grep -c "buildMarkdownContent" /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/src/components/result/ResultView.vue</automated>
    <automated>grep -c "Fazer novo upload" /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/src/components/result/ResultView.vue</automated>
  </verify>
  <done>
    - All ResultView tests pass (2 existing tests updated + 1 new download test = 3 total passing)
    - ResultView.vue contains buildMarkdownContent and downloadAll functions
    - ResultView.vue header has "Baixar todos os chunks como arquivo Markdown" aria-label on Download button
    - ResultView.vue Novo Upload button has aria-label="Fazer novo upload"
    - ResultView.spec.ts reset test uses wrapper.get('[aria-label="Fazer novo upload"]') selector
    - npm run test:unit passes without regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExtractionResponse.metadata → .md file content | API response data flows into the generated Markdown file — no re-sanitization needed since data came from the trusted backend via existing typed API layer |
| slugifyFilename output → anchor.download attribute | Slugified filename is safe (only a-z0-9 and hyphens) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No npm installs in this plan |
| T-14-05 | Information Disclosure | Downloaded .md file contains metadata | accept | Tool is a single-user local tool; the user is intentionally downloading their own extraction results |
| T-14-06 | Tampering | Blob download URL lifecycle | mitigate | URL.revokeObjectURL() is called immediately after anchor.click() to release the object URL and prevent memory leaks |
</threat_model>

<verification>
cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit 2>&1 | tail -10
</verification>

<success_criteria>
- ResultView.vue contains buildMarkdownContent function generating YAML front-matter + chunk separators
- ResultView.vue contains downloadAll function using URL.createObjectURL + anchor.download
- ResultView.vue has "Download .MD" button with aria-label "Baixar todos os chunks como arquivo Markdown"
- ResultView.vue Novo Upload button has aria-label="Fazer novo upload"
- All 3 ResultView tests pass (2 updated + 1 new)
- npm run test:unit passes
</success_criteria>

<output>
Create `.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-04-SUMMARY.md` when done
</output>
