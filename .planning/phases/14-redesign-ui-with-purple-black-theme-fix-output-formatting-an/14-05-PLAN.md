---
phase: 14
plan: 05
type: execute
wave: 2
depends_on: [14-01, 14-03]
files_modified:
  - frontend/src/components/result/ChunkCard.vue
  - frontend/src/components/result/ChunkCard.spec.ts
autonomous: true
requirements: [RES-03]

must_haves:
  truths:
    - "ChunkCard has glassmorphism background (bg-white/5, backdrop-blur-md) and purple border"
    - "ChunkCard shows purple glow on hover via box-shadow transition"
    - "ChunkCard has an icon-only Download button beside the Copy button"
    - "Download button shows CheckIcon for 1.5s after click, then reverts to DownloadIcon"
    - "Clicking ChunkCard Download button triggers file download named chunk-{N}-{section-slug}.md"
    - "ChunkCard content body is capped at max-h-[400px] with overflow-y-auto scroll"
    - "Existing copy behavior is unchanged"
  artifacts:
    - path: "frontend/src/components/result/ChunkCard.vue"
      provides: "Glassmorphism Card + hover glow + download button + max-height scroll wrapper"
      contains: "bg-white/5 backdrop-blur-md"
    - path: "frontend/src/components/result/ChunkCard.spec.ts"
      provides: "Tests for download button presence and CheckIcon feedback"
      contains: "Baixar chunk como Markdown"
  key_links:
    - from: "frontend/src/components/result/ChunkCard.vue"
      to: "frontend/src/lib/formatters.ts"
      via: "import { slugifyFilename } from '@/lib/formatters'"
      pattern: "slugifyFilename"
    - from: "frontend/src/components/result/ChunkCard.vue"
      to: "URL.createObjectURL"
      via: "downloadChunk() Blob API"
      pattern: "createObjectURL"
---

<objective>
Apply glassmorphism + hover glow to ChunkCard, add the per-chunk Download icon button with 1.5s CheckIcon feedback, and wrap the Markdown content area in a max-height scroll container. Extend ChunkCard.spec.ts with download button tests.

Purpose: Delivers D-03 (glassmorphism), D-07 (hover glow), D-14 (max height scroll), D-20 (per-chunk download icon), and D-21 (download feedback). Depends on Plan 01 for purple theme tokens and Plan 03 for slugifyFilename.

Output: Updated ChunkCard.vue with glassmorphism, glow, download button, and scroll cap; extended ChunkCard.spec.ts with 2 new tests.
</objective>

<execution_context>
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/workflows/execute-plan.md
@/home/fmoura-laptop/Documentos/Projetos/SelectionMaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-CONTEXT.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-UI-SPEC.md
@.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md

<interfaces>
<!-- Current ChunkCard.vue — key sections: -->
<!-- imports: useClipboard, Check, Copy, motion, computed, Button, Card, formatPageRange, Chunk, MarkdownRenderer -->
<!-- Card element (line 33): <Card class="w-full overflow-hidden"> -->
<!-- header div (line 34): class="flex flex-col gap-4 border-b border-border p-4 sm:flex-row sm:items-center sm:justify-between" -->
<!-- Copy button (lines 47-58): variant="outline" size="sm" class="self-start" -->
<!-- content div (lines 60-63): <div class="p-4"><MarkdownRenderer :content="chunk.content" /></div> -->

<!-- ChunkCard spec — current: 2 tests -->
<!-- Test "copies raw chunk text" does wrapper.get('button').trigger('click') — targets FIRST button -->
<!-- After adding Download button (first in DOM order) this test BREAKS unless selector is updated -->

<!-- Glassmorphism spec (UI-SPEC.md): bg-white/5 backdrop-blur-md border border-purple-900/40 -->
<!-- Hover glow (UI-SPEC.md): hover:shadow-[0_0_20px_2px_rgba(147,51,234,0.3)] transition-shadow duration-200 -->
<!-- Download button: Button variant="ghost" size="sm" icon-only (no text label) -->
<!-- aria-label: "Baixar chunk como Markdown" (idle) / "Chunk baixado" (after click) -->
<!-- Per-chunk filename: chunk-{chunk_index+1}-{slugified-section_title || 'section-N'}.md -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add glassmorphism, glow, download button, and max-height scroll to ChunkCard.vue; extend spec</name>
  <files>
    frontend/src/components/result/ChunkCard.vue
    frontend/src/components/result/ChunkCard.spec.ts
  </files>
  <read_first>
    - frontend/src/components/result/ChunkCard.vue — read the complete file (66 lines) to see all current imports, the copy button template, and the content div
    - frontend/src/components/result/ChunkCard.spec.ts — read the complete file (65 lines) to see the existing test structure — specifically the line where `wrapper.get('button').trigger('click')` targets the Copy button (this will break if Download is added first)
    - .planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-PATTERNS.md — ChunkCard.vue section with the full updated imports, downloadChunk function, updated Card template with glassmorphism + glow + button order + max-height div
  </read_first>
  <behavior>
    - ChunkCard renders a button with aria-label "Baixar chunk como Markdown" (Download icon button)
    - Clicking the Download button triggers URL.createObjectURL and URL.revokeObjectURL
    - After clicking Download button, aria-label changes to "Chunk baixado" (CheckIcon shown for 1.5s)
    - Existing copy test: wrapper.get('[aria-label="Copiar texto do chunk"]').trigger('click') → copy is called (update selector to be specific)
  </behavior>
  <action>
    Update frontend/src/components/result/ChunkCard.vue:

    1. Script imports: add `Download` to lucide-vue-next import (alongside existing Check, Copy), add `ref` to vue import (alongside existing computed), add `slugifyFilename` to formatters import.

    2. After the existing useClipboard and copyChunk setup, add the download state and function:
       - `const chunkDownloaded = ref(false)`
       - `function downloadChunk(): void` — builds filename as `chunk-${props.chunk.chunk_index + 1}-${sectionSlug}.md` where sectionSlug uses slugifyFilename on section_title if present, else `section-${props.chunk.chunk_index + 1}`. Creates Blob from `props.chunk.content` with type 'text/markdown; charset=utf-8'. Programmatic anchor click via createObjectURL/revokeObjectURL. Sets chunkDownloaded.value = true, then setTimeout 1500ms to reset to false.

    3. Template — Card element: replace `class="w-full overflow-hidden"` with `class="w-full overflow-hidden bg-white/5 backdrop-blur-md border border-purple-900/40 transition-shadow duration-200 hover:shadow-[0_0_20px_2px_rgba(147,51,234,0.3)]"`.

    4. Template — header div: replace the header div's classes from `"flex flex-col gap-4 border-b border-border p-4 sm:flex-row sm:items-center sm:justify-between"` to `"flex flex-col gap-4 border-b border-purple-900/40 p-4 sm:flex-row sm:items-center sm:justify-between"`.

    5. Template — buttons area: replace the single Copy Button with a `<div class="flex items-center gap-2 self-start">` containing TWO buttons:
       - First button (Download, icon-only): `Button variant="ghost" size="sm"`, aria-label binding `chunkDownloaded ? 'Chunk baixado' : 'Baixar chunk como Markdown'`, @click="downloadChunk". Shows `<Check v-if="chunkDownloaded" class="text-primary" aria-hidden="true" />` or `<Download v-else aria-hidden="true" />`. No text label.
       - Second button (Copy, existing): same as before but remove `class="self-start"` since the parent div handles alignment. Update aria-label to `copied ? 'Texto copiado' : 'Copiar texto do chunk'` (no change in semantics, just confirm it's there for test targeting).

    6. Template — content div: wrap MarkdownRenderer inside a scroll container: replace `<div class="p-4"><MarkdownRenderer .../>` with `<div class="p-4"><div class="max-h-[400px] overflow-y-auto"><MarkdownRenderer .../></div></div>`.

    Update frontend/src/components/result/ChunkCard.spec.ts:

    1. Add `vi.stubGlobal('URL', { createObjectURL: vi.fn(() => 'blob:mock'), revokeObjectURL: vi.fn() })` inside the describe block (or in beforeEach for reset between tests).

    2. Fix the existing copy test (line 60): change `wrapper.get('button').trigger('click')` to `wrapper.get('[aria-label="Copiar texto do chunk"]').trigger('click')` — the Download button is now first in DOM order, so `.get('button')` would target the wrong button.

    3. Add two new it() tests:
       - "renders a download button for the chunk" — mounts ChunkCard, asserts `wrapper.find('[aria-label="Baixar chunk como Markdown"]').exists()` is true.
       - "shows CheckIcon feedback on chunk download click" — mounts ChunkCard, clicks `[aria-label="Baixar chunk como Markdown"]`, asserts `wrapper.find('[aria-label="Chunk baixado"]').exists()` is true.
  </action>
  <verify>
    <automated>cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit -- --reporter=verbose src/components/result/ChunkCard.spec.ts 2>&1 | tail -20</automated>
    <automated>grep -c "bg-white/5 backdrop-blur-md" /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/src/components/result/ChunkCard.vue</automated>
    <automated>grep -c "max-h-\[400px\]" /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/src/components/result/ChunkCard.vue</automated>
    <automated>grep -c "Baixar chunk como Markdown" /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend/src/components/result/ChunkCard.vue</automated>
  </verify>
  <done>
    - All 4 ChunkCard tests pass (2 existing updated + 2 new)
    - ChunkCard.vue Card element has bg-white/5 backdrop-blur-md border border-purple-900/40
    - ChunkCard.vue Card element has hover:shadow-[0_0_20px_2px_rgba(147,51,234,0.3)]
    - ChunkCard.vue has max-h-[400px] overflow-y-auto wrapping MarkdownRenderer
    - ChunkCard.vue has Download button with aria-label "Baixar chunk como Markdown"
    - npm run test:unit passes without regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunk.content → Blob → anchor.download | Chunk content comes from the trusted API response; no sanitization needed for local file download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No npm installs in this plan |
| T-14-07 | Tampering | Per-chunk filename in download attribute | mitigate | sectionSlug is produced by slugifyFilename() — restricted to [a-z0-9-]; safe for download attribute |
| T-14-08 | Tampering | Blob object URL memory leak | mitigate | URL.revokeObjectURL() called immediately after anchor.click() in downloadChunk() |
</threat_model>

<verification>
cd /home/fmoura-laptop/Documentos/Projetos/SelectionMaid/frontend && npm run test:unit 2>&1 | tail -10
</verification>

<success_criteria>
- ChunkCard.vue Card has bg-white/5 backdrop-blur-md border border-purple-900/40 hover:shadow glow
- ChunkCard.vue has max-h-[400px] overflow-y-auto wrapper around MarkdownRenderer
- ChunkCard.vue has icon-only Download button with aria-label "Baixar chunk como Markdown"
- All 4 ChunkCard tests pass (2 updated + 2 new)
- npm run test:unit passes without regressions
</success_criteria>

<output>
Create `.planning/phases/14-redesign-ui-with-purple-black-theme-fix-output-formatting-an/14-05-SUMMARY.md` when done
</output>
